# 验证记录

- 最后一次相关测试：27 项全部通过（含原 PoB 导出）。覆盖空路径、取消选择、保存与清空、部分安装、版本未知、重复操作、运行中阻止更新、启动错误、故障回滚、中断恢复、默认配置补齐、已有设置保留、目录联接、ZIP 路径越界/别名/链接/重复项/CRC 损坏。
- 首次完整 `npm test`：2630 项，2628 通过，2 跳过，0 失败。
- 第二轮全量回归：2632 项，2629 通过，2 跳过，1 失败。原有 `runtimeDistribution.test.js` 的 Python 探测约 11.5 秒后失败；探测超时配置为 10 秒。单独复跑该文件 5 项全部通过，探测约 1.1 秒，疑似并发负载导致。原始记录保留在 `artifacts/pob-launcher-tests-final.log`，未修改无关 Python 代码。
- 最终降低并发的全量回归 `npm test -- --test-concurrency=4`：2632 项，2630 通过，2 跳过，0 失败；原 Python 探测约 1.3 秒通过。记录为 `artifacts/pob-launcher-tests-bounded.log`。
- 新卡片与原 PoB 页面通过现有 Vite 开发服务器转换；浏览器检查确认顶部卡片、我的角色、他人角色布局。
- 使用模拟服务挂载真实 Vue 卡片，验证手动路径保存、未知版本、更新进度、busy 禁用输入与按钮、完成恢复操作及清空。该验证不等同原生 Electron 端到端测试。
- 用户提供的本地安装只读检测：两个程序均存在，版本未知，进程查询成功。
- 对本地安装的完整临时副本执行模拟更新，9 个用户文件哈希保持不变；原目录文件哈希一致。验证副本已清理。
- OpenSpec 严格校验、相关 JavaScript 语法检查和 diff 空白检查通过。

## 尚未完成的真实环境验收

- 启动隔离 Electron 开发验证进程被自动审批审查拒绝，工具仅返回 `blocked by policy`。未绕过该限制，未声称实际启动 PoeCharm 已验证。移除了被阻止的临时启动脚本。
- 后续限流修复已验证真实官方版本查询与安装包地址可达（200）；完整在线下载安装的端到端验收尚未执行，没有以查询成功代替完整安装成功。
- 手工验收：重启开发版，在 PoB 导出页关联本地目录并点击启动；关闭两个程序后，在测试目录执行安装和更新。无需打包。

## 修正与清理

- 更新查询限流修复：复现匿名 REST API 返回 403、剩余配额为 0，同时官方 Atom 提交订阅及 releases/latest 页面返回 200。改用官方订阅、稳定版重定向与资产列表，删除旧匿名 API 查询代码，无需 Token。
- 修复后 15 项服务/IPC 测试通过，覆盖 API 不可用、无效订阅、跨站跳转、预发布标签、错误版本资产和校验值保留。真实查询两个版本及官方安装包 HEAD 均成功（200）；本次没有对原安装执行更新。
- 限流修复后全量回归 `npm test -- --test-concurrency=4`：2633 项，2631 通过，2 跳过，0 失败；日志位于 `artifacts/pob-launcher-rate-limit-tests.log`。

- ZIP 旧版流的异步迭代读取导致测试等待；改用事件读取完成 CRC 校验后相关测试通过，旧实现已删除。
- 独立 Vite 检查命令遇到引号和热更新端口冲突，改用现有开发服务器成功验证。
- 临时 Vue 验证页最初重复引入不同 Vue 预构建实例，改为标准模块导入后通过；验证结束后已删除临时页面。

## 阶段进度与停止验收（2026-09-13）

- 普通 Node 24.14 可处理缓存包，Electron 43 / Node 24.17 在旧 ZIP 读取流等待 EOF 时挂起；真实包校验停在约 11%。旧 fd-slicer 在 push(null) 前设置 destroyed，使用 yauzl RandomAccessReader 配合原生 Readable 生命周期后，在 Electron 主进程完成全部 83,773,006 字节的校验与解压。结果见 `artifacts/pob-electron-check.json`。
- 排查中试用共享句柄的 createReadStream 导致后续读取 EBADF；已撤除，改为 FileHandle 分段读取并由归档生命周期统一关闭。临时控制台诊断、定时器及不再使用的 extract-zip 直接依赖已移除。
- 当次用户界面的原始 IPC 拒绝没有保留堆栈，不能证明所有“连接失败”均由 EOF 问题引起；新增阶段日志、通知隔离和通信恢复，测试分别覆盖观察者抛错及 IPC 拒绝。
- 相关服务、IPC、真实 Vue 组件与原导出测试共 37 项通过；覆盖各阶段取消、空包/空文件、未知大小、替换回滚、提交后清理失败、旧状态过滤及通信恢复。Electron 自带 Node 运行同一服务测试也通过。
- Electron 主进程在临时安装副本上使用真实 PoeCharm 缓存归档完成更新，保留 3 个设置/BD/新增文件的 SHA-256；PoB 组件版本不变分支保留已有副本。结果见 `artifacts/pob-update-smoke.json`；两个组件同时更新由模拟归档服务测试覆盖。
- 本地安装两个组件识别成功，启动 PoeCharm 后进程检查确认运行。未在原目录执行更新。
- 全量 `npm test -- --test-concurrency=4`：2642 项，2640 通过，2 跳过，0 失败；日志 `artifacts/pob-launcher-progress-tests.log`。
- 启动卡片、导出页和前端 API 的 Vite 开发转换通过，OpenSpec 严格校验通过。不执行打包。
- 初次独立 Vite 检查结束时触发依赖预构建取消与 HMR 端口冲突；最终验证脚本关闭自身预构建、预热及 HMR 后正常退出，未更改应用 Vite 配置。
- 自动审批拒绝批量删除诊断临时目录（仅返回 blocked by policy），未绕过；部分诊断临时目录保留在系统临时目录中。
