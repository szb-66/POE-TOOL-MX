## 1. CloudBase 数据与权限

- [x] 1.1 只读确认目标完整 EnvId 的 PostgreSQL 后端、匿名登录状态、现有 `app_feedback` 身份语义和最新 migration 版本，不使用隐式环境或 MySQL/NoSQL 路径
- [x] 1.2 添加并应用初始版本化 PG migration，创建 `app_presence` 表、最后在线索引、3 分钟在线汇总视图和最初设计的 `report_app_presence` RPC
- [x] 1.3 查询官方 PG HTTP OpenAPI并确认其未提供 RPC 路径，保留已应用迁移历史并确定改用文档化表 Upsert + 触发器/RLS
- [x] 1.4 应用第一次纠正 migration 删除 RPC并尝试基础表 Upsert，真实匿名测试确认冲突更新因不可授予 SELECT而返回 `DATABASE_42501`
- [x] 1.5 添加、plan并应用最终收口 migration，撤销基础表直写并删除旧触发器/策略，创建只允许 INSERT 的 `app_presence_heartbeat` 视图及内部 Upsert 安全触发器，随后核验对象、权限和远端历史

## 2. 主进程心跳实现

- [x] 2.1 增加独立在线心跳客户端与服务，复用现有匿名认证窄接口，实现官方 PG REST只写视图 INSERT、401 单次重新认证、最小字段映射和安全错误边界
- [x] 2.2 实现立即上报、60 秒周期、单请求门禁、`unref` 计时器与幂等 `start`/`stop`，确保网络失败只等待下一周期
- [x] 2.3 在取得单实例锁并完成主服务初始化后非阻塞启动心跳，在统一资源清理时停止计时器，并保持 renderer/preload 接口不变

## 3. 自动化测试

- [x] 3.1 添加心跳服务单元测试，覆盖首次和周期调度、重叠调用合并、停止、字段最小化、开发/正式模式、401 单次重试和普通错误隔离
- [x] 3.2 添加 migration 安全测试，验证桌面角色只能向心跳视图 INSERT，不能读取基础表、心跳视图或汇总视图，且触发器拒绝通用身份并仅接受最小字段
- [x] 3.3 增加环境变量门禁的真实 CloudBase 开发环境测试：写入 `development` 心跳、管理端读回服务端时间与汇总，并确认通用身份写入和匿名读取被拒绝

## 4. 验证与收尾

- [x] 4.1 运行受影响的 Node 测试并在开发版启动应用，确认窗口展示不等待心跳且 CloudBase 故障不影响其他功能
- [x] 4.2 运行完整 `npm test` 和 OpenSpec 严格校验，不执行安装包构建
- [x] 4.3 检查工作区差异，保留现有反馈诊断和全局急停改动，删除任何失败尝试留下的重复心跳、无用配置或宽松权限
- [x] 4.4 按 CloudBase code review 技能检查 PG 模式、认证、凭据、权限和自验证要求，修正发现的问题后记录最终验证结果
