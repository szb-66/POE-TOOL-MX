## 1. 组合折叠交互

- [x] 1.1 在 `ModuleTwo.vue` 中增加按组合 ID 隔离的本地收起状态与切换、删除清理逻辑，确保折叠不触发预设提交。
- [x] 1.2 在组合标题栏加入带动态图标和可访问标签的展开/收起按钮，仅按状态隐藏条件内容区，并保持标题栏管理操作可用。

## 2. 自动化回归测试

- [x] 2.1 扩展 `test/itemAffixUi.test.js`，覆盖组合默认展开、独立切换所需的状态绑定、折叠控制可访问性以及条件内容区的条件渲染。
- [x] 2.2 覆盖新增和复制组合默认展开、折叠不提交预设、删除组合清理本地状态且不改变原配置的行为边界。

## 3. 开发版验证

- [x] 3.1 运行 `node --test test/itemAffixUi.test.js` 和完整 `npm test`，确认既有物品制作行为无回归。
- [x] 3.2 启动 Vite 开发版并在 `/#/items` 验证默认展开、多个组合独立收起/展开、新增与复制默认展开，以及收起后配置保持不变；不执行应用打包。
- [x] 3.3 运行 OpenSpec 严格校验和差异格式检查，确认仅保留本次有效改动且不覆盖工作区中其他未提交内容。
