## 1. 实现与验证

- [x] 1.1 两个圣所浮窗设置 thickFrame: false，检查窗口创建配置并运行现有圣所浮窗与拖动测试。
- [x] 1.2 运行 npm test 与 OpenSpec 严格校验，记录结果及验证限制。

验证记录：相关测试 42/42 通过；npm test 共 2353 项，2351 通过、2 跳过、0 失败；OpenSpec 严格校验通过。未打包，未进行实际游戏内目视验证；窗口创建配置须重启开发版生效。

## 2. 用户反馈后的修正

- [x] 2.1 首次显示前禁用窗口 DWM 过渡，验证原生调用成功、异步初始化顺序、失败与销毁处理。
- [x] 2.2 运行相关测试、全量测试和严格校验，记录真实窗口验证结果与限制，检查失败方案遗留代码。

修正验证：相关测试 37/37 通过，包含真实 Windows 隐藏窗口 DWM 调用；开发版 Electron 260×56 与 1280×720 两种透明窗口均成功设置 DWM 后连续显隐三次且坐标不变。验证脚本首次因关闭最后一个窗口自动退出而未完成第二个窗口验证，修正脚本后两种尺寸通过。尝试应用到旧运行窗口时句柄归属检查失败，未修改旧窗口或其他窗口。

全量测试 2360 项：2356 通过、2 跳过、2 失败。失败为 sanctumPresentation 的奖励数量摘要和 sanctumRewards 的通货图标识别；对应实现文件在此次排查期间发生其他修改，本变更未编辑这些逻辑。OpenSpec 严格校验通过。未打包，未进行实际游戏内目视验证，需重启开发版加载新主进程代码。

遗留检查：保留 thickFrame: false 作为无框样式配置，动画禁用由 DWM 调用负责；没有增加无效 CSS 或每次显示重复启动子进程的补丁。

## 3. 中心放大与闪烁的实际复现修正

第 2 阶段仅证明 DWM 调用成功，并未解决用户问题；以上记录为失败方案历史。

- [x] 3.1 用局部 Chromium 显隐开关替换 DWM 方案，删除无效异步初始化及对应测试，验证开关恢复和既有显隐生命周期。
- [x] 3.2 对实际遮罩及按钮逐帧验证首次显示与恢复无缩放、无额外闪烁，并运行相关测试及 OpenSpec 校验。

最终验证：开发版 Electron + Vite 实际 SanctumOverlayView 与 SanctumControlOverlayView，诊断时用轮廓颜色跟踪屏幕像素；各显示/隐藏两次。路线窗口全部可见帧边界为 [0,0,900,600]，按钮全部为 [0,0,390,84]；各恰好两段可见区间，无中间缩放帧、无额外闪灭。此前 DWM 对照仍记录到 881×587、892×595、900×600 的中心放大，证明前两次修复无效。复现过程无游戏输入；实际游戏交互环境仍需用户重启开发版核对。

相关测试 34/34 通过；最终 npm test 共 2361 项，2359 通过、2 跳过、0 失败。OpenSpec 严格校验通过。已删除失败的 DWM 子进程、异步门控、对应测试及过时诊断脚本；保留实际画面诊断脚本和帧数据在 .cache 中供复查。未打包。

用户验收：2026-09-10，用户明确反馈“修复成功”，游戏内效果已确认，完成验收。
