## 验证结果

- 新增效果纠正回归：8 项通过，覆盖楼层上下文、部分处理、过期拒绝、词库规则、其他来源保留和历史保存。
- 相关识别、规则、证据、展示及效果更新测试：42 项通过；最后追加回归与开发转换组合：13 项通过。
- Vue 开发转换包含新增 SanctumEffectReview，脚本及 Less 均通过。
- 使用正在运行的 electron:dev / Vite 开发服务器，以独立内存测试状态连接真实纠正服务进行浏览器交互验证：问题入口、缺图提示、取消、排除原文、搜索替换、添加词条、空选择禁止提交、应用后分类及人工标记均正确。未修改实际游戏记录，未执行游戏采集或打包。
- OpenSpec 严格校验通过。
- 首次 npm test：2579 项通过、1 项失败、2 项跳过；失败为内置 Python 运行时探测在全量并发负载下未就绪。独立复跑 runtimeDistribution：5 项通过。
- 最终 npm test -- --test-concurrency=4：2582 项通过、2 项跳过、0 失败（2584 项，约 226 秒）。

## 清理与生效

- 临时界面验证外壳最初因重复依赖实例及 Vue 运行时模板编译限制未渲染；统一依赖并改用 render 后完成验证，临时页面、API 服务及日志均删除，没有把测试外壳修改引入产品。
- 当前已启动的开发进程需要重启，才能加载新增主进程服务和 preload IPC 接口；页面热更新本身不能加载这些接口。
