## 1. 推荐模型与协议

- [x] 1.1 增加校准测量、进度、结构化错误和逐字段证据的规范化协议，确保跨 IPC 数据不包含物品原文
- [x] 1.2 实现稳定优先推荐纯函数，覆盖 `guard`、实测字段、推导字段、取整、默认/当前值下限和置信度回退
- [x] 1.3 为候选搜索与推荐函数补充单元测试，覆盖 5/5、15/15、上限、总时限、P95和无画面信号场景

## 2. 安全测量服务

- [x] 2.1 增加只移动鼠标和发送 Ctrl+C 的校准 Python 脚本，实现五格基线、分阶段搜索、剪贴板/画面测量、脱敏输出和剪贴板恢复
- [x] 2.2 实现可中断等待、stdin 合作式取消、逐输入前台门禁、输入释放与异常清理，并用 Python 模拟检查覆盖无点击、焦点丢失和取消
- [x] 2.3 增加 Electron 校准服务，接入 Python 运行时、一次性配置、180 秒时限、自动化互斥、进度转发、停止兜底和设置窗口恢复
- [x] 2.4 把校准服务接入全局紧急停止和运行时资源路径，并为锁冲突、重复停止、子进程错误、脱敏结果和清理补充测试

## 3. IPC、设置应用与撤销

- [x] 3.1 增加 start/stop/progress 的 preload 和渲染端 Electron API，使用现有序列化边界并提供非 Electron 回退
- [x] 3.2 在设置 store 中实现校准成功后的完整时序原子应用、失败不保存和会话级校准前快照
- [x] 3.3 实现撤销本次检测，并在手工时序更新成功、再次检测、设置重置时清理快照；补充原子回滚和撤销边界测试

## 4. 设置页体验

- [x] 4.1 在操作延迟区域增加自动检测入口和 12×5 背包准备引导，明确第一列五个不同单格物品、背包打开和紧急停止要求
- [x] 4.2 实现缺少背包网格时的禁用/跳转、检测阶段进度、停止操作、结构化错误提示和结束后的窗口恢复状态
- [x] 4.3 实现自动应用后的原值/推荐值/实测或推导结果表和会话撤销按钮，并补充设置页、手工修改及重置测试

## 5. 验证与规范收尾

- [x] 5.1 运行校准、自动化时序、设置、IPC和紧急停止定向测试，并修复所有失败及失败尝试遗留
- [x] 5.2 运行完整 `npm test`、`npm run build`、`git diff --check` 和 OpenSpec 严格校验，不执行打包
  - 2026-08-30 完整测试 1701 项中 1699 项通过、2 项按既有条件跳过、0 项失败；开发构建、差异检查和严格 OpenSpec 校验均通过，未执行打包。
- [x] 5.3 使用 `npm run electron:dev` 验证有效五格、重复/缺失物品、焦点丢失、紧急停止、自动应用、手工修改清除撤销和撤销恢复；记录无法在当前环境完成的游戏内验收项
  - 2026-08-30 开发版已验证设置入口、12×5 第一列引导、三项确认门禁、当前紧急停止快捷键、找不到游戏的结构化错误、设置窗口恢复及原值保持；当前环境未运行游戏，无法安全执行有效/重复五格、运行中失焦、快捷键停止、自动应用与撤销的真实游戏输入验收，这些路径已由 Python 模拟、Electron 服务和设置状态定向测试覆盖。

## 6. 鼠标关键时序安全修订

- [x] 6.1 将 `releaseSettleMs` 改为带当前值与默认值下限的推导建议，并把逐字段证据从鼠标实测语义修正为键盘测量依据
- [x] 6.2 增加实测为零、当前值较高、默认下限与来源标记回归，并运行校准定向测试与严格规范校验
