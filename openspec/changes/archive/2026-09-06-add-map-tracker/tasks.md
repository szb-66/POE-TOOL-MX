## 1. 日志事件与区域数据

- [x] 1.1 扩展 Client.txt 解析器和服务订阅，发布带实例种子的区域、死亡、击杀结果、角色资料与验证机制事件，并保持原始行隔离。
- [x] 1.2 新增版本化 PoE1 地图、Boss、城镇/藏身处与附属区域目录，实现中文名优先和未知 ID 安全回退。
- [x] 1.3 为新事件、中英文特征、实例种子、区域分类与事件白名单增加单元测试。

## 2. 地图模型、状态机与持久化

- [x] 2.1 实现规范化设置、历史记录和经验效率纯函数，对未知等级返回 `null`。
- [x] 2.2 实现地图生命周期状态机，覆盖新实例、回城再入、附属区域、暂停、前台/空闲计时、手动保存与丢弃。
- [x] 2.3 实现 `<userData>/map-tracker/` 中的设置、活动草稿和赛季/月份记录仓储，使用原子写入并对损坏分片只读隔离。
- [x] 2.4 实现历史筛选、排序、分页、允许字段编辑、确认删除和 UTF-8 BOM CSV 导出。
- [x] 2.5 增加状态机、计时门禁、退出恢复、原子仓储、损坏隔离、查询编辑和 CSV 转义测试。

## 3. 主进程服务与安全采集

- [x] 3.1 组装 MapTrackerService，订阅结构化日志、游戏前台和系统空闲状态，维护单一快照并完成正常关闭/崩溃草稿生命周期。
- [x] 3.2 实现地图开始及离开时自动 `/kills` 采样和手动补采，并实现手动 `/whois`；校验模块、功能开关、活动局、游戏前台与节流，禁止任意命令输入。
- [x] 3.3 复用悬停物品解析实现“记录为下一张地图”、备注、投入物和重要掉落的白名单采集。
- [x] 3.4 实现默认关闭、可立即停止的 `Ctrl+单击` 入库监听，只在前台和仓库/背包门禁通过时拦截，并确保成功或失败都只重放一次原点击。
- [x] 3.5 接入日志机制目录和用户校准的图像机制识别，对当前局幂等记录，未校准项不执行。
- [x] 3.6 为主动命令、前台拒绝、节流、物品白名单、单次点击重放和机制去重增加安全测试。

## 4. IPC、配置引导与功能模块

- [x] 4.1 新增经过字段、长度、枚举和路径限制的地图跟踪 IPC/preload/API，覆盖快照、设置、查询、当前局、编辑删除、导出、命令、采集和浮窗。
- [x] 4.2 将 Client.txt、掉落识别和机制模板问题接入现有配置引导，按当前分支持续重检并只执行一次原启用操作。
- [x] 4.3 将 `map-tracker` 注册为仅首页可见的非导航功能模块和运行时适配器，不创建侧边栏页面或页面路由，并安全实现移除、草稿保留、重新添加和专属快捷键恢复。
- [x] 4.4 增加击杀刷新键和显式采集键的默认配置、捕获、重复校验、模块过滤与前台分发，并移除地图浮窗显隐快捷键。
- [x] 4.5 为 IPC 输入拒绝、引导取消/继续、模块移除/恢复、无重复导航入口和快捷键门禁增加集成测试。

## 5. 首页卡片与历史抽屉

- [x] 5.1 新增地图跟踪 Pinia store 作为主进程快照的薄适配层，完成初始化、订阅、重连和错误收敛。
- [x] 5.2 在首页“检测”分组加入地图跟踪状态卡，展示当前地图、阶级、计时、今日次数和平均耗时，并提供启用、暂停/继续、击杀刷新与明细操作。
- [x] 5.3 实现首页响应式地图跟踪抽屉，包含当前局操作、增强开关/角色设置、赛季与日期筛选、分页历史表格、行详情、编辑删除和 CSV 导出。
- [x] 5.4 增加首页汇总、卡片操作防重、抽屉查询/响应式布局、记录编辑删除和导出反馈测试。

## 6. 游戏内浮窗

- [x] 6.1 复用现有窗口管理创建地图跟踪 BrowserWindow，接入加载感知快照发布、浮窗开关与游戏前台常显策略、DPI 和位置持久化。
- [x] 6.2 实现浮窗 Vue 界面，展示地图、阶级、有效计时、机制、备注和角色，并支持默认点击穿透、显式交互、藏身处拖动和重置。
- [x] 6.3 移除浮窗显隐快捷键，使用浮窗开关控制活动地图期间常显，并在关闭、失焦、暂停、模块移除和紧急停止时立即收敛。
- [x] 6.4 增加浮窗开关常显、关闭隐藏、前台收敛、点击穿透和位置持久化测试，并确认不存在浮窗显隐快捷键。

## 7. 隐私、归属与验收

- [x] 7.1 检查快照、历史、诊断和错误路径，确保不包含原始日志、完整剪贴板、截图、账号凭据或完整用户路径。
- [x] 7.2 若实际复用 Exile-UI 的 MIT 数据或算法，更新 `THIRD_PARTY_NOTICES.md`；否则记录为独立实现且不复制其图标素材。
- [x] 7.3 运行受影响的 Node 测试、完整 `npm test`、Vite 开发构建与 `openspec validate add-map-tracker --strict`，清理失败尝试遗留代码。
- [x] 7.4 在 Electron 开发版手动验收日志启用引导、切图/回城/死亡、`/kills`、`/whois`、入库掉落、机制识别、浮窗、重启恢复和 CSV 导出；不执行打包。
