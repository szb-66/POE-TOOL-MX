## 1. 配置包与预设纯逻辑

- [x] 1.1 实现 `ConfigBundleV1` 构建、UTF-8 字节限制、解析、主版本/区块版本兼容性和安全错误码纯函数。
- [x] 1.2 实现稳定区块元数据、字段白名单、隐私/绝对路径过滤和 map/chart `deviceGrid` 双重选择数据模型。
- [x] 1.3 实现预设传输记录、无冲突导入名称、新顶层/嵌套 ID、未配置网格、工具站点和制作价格合并纯函数。
- [x] 1.4 为格式、隐私、兼容性、坐标剥离、预设重复导入及集合合并补充纯单元测试。

## 2. Electron 文件与主进程数据接口

- [x] 2.1 实现受主窗口发送者约束的 `config-transfer:open`/`config-transfer:save` IPC，包括 `.json` 对话框、5 MiB 限制、原子替换和路径脱敏。
- [x] 2.2 在 preload 与 renderer API 中暴露配置传输接口，并为非 Electron 开发环境提供可预测的失败 mock。
- [x] 2.3 为制作价格覆盖增加经过校验的完整快照和原子替换接口，供提交与回滚复用。
- [x] 2.4 覆盖文件取消、超限、非主窗口、原子保存清理、安全返回结构和价格覆盖恢复测试。

## 3. 区块描述器与业务适配

- [x] 3.1 建立区块描述器注册表，接入通用、赛季、时序、覆盖层及本机快捷键/窗口/屏幕/战斗设置的白名单导出、normalize、snapshot、apply 和 rollback。
- [x] 3.2 接入物品、地图、海图、商城、剧情和技能预设描述器，确保逐项选择、新身份、当前预设保持和资产/坐标边界。
- [x] 3.3 接入背包、查价、混沌配方、仓库取件、君锋、九宫格、工具站点和制作价格描述器，并强制持续输入模块导入后关闭。
- [x] 3.4 提取并复用现有 store normalizer，增加各区块只修改所属字段且不泄露私密/临时数据的适配器测试。

## 4. 导入预览与安全事务

- [x] 4.1 实现无副作用导入预览，计算可选区块、默认预设选择、计划导入名称、冲突数、坐标和兼容性警告。
- [x] 4.2 实现单队列导入事务：全候选预校验、完整快照、按需紧急停止、候选运行时同步、持久化、后续同步和逆序回滚。
- [x] 4.3 复用快捷键先注册后保存及查价回滚链路，并保证一次性任务不启动、相关持续模块保持关闭。
- [x] 4.4 通过故障注入测试验证预校验零写入、停止失败、快捷键失败、中途失败、完整回滚、回滚异常强制关闭和并发提交门禁。

## 5. 设置页界面

- [x] 5.1 在“设置 → 系统”增加全宽配置传输卡片，保持七个 Tab、路由和全局重置入口不变。
- [x] 5.2 实现导出弹窗的分组/逐预设选择、“可分享预设”“完整备份”“清空”、坐标风险确认和安全文件名保存。
- [x] 5.3 实现导入文件预览、默认只选预设、逐项/坐标选择、最终确认、事务进度和按区块结果摘要。
- [x] 5.4 增加取消、格式/大小/兼容性/停止/同步/回滚错误中文反馈及 Vue 组件行为测试。

## 6. 验证与规范收尾

- [x] 6.1 运行配置传输、Electron IPC、预设及运行时事务目标测试并修复所有确定失败。
- [x] 6.2 运行 `npm test`、`npm run build`、`git diff --check` 和严格 OpenSpec 校验，不执行打包命令。
- [x] 6.3 使用 `npm run electron:dev` 验证单制作预设、海图分享、重名、坐标剥离/接收、完整备份、损坏文件及自定义资产保持。
- [x] 6.4 同步本变更 delta specs 到主规范，并确认既有海图识别活动变更和未提交文件未被修改或混入。
