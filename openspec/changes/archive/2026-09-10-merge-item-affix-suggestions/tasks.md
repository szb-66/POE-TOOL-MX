## 1. 实现

- [x] 1.1 修改安全聚合、搜索顺序及效果标题。
- [x] 1.2 调整共用联想组件，标注示例并保证来源布局。
- [x] 1.3 增加聚合边界、搜索及匹配物等和预设兼容测试。

## 2. 验证

- [x] 2.1 运行相关测试、全量测试、Vite 开发转换及 OpenSpec 严格校验。
- [x] 2.2 开发版检查每秒再生及通用、精华、花园入口的选择、T 级和保存重载。

验证记录：新增 4 项测试通过；全量 2242 项中 2240 通过、2 跳过、0 失败。共用条件行、目标编辑器、通用模块、专用面板的 Vite 开发转换通过，OpenSpec 严格校验通过。

2.2 开发版复验通过：关闭旧进程后以 npm run electron:dev 重新启动，三个制作入口均实际搜索到唯一的“专注时每秒再生”候选，来源显示“工艺台 / 隐匿”，示例标签正常。花园每秒再生查询保留固定值、百分比和不兼容物等候选。临时预设选中合并项后显示“不限 T (60)”，可切换至“最低 T1 (60)”；切换页面并 Ctrl+R 重载后词缀、预设和阶级物等均保留。临时预设及其他入口临时搜索行已删除，回到原花园默认预设“不限 T (68)”。未启动制作操作，未打包。此前旧进程界面验证未成功，本次重启复验已补齐。
