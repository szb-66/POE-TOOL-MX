## 1. 交易目录负向元数据

- [x] 1.1 升级交易目录 schema，在 stat 记录中校验可选 `negatedMatchers` 为 `matchers` 的唯一子集，并为损坏或冲突数据增加拒绝测试
- [x] 1.2 修改固定 Awakened 数据生成和 stat ID 合并，保留 matcher 对象的 `negate` 标记并确定性生成 `negatedMatchers`
- [x] 1.3 修改 Sidekick 简繁转换、官方本地化目录刷新与降级合并，使负向 matcher 元数据跨所有目录路径保持不变
- [x] 1.4 重建内置交易目录并增加全部固定负向 matcher 的覆盖审计，确认同一输入可重复生成相同结果

## 2. 有符号解析与显示边界

- [x] 2.1 在运行时 matcher 命中后按 `negatedMatchers` 转换全部捕获值，并保留原始词缀及来源文本
- [x] 2.2 让正负文案都以正数建立浮窗显示边界，并在负向词条生成官方查询时乘以 `-1`、交换 `min/max`
- [x] 2.3 让词缀合并、综合条件、歧义候选选择、模型清理和重新查询全程保留查询方向、官方 values 与正数手动边界
- [x] 2.4 增加“回复速度加快 45%”与“回复速度减慢 45%”浮窗模型及官方查询 JSON 回归，覆盖负向单边界、双边范围和至少一个非药剂负向词条

## 3. 设置与浮窗

- [x] 3.1 从默认设置、规范化白名单、主进程选项、store 同步和捕获载荷中删除 `valueRange`，验证旧值被忽略且后续保存不再写入
- [x] 3.2 删除查价页面和浮窗的“数值范围”控件及相关双向同步，保留词缀与属性行的手动最小值和最大值输入
- [x] 3.3 增加浮窗回归，确认负向词缀显示剪贴板原文与正数边界，且页面、浮窗和设置快照均不再暴露范围选项

## 4. 未鉴定传奇药剂

- [x] 4.1 使用真实“未鉴定圣化生命药剂”剪贴板格式回放当前内置目录，断言自动解析“卡鲁之血”、药剂大类、底材和 `identified=false`
- [x] 4.2 增加药剂底材唯一候选、多候选、无候选及选择后查询测试，确认未解决身份不会请求官方接口
- [x] 4.3 验证候选选择和模型清理不会丢失药剂 category、未鉴定事实或可信候选图片字段

## 5. 验证与收尾

- [x] 5.1 运行查价目录、模型、设置、浮窗与未鉴定传奇相关 Node 测试，并使用 Vite 开发转换验证两个查价 Vue 页面
- [x] 5.2 运行完整 `npm test`、`git diff --check` 和 `openspec validate support-unidentified-unique-flasks-and-signed-stats --strict`
- [x] 5.3 审查与当前地图/海图及其他未提交改动的重叠差异，删除失败尝试遗留代码并确认没有覆盖用户工作
- [x] 5.4 在已登录开发版中分别用未鉴定传奇药剂和负向词条执行真实国服搜索并核对网页市集条件；若当前环境未认证则如实记录未验证，不进行打包
  - 用户已在游戏内确认未鉴定传奇药剂与负向词条的实际查询均正常；本次不进行打包。
