## 1. 默认值与兼容行为

- [x] 1.1 将地图与海图共用 rolling profile 中的崇高石、瓦尔宝珠和自动存仓默认值统一为关闭，保持配置结构与页面绑定不变
- [x] 1.2 核对预设规范化和嵌套海图迁移只补齐缺失字段，并保留显式 `true`/`false`；若现有合并逻辑不足则在迁移层修正

## 2. 回归测试

- [x] 2.1 扩展地图预设迁移测试，覆盖空配置和缺字段旧配置的三项开关均为关闭
- [x] 2.2 覆盖航海海图初始/迁移配置，并验证地图与海图显式启用的三项开关均被保留
- [x] 2.3 运行受影响的预设迁移与地图收尾目标测试，确认用户主动启用后的既有处理顺序未回归

## 3. 开发版验证

- [x] 3.1 运行全量测试与 Vite 构建，确认共享默认值调整未影响其他模块
- [x] 3.2 在开发版分别查看异界地图和航海海图的新建预设，确认三项复选框默认未勾选且手动启用可保存
- [x] 3.3 运行 `openspec validate disable-map-optional-action-defaults --strict` 并处理全部校验问题
