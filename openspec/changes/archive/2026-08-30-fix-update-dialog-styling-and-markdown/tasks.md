## 1. 回归契约

- [x] 1.1 为受限 Markdown 块级与行内语法、实际 1.2.2 发布说明和畸形输入新增解析器测试
- [x] 1.2 为原始 HTML、事件属性和非 HTTP(S) 链接新增安全降级测试
- [x] 1.3 扩展更新集成契约，要求主入口显式加载 MessageBox CSS、三处入口共用组件且不使用 HTML 注入

## 2. 安全 Markdown 展示

- [x] 2.1 实现无 DOM 依赖的受限 Markdown 解析器，输出固定块级与行内节点模型
- [x] 2.2 实现共享 Vue 展示组件及 scoped Less，为标题、列表、重点、代码和链接提供一致语义与样式
- [x] 2.3 在主渲染入口显式引入 Element Plus MessageBox CSS

## 3. 更新界面接入

- [x] 3.1 将升级后首次启动弹窗替换为共享 Markdown 组件，保留已读确认、关闭与滚动行为
- [x] 3.2 将标题栏下载前确认弹窗替换为共享 Markdown 组件，保留版本/日期信息和确认门禁
- [x] 3.3 将设置页发布说明替换为共享 Markdown 组件，保留更新状态和操作布局

## 4. 验证与清理

- [x] 4.1 运行 Markdown 解析与应用更新定向测试，确认格式、安全降级和原更新流程契约
- [x] 4.2 运行完整 `npm test`、`npm run build` 和 OpenSpec 严格校验，并确认首页初始 CSS 包含 `.el-message-box` 核心规则
- [x] 4.3 仅在开发版用 1.2.2 说明验证三处界面的布局、滚动、深色主题、Markdown 层级与外链行为，不执行 Electron 打包
- [x] 4.4 核对工作区差异，删除临时调试内容并确认未覆盖标题栏、设置页及其他已有未提交修改
