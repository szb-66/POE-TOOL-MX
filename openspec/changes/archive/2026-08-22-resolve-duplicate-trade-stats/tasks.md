## 1. 交易目录元数据

- [x] 1.1 扩展目录 schema 和校验器，支持根级 `statEquivalenceGroups` 与按 Provider/精确游戏版本约束的 `unqueryableStats`，并为无效成员、类型冲突和空版本添加校验回归
- [x] 1.2 从腾讯官方 canonical stat 的 type、标准化中文文案和数值占位签名确定性生成等价组，为成员和 group key 建立稳定排序，并验证当前固定数据重复生成结果一致
- [x] 1.3 增加腾讯国服 3.29 的 `implicit.stat_1571797746` 不可查询记录，重生成内置目录，并验证该记录仍参与覆盖审计但不进入当前版本查询候选
- [x] 1.4 让在线目录刷新重新生成等价组并精确应用当前 Provider/版本排除清单，回归新版本不继承 3.29 结论以及刷新后元数据仍有效
- [x] 1.5 扩展覆盖审计输出重复组、等价组、当前版本不可查询记录和拒绝原因，同时保持静默丢弃数为零

## 2. 解析与逻辑词缀模型

- [x] 2.1 在现有类型、类别、词缀元数据、local/global 和多行规则缩小候选后过滤当前版本不可查询 ID，并回归过滤后唯一候选按普通 stat 处理
- [x] 2.2 对属于同一可信等价组且 type、捕获值数量和数值方向一致的剩余候选生成一条逻辑 stat 及排序后的 `queryVariants`，并确保聚合值和来源只计算一次
- [x] 2.3 为 implicit、explicit、enchant、crafted 和 crucible 代表性重复组添加解析回归，并验证别名碰撞、占位结构冲突或数值方向冲突仍进入人工消歧
- [x] 2.4 加入用户胸甲剪贴板回归，验证“阻挡受到的 +50% 反射伤害”唯一解析为 `implicit.stat_2510655429`、方向为正且不产生未识别候选

## 3. 信任边界与官方查询

- [x] 3.1 扩展主进程模型清理，忽略渲染层提交的变体 ID、等价组、type 和方向，从当前可信目录重建有效变体
- [x] 3.2 添加安全回归，验证伪造组成员、当前版本不可查询 ID、类型/方向篡改及旧模型中的无效 ID 都会被清理且不会进入请求
- [x] 3.3 保持单一有效 ID 在普通 `and` 组，将多个有效变体序列化到每条逻辑词缀独立的 `count` 组并设置 `value.min=1`
- [x] 3.4 回归二至四个等价变体、多条逻辑词缀分组、综合条件、模型清理和重新查询，确保不跨词缀混组
- [x] 3.5 回归每个变体复用显示 min/max 但按其可信方向转换，并验证反射伤害单 ID 查询使用 `value.min=50` 而不是 `value.max=-50`

## 4. 浮窗展示

- [x] 4.1 让含多个可信查询变体的逻辑词缀只显示一行、一个勾选状态和一组 min/max，不显示内部 ID 选择按钮
- [x] 4.2 为多变体逻辑词缀增加非交互的合并提示并添加 Vue 渲染/交互回归，确保用户只能修改启用状态和显示边界

## 5. 开发版验收

- [x] 5.1 运行受影响的目录、解析、模型清理、查询序列化和 Vue 针对性测试，并清理任何失败尝试留下的无用代码
- [x] 5.2 运行完整 `npm test`、`npm run build`、`openspec validate resolve-duplicate-trade-stats --strict` 和 `git diff --check`，仅在开发版验证且不打包、不发布
- [x] 5.3 检查最终 diff 只包含本变更的目录、解析、查询、安全、浮窗和 OpenSpec 文件，保留工作区其他未提交修改
