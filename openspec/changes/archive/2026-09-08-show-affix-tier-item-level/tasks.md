## 1. 实现

- [x] 1.1 完成标签、空白关键词及数据归一化，验证相关回归测试。
- [x] 1.2 调整列宽并通过 Vite 开发转换验证两个组件。

## 2. 验证

- [x] 2.1 执行 npm test、OpenSpec 严格校验并检查差异。

## 3. 修正等级偏差

- [x] 3.1 删除指定 T 级标签额外加 1 的计算，修正回归预期并覆盖与制作校验一致的行为。
- [ ] 3.2 运行相关回归与 OpenSpec 严格校验，在开发版验证必选、挑选切换及清空阶级的显示，不打包。

本次纠错验证：相关测试 23 项通过；OpenSpec 严格校验与两个组件 Vite 开发转换通过。全量测试 2025 项，2022 通过、2 跳过、1 失败，失败位于 mapTrackerAutomation.test.js:87（新 seed 的记录 ID 未变化），未修改该模块。尝试用服务端渲染断言两列选中标签失败：Element Plus 服务端渲染未输出选中标签，不能替代浏览器交互验证；该临时检查没有写入项目。桌面开发版切换和清空选择尚未验证，因此 3.2 保持未完成。未打包。

验证结果：相关测试 22 项通过；全量测试 2022 项通过、2 项跳过、0 失败；两个组件 Vite 开发转换及 OpenSpec 严格校验通过。未打包，未进行桌面窗口手动交互及窄屏目视验证。
