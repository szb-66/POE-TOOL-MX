## 1. 默认值与设置迁移

- [x] 1.1 添加 50ms 默认值、时序版本和旧默认一次性迁移的回归测试。
- [x] 1.2 实现共享默认值、设置持久化版本与 Electron/Python 运行入口兜底统一。

## 2. 共享输入事务

- [x] 2.1 添加取件持续持有 Ctrl、重试、异常清理和固定耗时基线测试。
- [x] 2.2 重构共享输入控制器和取件转移函数，消除同一候选内重复 Ctrl 周期且保持全部安全门禁。

## 3. 规格与开发版验证

- [x] 3.1 运行受影响的 Node/Python 测试并清理临时诊断或失败方案遗留。
- [x] 3.2 运行全量 `npm test` 与 `openspec validate --strict`。
- [x] 3.3 启动 `npm run electron:dev` 验证开发版可正常加载；当前无可用游戏会话，实机同组物品和背包已满交互留待游戏内复核；未执行打包。

## 4. 用户自定义时间范围

- [x] 4.1 添加时间配置接受 0ms、超过旧上限和非步长数值，并对负数及非有限值回退默认值的测试。
- [x] 4.2 移除设置页及共享归一化层的时间范围裁剪，保持默认值和持久化兼容。
- [x] 4.3 运行受影响测试、全量测试、OpenSpec 严格校验及开发版加载验证，不执行打包。
