## 1. 基线与迁移矩阵

- [x] 1.1 固定当前 Electron 开发版的启动、窗口、IPC、错误码和自动化行为基线，并生成覆盖全部 229 个入口的迁移矩阵
- [x] 1.2 运行并记录现有受影响测试、完整 `npm test`、Vite 构建和开发启动基线，确认仓库既有失败与用户改动
- [x] 1.3 建立 Tauri 功能对等清单，将每个主窗口/浮窗/系统服务/业务域映射到 Rust command、event、capability 和回归测试

## 2. Tauri 与 TypeScript 基础

- [x] 2.1 添加严格 TypeScript、Vue 类型检查和 TS 测试工具链，将 Vite/入口/路由改为可编译的 TypeScript 基础结构
- [x] 2.2 用官方 Tauri v2 初始化 `src-tauri`，配置 Windows 10/11 x64、固定产品标识、WebView2 和开发资源路径
- [x] 2.3 嵌入 `requireAdministrator` Windows 清单并实现只提升 Tauri 主程序、保持 Vite/终端普通权限的固定 3000 端口开发启动器
- [x] 2.4 配置主窗口、业务浮窗、登录窗口和屏幕选择窗口的最小 capability，确认未启用通用 shell、任意文件或前端 sidecar 权限
- [x] 2.5 实现 Rust 单实例、启动阶段、管理员完整性、WebView2 创建、结构化崩溃原因和统一退出清理骨架

## 3. 类型化桌面桥

- [x] 3.1 建立按领域组织的 `desktopApi` TypeScript 请求、结果、结构化错误和事件订阅契约，并提供仅用于浏览器测试的 mock adapter
- [x] 3.2 实现 Tauri adapter 和 Rust serde command/event 基础类型，确保前端业务代码不直接导入 Tauri invoke/event/plugin
- [x] 3.3 实现按窗口标签的 sender 验证、敏感命令拒绝和无秘密结构化审计事件
- [x] 3.4 实现普通数据序列化边界和幂等取消订阅，覆盖嵌套 Vue/Pinia Proxy、重复挂载与窗口关闭回归
- [x] 3.5 建立 TypeScript/Rust 共享 fixture、序列化往返、错误码和迁移矩阵契约测试

## 4. 产品代码 TypeScript 迁移

- [x] 4.1 迁移共享配置、校验、剪贴板、坐标、延迟、预设和故事等基础模块到严格 TypeScript
- [x] 4.2 迁移 Pinia stores、composables、主题、路由和主运行时初始化，并把持久化访问收敛到类型化 repository
- [x] 4.3 迁移公共组件、布局、设置、帮助和首页 Vue 脚本为 `<script setup lang="ts">`，修复所有模板类型错误
- [x] 4.4 迁移物品、地图、战斗、背包、商城、剧情、工具和浮窗领域页面到严格 TypeScript
- [x] 4.5 迁移制作模型、解析、匹配、规则、模拟、价格和规划领域逻辑到 TypeScript，保持固定数据集结果一致
- [x] 4.6 将 CPU 密集制作规划迁入可取消 Web Worker，保留请求 ID、进度、取消和结果同步语义
- [x] 4.7 使所有产品路径通过 `vue-tsc --noEmit` 和 TS 单元测试，并禁止新增产品 JavaScript 或 Node 专用导入

## 5. Rust 系统与窗口能力

- [x] 5.1 实现稳定用户数据根、版本化设置 repository、原子文件操作、剪贴板、文件 watcher 和允许目录资源解析
- [x] 5.2 实现外部链接策略、全局快捷键注册/捕获/游戏作用域和前台窗口检测
- [x] 5.3 实现显示器枚举、物理坐标/DPI 换算、窗口状态、移动缩放和主窗口标题栏控制
- [x] 5.4 实现制作、背包、剧情、海图、混沌配方、查价和调试原生浮窗及其透明/置顶/穿透/焦点行为，并合并高频状态、热区和尺寸同步以阻止原生调用洪泛及尺寸反馈循环
- [x] 5.5 实现多显示器点选与区域选择窗口、截图会话、sender 约束、取消和资源释放
- [x] 5.6 实现结构化诊断、启动日志、事件存储、脱敏导出、反馈附件选择和上传进度桥接

## 6. 账号、网络与业务服务

- [x] 6.1 在 Rust 原生层实现独立国服登录 WebView、HttpOnly Cookie 状态、手动 Cookie、登出、赛季和凭证隔离
- [x] 6.2 迁移国服仓库、混沌配方、查价客户端、目录、结果分页和官方页面打开服务，保持超时与错误语义
- [x] 6.3 迁移制作数据 repository、更新、价格服务和 worker 调度，保持缓存、取消和进度事件
- [x] 6.4 迁移反馈认证、CloudBase 请求、每日使用和网络失败隔离，验证所有诊断与日志不泄露秘密

## 7. Python 与自动化安全

- [x] 7.1 实现 Rust Python runtime 探测、正式内置/开发回退、模块完整性、资源路径和结构化健康状态
- [x] 7.2 建立固定自动化 operation ID 与逐操作配置验证，移除前端任意脚本路径、脚本正文和通用参数执行能力
- [x] 7.3 实现统一 Python 进程 registry、输出事件、启动超时、停止、强制清理和应用退出协调
- [x] 7.4 迁移制作、战斗、背包、仓库页、君锋镇、海图和界面检测自动化 manager，保留前台/DPI/坐标/模板预检，并保持自动入库、仓库取件、混沌配方与君锋镇共享唯一检测进程
- [x] 7.5 迁移自动化互斥锁和全局紧急停止，区分输入能力与独占锁，恢复被动喝药、主动循环和一键回城的并行及幂等启动语义，并验证即时 operation 状态、真正冲突的并发拒绝、预检失败零输入和所有 manager 无残留进程

## 8. Electron 数据直接迁移

- [x] 8.1 实现旧 Electron 运行检测、数据文件锁检测、版本化只读快照、临时目录和原子迁移提交
- [x] 8.2 实现受支持文件目录 allowlist、路径边界、格式校验和兼容读取，覆盖背景、模板、校准、制作与窗口状态
- [x] 8.3 实现当前 Electron Local Storage LevelDB 白名单解析器和设置规范化，不导入未知或损坏记录
- [x] 8.4 实现 Windows 原生 Cookie 数据读取、允许域名/名称过滤、当前用户解密、登录 WebView 写入和立即清理秘密
- [x] 8.5 实现迁移版本标记、幂等重试、部分失败回滚、Cookie 重新登录降级和无敏感内容迁移报告
- [x] 8.6 使用合成及脱敏真实结构夹具验证正常、占用、损坏、未知版本、重复运行和回退场景
- [x] 8.7 修复剧情 `storyGuide:v1` 键与 Rust 设置校验不兼容的问题，统一迁移白名单校验，并在主窗口关闭前提交剧情延迟保存、等待全部设置落盘及覆盖失败回归
- [x] 8.8 增加已完成 v1 设备也会执行的 v2 幂等修复，并将旧 `presets` 与物品、地图、海图和商城预设安全合并到当前类型化仓库
- [x] 8.9 将旧君锋镇校准与训练素材逐项转换到当前校准仓库，按路径、PNG、字段和单文件限制跳过无效条目且保留旧源
- [x] 8.10 恢复谜题区域预览的迁移、读取、重新框选和清空持久化，并改为逐文件审计背景、模板及其他兼容文件类别
- [x] 8.11 添加 v1 到 v2、重复运行、新旧冲突、部分损坏、路径逃逸、超限文件及真实结构脱敏夹具回归，并完成开发版数据重启验收

## 9. Tauri 更新与切换

- [x] 9.1 实现 Tauri updater 的 CNB 默认/GitHub 可选来源、手动/自动模式、启动检查、六小时调度和并发门禁
- [x] 9.2 实现签名/摘要验证、下载进度、用户确认、统一资源清理、安装前记录和升级后一次性发布说明
- [x] 9.3 更新发布元数据生成与校验脚本以生成两个来源一致的 Tauri 平台元数据、完整安装器 URL、签名和发布说明
- [x] 9.4 完成全部功能迁移后切换开发入口并删除 Electron 主进程、preload、electron-builder、Electron updater 和无用失败方案遗留
- [x] 9.5 清理产品路径剩余 JavaScript 和 Node 桌面依赖，保留经确认仍需要的构建/测试脚本并更新许可证清单

## 10. 开发版与正式版验收

- [x] 10.1 通过 TypeScript、Vue、Rust、契约、迁移、现有完整测试、Vite 构建、Cargo check/clippy/test、OpenSpec strict 和 diff check
- [ ] 10.2 在开发版连续验证管理员 UAC、主窗口/全部浮窗冷启动、多显示器/DPI、透明/穿透/焦点、账号、查价、反馈和更新状态
- [ ] 10.3 在不向游戏注入测试输入的前提下验证管理员游戏环境预检、Python 启停、全局停止和进程清理，并由用户完成真实游戏操作验收
- [ ] 10.4 经用户单独授权后构建正式包，验证 WebView2 已安装/缺失、Windows 10/11 x64、完整安装器覆盖和签名更新
- [x] 10.5 经用户单独授权后按同机同数据基线验证安装体积至少下降 35%、空闲内存至少下降 20%、冷启动不变慢，再决定正式入口切换
