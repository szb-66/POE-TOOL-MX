## 1. 建立分层 schema 与解析回归夹具

- [x] 1.1 扩展做装模型，加入底材分类路径/配置、统一目标效果、阶级标签与实际词缀选项，并将请求目标改为 `goalId + minTierId`
- [x] 1.2 更新 POEDB fixture，覆盖共享 Mod Family 的四种伤害效果、召集法杖专属记录、`master` 工艺记录和对应工艺台行
- [x] 1.3 为效果签名、阶级排序、标签/权重保留、复合词缀和旧 schema 回退添加失败优先的模型与解析测试

## 2. 修正底材目录和词缀生成

- [x] 2.1 从 POEDB 词缀导航提取受支持装备分类路径和普通/特殊词缀配置，并让来源清单同时驱动构建脚本与运行时更新器
- [x] 2.2 为底材绑定 `modifierProfileId`，过滤 Royale、测试、占位和废弃记录，并为合法中文同名底材生成唯一显示名称
- [x] 2.3 按配置、前后缀、来源范围和 Mod Family 建立父项，内部按效果签名区分目标并保留等级标签、数值、需求等级和独立权重
- [x] 2.4 更新词缀合法性与仓库查询，使特殊配置、阶级标签和权重参与正确过滤，并移除来源筛选参数
- [x] 2.5 添加目录集成测试，验证召集法杖归入特殊分类、专属召唤词缀不泄漏、朽木法杖无 Royale 重复且合法同名项可区分

## 3. 接入工艺台目标选项

- [x] 3.1 解析 `master` 分区并用 hover 标识关联工艺台表格，保存实际成本、适用类别、解锁信息和目标效果引用
- [x] 3.2 在快照校验中检查工艺外键、关联完整性与成本，拒绝把无法关联的 `master` 记录注册为可执行工艺
- [x] 3.3 添加工艺解析测试，覆盖正常关联、孤立记录、普通词缀成本和元工艺保留

## 4. 迁移规划器到来源无关目标

- [x] 4.1 调整 CraftState、目标满足判断和请求校验，以实际选项追踪来源并以统一目标、等级门槛、Mod Group 和槽位判定成功
- [x] 4.2 修改天然概率池按父项总权重及具体等级权重两级抽取，验证共享 Mod Group 词缀概率正确且仍互斥
- [x] 4.3 实现工艺台逐数值分量最低值达标判定，并覆盖最高值才达标、复合效果部分不足和纯工艺目标
- [x] 4.4 让策略生成器自动比较天然与工艺收尾路径，使用关联的真实工艺成本并删除普通工艺词缀的固定成本占位
- [x] 4.5 更新规划器单元和 Worker 集成测试，覆盖来源自动选择、成本排序、冲突校验、破裂状态和任务取消

## 5. 更新目标选择界面

- [x] 5.1 更新 store 和 IPC 调用以使用统一目标搜索结果和 `goalId + minTierId` 请求，删除所有 `sourcePolicy` 状态与控件
- [x] 5.2 将目标卡片改为左侧已选、右侧待选，窄窗口纵向降级，并确保已选项只有等级选择和删除
- [x] 5.3 在待选项与等级选项中展示目标/阶级标签、阶级文本、需求等级和权重，并保持前后缀搜索及空状态清晰
- [x] 5.4 添加页面结构与交互测试，验证无工艺台 Tab/来源选择、双栏顺序、等级编辑和输入变化取消旧任务

## 6. 生成数据并完成验收

- [x] 6.1 更新构建和运行时更新流程的 schema、checksum、数量及唯一性验证，并生成新的内置快照
- [x] 6.2 验证旧活动快照回退到新版内置快照、显示诊断且不删除旧用户目录
- [x] 6.3 更新 README 与相关 OpenSpec 描述，说明 POEDB 分类、标签/权重、统一目标和工艺最低值判定
- [x] 6.4 运行完整 `npm test`、生产构建和一次实时 POEDB 数据生成，检查分类、外键、重复显示名、工艺关联和关键黄金样例
- [x] 6.5 审查并删除实施期间失败尝试遗留的无用解析分支、硬编码来源逻辑和兼容代码

## 7. 恢复 POEDB 父项与分层权重

- [x] 7.1 升级到 schema v3，以 `ModifierFamily -> ModifierGoal -> ModifierTier` 保存父项、效果分支和等级记录
- [x] 7.2 将天然生成改为父项总权重与内部等级权重的两级抽取，保持标签过滤和 Mod Family 互斥
- [x] 7.3 查询返回全部等级、可用状态、小项数和当前总权重，不再删除等级不足记录

## 8. 补全分类与纵向目标界面

- [x] 8.1 为手套、鞋子、胸甲、头盔和盾牌生成力量/敏捷/智慧组合的三级分类路径
- [x] 8.2 将三个主模块改为全宽纵向排列，待选词缀使用可展开父项和绿/红统计标签
- [x] 8.3 置灰且禁用等级不足记录，降低物品等级时保留已选目标并阻止计算

## 9. 回归与快照验收

- [x] 9.1 改写共享法杖 fixture 和模型/概率/仓库/UI 测试，验证 iLv80 总权重 1500 与 iLv84 总权重 1576
- [x] 9.2 生成并校验内置 schema v3 实时快照，验证 schema v2 安全回退
- [x] 9.3 运行完整测试和生产构建，并删除 schema v2 错误拆分遗留的无用代码与相反规格

## 10. 修正父项效果标题

- [x] 10.1 父项标题保留数值占位符后的完整效果语义，并为通用句首与共享效果摘要添加回归测试
- [x] 10.2 更新内置 schema v3 快照，验证待选列表不再显示无辨识度的“该装备附加”标题

## 11. 将待选词缀压平为两层

- [x] 11.1 删除父项展开区中的效果分支标题层，将精确标签和“添加目标”操作合并到等级行
- [x] 11.2 验证点击等级行操作会直接以该等级添加对应效果目标，并清理旧分支容器样式与辅助函数

## 12. 外层显示完整标签

- [x] 12.1 查询结果汇总父项所有内部目标和等级的标签并去重
- [x] 12.2 将父项标题与完整标签左右排列，并补充仓库与页面回归测试

## 13. 调整标签视觉归属

- [x] 13.1 将父项标题和标签归入左侧内容组，仅把小项数与总权重留在右侧
- [x] 13.2 将等级标签移到等级文本右侧并整体靠左，更新布局回归测试
