## 1. 每日总数数据模型

- [x] 1.1 新增每日汇总迁移，创建仅含日期、正式版去重总数和更新时间的 `app_daily_usage_totals`，并以迁移当天现有最新记录初始化可信起点。
- [x] 1.2 重写每日使用安全定义函数，以安装唯一约束、条件更新和行锁保证同日重复/并发只计一次、跨日首次递增，同时继续维护每安装一条最新记录。
- [x] 1.3 将 `app_daily_usage_summary` 改为只向服务角色提供历史每日汇总，保持匿名客户端只写入口的列、载荷和权限不变。

## 2. 迁移契约验证

- [x] 2.1 扩展静态策略测试，验证没有安装日期明细、迁移前不回填、当天零值种子、匿名拒绝和服务角色读取授权。
- [x] 2.2 扩展真实 CloudBase 可选集成测试，验证同安装同日重复、并发、跨日、事务失败回滚和每日总数结果；测试不得指向生产环境。
- [x] 2.3 将迁移字节一致地同步到反馈后台仓库并增加 SHA-256 一致性检查，不触碰 Electron 每日上报模块或载荷。

## 3. 开发验证

- [x] 3.1 运行每日使用定向测试、完整测试、构建、`git diff --check` 和严格 OpenSpec 校验，不执行 Electron 打包或发布。
- [x] 3.2 核对工作区 diff，只保留本变更文件并确认现有未提交修改未被覆盖；记录生产 CloudBase 迁移未执行。
