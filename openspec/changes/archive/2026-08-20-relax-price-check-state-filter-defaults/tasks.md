## 1. 状态默认逻辑

- [x] 1.1 将集中状态初始化改为“明确肯定为是、未鉴定对已鉴定形成明确否定、其余为任意”，并使新模型、非法值回退和旧布尔模型迁移共享该规则。
- [x] 1.2 确认模型清理仍保留用户提交的合法“否”，且“任意”不会写入官方状态过滤。

## 2. 回归测试

- [x] 2.1 更新新模型断言，覆盖普通已鉴定物品的“已鉴定”为任意、文本含“未鉴定”时“已鉴定”为否，以及其他明确肯定状态为是。
- [x] 2.2 更新清理与兼容断言，覆盖缺失或非法值回退、旧 `unidentified=true` 迁移为“已鉴定=否”、其他旧布尔 `false` 不生成否定条件，以及手动 `false` 仍正常提交。

## 3. 验证与收尾

- [x] 3.1 重新运行查价定向 Node 测试、完整 `npm test`、Vite 开发转换和 `openspec validate relax-price-check-state-filter-defaults --strict`，不执行 Electron 打包。
- [x] 3.2 重新检查最终 diff，确认未覆盖工作树中星团珠宝、势力/回忆束丝等现有未提交修改，且没有留下失败方案代码。
