## 1. 物品解析与语料

- [x] 1.1 整理六种经典势力的稳定标识、官方 pseudo ID 和国服尾标别名，补齐“裂界之器”及其他受支持当前/兼容文本。
- [x] 1.2 扩展物品解析模型以保留六种势力和格式有效的回忆束丝数值，并对附件样本、单/双/无势力、无效束丝文本添加解析回归。

## 2. 查价模型与官方查询

- [x] 2.1 为已识别的六种势力创建默认启用的无数值 pseudo stat，确保双势力生成两条、取消后不提交，并使其通过现有词缀过滤区展示。
- [x] 2.2 将回忆束丝建模为默认启用的可编辑物品属性，默认只设当前值为最小值，并映射到 `misc_filters.memory_level`。
- [x] 2.3 在主进程模型清理中以可信捕获模型重建势力条件和束丝属性，仅保留允许的启停/范围编辑并拒绝伪造 ID、属性与非法数值。

## 3. 验证与收尾

- [x] 3.1 补充查价模型和 query 回归，覆盖六种单势力、双势力、用户取消、伪造势力条件及 `回忆束丝: 32` 的 `memory_level.min` 序列化。
- [x] 3.2 运行受影响的查价测试、完整 `npm test` 和 `npm run build`，修复回归并清理失败尝试留下的无用代码。
- [x] 3.3 在 `npm run electron:dev` 开发版回放附件样本，确认势力条件和回忆束丝展示、编辑、取消与重新查询行为；不执行打包。
- [x] 3.4 运行 OpenSpec 严格校验，对照 delta spec 检查最终差异并确认未改动无关工作。
