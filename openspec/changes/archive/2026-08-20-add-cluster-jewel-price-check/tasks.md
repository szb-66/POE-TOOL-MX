## 1. 解析与语料

- [x] 1.1 将非说明性的 `(enchant)` 行解析为保留原文和清理文本的 `enchant` modifier，并确保括号机制说明不进入任何词缀集合。
- [x] 1.2 在共享解析边界排除大型、中型和小型星团珠宝的放置说明，同时保留现有显式词缀、势力与回忆束丝解析。
- [x] 1.3 使用附件中的大型/中型文本及补充的小型文本增加解析回归，覆盖底材、鉴定状态、有效附魔和说明文案过滤。

## 2. 查价模型与官方查询

- [x] 2.1 让三种星团底材的有效附魔通过现有目录映射为 `enchant.stat_*`，在自动模式下全部默认启用，并保持 `all`、`none` 与普通物品附魔语义不变。
- [x] 2.2 增加模型、清理和官方 query 回归，验证天赋数量、珠宝槽数量、小天赋效果、用户取消/编辑、未鉴定魔法星团及物品等级默认关闭。
- [x] 2.3 回归星团显式/核心天赋词缀与普通珠宝，确认说明文案不出现在 `stats` 或 `unknownStats`。

## 3. 验证与收尾

- [x] 3.1 运行受影响的查价测试、势力/回忆束丝测试、完整 `npm test` 和 `npm run build`，修复回归并清理失败尝试遗留代码。
- [x] 3.2 在 `npm run electron:dev` 开发版回放附件样本，确认附魔展示、默认选择、编辑、取消和重新查询行为；不执行打包。
- [x] 3.3 运行 OpenSpec 严格校验并审查最终 diff，确认只增加星团查价所需增量且未覆盖现有未提交改动。
