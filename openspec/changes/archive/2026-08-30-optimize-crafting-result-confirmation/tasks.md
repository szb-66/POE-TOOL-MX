## 1. 两阶段文件解析协议

- [x] 1.1 扩展 `test/fileParseProtocol.test.js`，覆盖 `publish` 缺省为 true、显式 false 的解码与 requestId/publish 元数据回写。
- [x] 1.2 为主进程解析即时路径和结果文件监听路径增加回归测试，覆盖候选不显示、确认提交只显示一次、陈旧 requestId 不显示及既有无字段调用兼容。
- [x] 1.3 在 `electron/modules/watcher/fileWatcher.js` 中传播 `publish` 并维护当前有效解析 requestId 的生命周期，停止监听或新请求时正确失效旧请求。
- [x] 1.4 在 `electron/modules/ipc/file.js` 中实现统一的内部发布门禁，使即时解析与结果文件监听共享候选抑制、当前 requestId 校验和幂等发布逻辑。
- [x] 1.5 保持地图/海图统计文件更新的既有行为，同时验证统计更新不能携带或发布 `publish: false` 的候选物品结果。

## 2. 共享动作感知观察协议

- [x] 2.1 建立可注入单调时钟、sleep、剪贴板序列和文本结果的 Python 协议测试夹具，先覆盖 `initial`、`post-action` 与 `parse-replay` 三种操作。
- [x] 2.2 新增共享 Python 观察协议片段，并由 `src/utils/python.js` 以 `?raw` 注入装备和地图/海图模板，验证两类最终生成脚本均自包含且不需要新增运行时文件。
- [x] 2.3 实现按目标隔离、每个目标最多 8 条的内存文本哈希历史；装备使用固定目标，地图/海图按目标类型与格位切换，任何输出均不得包含全文或哈希。
- [x] 2.4 实现剪贴板事件状态机：无事件时使用 `adaptiveTimeoutMs`，序列号已变化的同文本只保留约 30ms 提交宽限，并取消完整超时后的固定退避。
- [x] 2.5 实现相对动作完成时间约 200ms 的可疑结果确认窗口和每次通货最多三次复制观察预算；窗口已到时立即复核。
- [x] 2.6 实现同一候选最多三次的解析传输重放，保证重放不增加 Ctrl+C、不重复通货、不移动目标，并对 requestId 不匹配与结果等待施加统一上界。
- [x] 2.7 增加仅开发版启用的脱敏读取指标，字段限定为动作类型、复制次数、解析重放次数、结果枚举和耗时，并测试生产生成脚本关闭输出。

## 3. 通货后置条件与事务核心

- [x] 3.1 实现当前目标身份提取与动作前/动作后结构化快照比较，统一显式词缀计数并对不支持的词缀格式 fail closed。
- [x] 3.2 实现知识、蜕变、点金、重铸、增幅、富豪、崇高和瓦尔的确定变化策略，并为每种成功、未变化、错误稀有度/标记及身份不符添加测试。
- [x] 3.3 实现改造、混沌、工匠、链接、幻色和古灵直接通货的合法稳定策略：新合法结果直接确认，同文本必须由两次相隔确认窗口且有序列号证据的复制确认。
- [x] 3.4 实现单一 `apply_currency_and_confirm` 事务入口，确保只调用一次现有 `apply_currency`，以 `publish: false` 解析候选，验证通过后原子提交同一 requestId 的 `publish: true` 结果。
- [x] 3.5 为复制超时、解析超时、身份冲突、后置条件失败和用户取消建立可区分但不含物品内容的结果，接入现有异常停止与恢复上下文。
- [x] 3.6 增加安全断言测试：任一未确认动作之后不得出现下一次通货、目标移动、完成报告或当前目标统计提交。

## 4. 装备、插槽与古灵路径接入

- [x] 4.1 将装备首次读取迁移为 `initial`，并将知识、蜕变、点金和重铸预处理迁移到确认事务，保留前台窗口、坐标、DPI、通货预检和停止门禁。
- [x] 4.2 将改造、混沌和增幅主循环迁移到确认事务，删除“未变化直接下一颗通货”和“增幅未变化沿用旧结果”的旁路。
- [x] 4.3 将词缀命中后的富豪石和每颗崇高石迁移到独立确认事务，只有最终结果提交面板后才报告完成，并覆盖连续崇高逐颗确认。
- [x] 4.4 将工匠石、链接石和幻色石循环迁移到 `post-action` 事务，移除对首次读取同文本放行的复用。
- [x] 4.5 将古灵直接通货循环迁移到 `post-action` 事务，保持自然基底词缀判断与既有停止条件。
- [x] 4.6 更新装备、增幅、插槽、古灵和通货计数相关测试，覆盖逐次面板同步、富豪/崇高最终同步和未确认时只记录一次通货使用。

## 5. 地图与海图路径接入

- [x] 5.1 将地图/海图每个新格的首次扫描迁移为 `initial`，用目标类型与格位建立 targetId，并在换格时隔离历史。
- [x] 5.2 将知识、重铸、点金和混沌洗练路径迁移到确认事务，确认提交前不得再次洗练当前格。
- [x] 5.3 将瓦尔收尾迁移到确认事务，只有已腐化结果提交后才允许判定当前格终态。
- [x] 5.4 保持地图与海图的恢复检查点和完成格统计语义，确保失败格不提交临时统计、不换格且恢复时不重复此前格位。
- [x] 5.5 更新地图运行模板与解析同步测试，覆盖跨格历史隔离、候选不显示、当前格未确认不换格、海图类别以及统计只提交一次。

## 6. 性能回归与失败方案清理

- [x] 6.1 用虚拟时钟覆盖正常新文本 1 次 Ctrl+C 且无固定 200ms、旧文本后变新最多 2 次且默认调度不超过 500ms、合法稳定同文本最多 2 次的性能验收。
- [x] 6.2 覆盖没有剪贴板事件时三次有界观察后安全停止，以及解析超时重放同一候选但不增加 Ctrl+C 或重复通货的失败验收。
- [x] 6.3 删除两模板中的全文历史缓存、旧哨兵、`allow_unchanged_text`/`verify_freshness`、嵌套复核分支、失效注释及仅为失败方案添加的日志，仅保留共享协议所需代码。
- [x] 6.4 更新 `adaptiveTiming`、`inputTiming`、`operationDelay` 和解析同步等依赖旧函数源码形状的测试，使其断言生成脚本的新协议语义和真实调度常量，不再把退避覆写为 0。
- [x] 6.5 审计装备与地图/海图生成脚本中的所有 `apply_currency` 调用点，并添加静态回归断言，确保除事务内部外不存在使用通货后绕过确认的业务路径。
- [x] 6.6 检查实现过程中失败尝试遗留的辅助函数、日志、兼容分支和重复测试，删除无调用或不再证明需求的代码。

## 7. 自动化验证与开发版实测

- [x] 7.1 运行受影响的定向 Node 测试，包括文件协议、解析同步、装备预处理、增幅、通货计数、古灵、地图模板与时序测试，并修复所有确定失败。
- [x] 7.2 运行完整 `npm test`，确认共享协议和事务接入未破坏其他自动化安全门禁。
  - 完整套件 1695 项中 1693 项通过、2 项按既有条件跳过、0 项失败；本次补充的面板发布确认与重复 requestId 去重回归均通过。
- [x] 7.3 运行 `npm run build` 进行开发代码构建验证；不执行 Electron 打包或发布。
- [x] 7.4 运行 `git diff --check` 和 `openspec validate optimize-crafting-result-confirmation --type change --strict --no-interactive`，并确认未修改或暂存海图边缘识别、时序校准及其他无关未提交工作。
- [ ] 7.5 经用户确认测试窗口和低价值通货后，在开发版分别抽样装备与地图/海图流程，逐次核对游戏实物与面板、最终富豪/崇高同步，并从脱敏指标记录 p50/p95；不打包。

## 8. Windows 结果发布确认竞态

- [x] 8.1 增加确定性回归测试：即使 `fs.watch` 只报告候选写入并遗漏提交事件，轮询仍能发布并确认；停止/重启后旧会话延迟回调不得影响新 requestId。
- [x] 8.2 重构结果文件监听为共享的最新结果读取路径：保留 `fs.watch` 低延迟通知，当前请求未确认时每 10ms 轮询，按成功解析的完整内容去重，并在确认、新请求、停止或重启时正确管理轮询与会话标识。
- [x] 8.3 增加真实临时目录与子进程压力测试，验证 10ms 下连续至少 100 次候选、提交、确认全部成功且无重复发布。
- [x] 8.4 运行文件协议、观察协议、地图/装备解析同步、自动时序校准定向测试，以及完整 `npm test`、`npm run build`、`git diff --check` 和 OpenSpec 严格校验；清理失败方案遗留代码且不打包。
  - 定向测试 89 项全部通过；真实临时目录与子进程在 10ms 下连续 100 次全部确认、每个 requestId 只发布一次。
  - 完整测试 1694 项中 1692 项通过、2 项按既有条件跳过、0 项失败；开发构建、差异检查和严格规范校验均通过。

## 9. 通货选择安全门禁

- [x] 9.1 增加生成期共享通货动作协议，在装备、地图和海图统一执行右键按住、可中断释放等待、前台/坐标/右键状态复核，并传播 `CURRENCY_SELECTION_UNSAFE`
- [x] 9.2 用虚拟时钟与伪输入覆盖正常单次应用、停止、失焦、鼠标干扰、右键未释放、目标坐标失败及证据失败后零补点
- [x] 9.3 运行制作、地图、观察协议、时序校准定向测试及完整验证，清理失败方案遗留且不打包
  - 新增通货安全门禁相关定向测试 61/61 通过；补齐生成脚本测试夹具后，完整测试 1701 项中 1699 项通过、2 项跳过、0 项失败，开发构建、差异检查和两个变更的严格校验均通过，未打包。
