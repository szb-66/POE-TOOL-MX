## 1. 建立真实失败回归

- [x] 1.1 从本次两张截图按应用实际 E0/W0 几何裁出 1200×480 最小夹具，只保留游戏捕获范围并确认目标文字完整落在框内
- [x] 1.2 在 Python 检查中加入亮暗背景、仓库等级粘连和动态背景变化样本，先复现整框稳定失败并验证蓝紫色目标 ROI 能稳定提取
- [x] 1.3 在 JavaScript 匹配测试中加入「相邻区域包含」+「8个额外的海兽群」及「相邻区域的词缀数值提高」+「60%。」真实分段输出，先验证现有匹配器失败

## 2. 实现目标文字采样

- [x] 2.1 在 `chart_mods_probe.py` 实现基于通道相对优势的蓝紫色文字掩膜、连通域噪声过滤、联合边界和黑字白底 ROI 生成
- [x] 2.2 将边缘等待改为最多三帧的目标 ROI 采样，目标 ROI 缺失时保留一次现有全帧 OCR 兼容回退，并继续复用 DPI 捕获、50 像素热点和前台安全校验
- [x] 2.3 扩展内部 Python 边缘结果以返回有序文本样本，同时保留旧式 `texts` 单样本兼容路径，不修改公开 IPC

## 3. 实现语义稳定与匹配

- [x] 3.1 在 `matchBorderMods` 为相邻 2–3 段及完整短序列生成有限阅读顺序拼接候选，并复用现有精确、唯一包含、结构和 0.82 模糊匹配规则
- [x] 3.2 增加分段词缀成功、等级粘连成功、多目录与共享行不提前匹配、截断和无关 UI 不绕过阈值的目录安全测试
- [x] 3.3 在海图服务中逐样本执行共享匹配器，仅在连续两次命中同一目录条目时确认稳定；旧式单样本结果保持兼容
- [x] 3.4 所有样本均失败时按最高匹配置信度、再按有效字符数选择非空 `rawTexts`，确保空的增强/回退结果不会覆盖已有失败证据

## 4. 补充未知状态诊断展示

- [x] 4.1 更新边缘悬停内容：已匹配继续显示目录文本，未知且有 `rawTexts` 时显示“词缀：未知”及本次 OCR 原文，无原文时只显示未知
- [x] 4.2 增加页面与 store 集成测试，验证 `rawTexts` 通过现有 `borderMods` 保留、重新识别后替换且不会显示上一次文本

## 5. 清理与验证

- [x] 5.1 删除被目标 ROI/语义稳定替代且已无引用的整框 32×32 严格帧稳定代码和失败尝试遗留，确认没有两套稳定策略并存
- [x] 5.2 运行海图 OCR Python 检查、匹配器、服务、store、页面集成和 Vite 开发转换测试，再运行 `npm test`
- [x] 5.3 运行 `openspec validate stabilize-border-mod-ocr --strict`，确认 proposal、三份 delta spec、design 与 tasks 一致
- [x] 5.4 重启管理员权限开发版识别 12 段，重点验收 E0/W0 不再未知；若仍失败，直接记录悬停展示的 `rawTexts` 后定位，不扩大范围或降低阈值

## 6. 按验证反馈简化采集流程

- [x] 6.1 将三帧采样改为固定等待后单帧采样：删除 `sample_target_frames` 与主进程连续命中/证据选择逻辑，单样本直接执行共享匹配器
- [x] 6.2 同步 proposal、design 与 chart-mod-recognition delta spec，将"连续两帧稳定"改为"固定等待后单帧采样"
- [x] 6.3 更新 Python 检查与集成测试断言并全量验证（受影响测试、`npm test`、构建、严格校验）
- [x] 6.4 通过无条件转储取证 E1/W1 失败原因并修复：浮窗比其它边缘更靠上，偏下矩形捕获窗漏掉；改为以鼠标为中心的 800×800 正方形捕获并新增锚点行选择
- [x] 6.5 修复后删除临时调试转储代码，重跑受影响测试、`npm test` 与 `openspec validate --strict`，再完成 5.4 验收

## 7. 按实际框选尺寸适配热点

- [x] 7.1 将固定 50 像素热点改为沿对应方向按海图区宽高的 6% 计算，保持 825×828 标定结果并补充多尺寸、非正方形与边界钳制测试
