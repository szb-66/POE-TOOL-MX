## 1. 浮窗状态渲染

- [x] 1.1 将物品制作与地图制作内容分支的关闭按钮限定为停止状态，同时保留等待态和完成态操作。
- [x] 1.2 保持现有 `close` 事件、处理器和 Electron 窗口关闭接口不变，并确认未覆盖组件中的既有未提交样式改动。

## 2. 最新配置重新开始

- [x] 2.1 让浮窗重新开始前重载当前物品预设与制作设置，并复用 `startCrafting()` 完整启动链路。
- [x] 2.2 让 `startCrafting()` 返回结构化成功或失败结果；最新配置失败时恢复完成画面并显示具体原因，不得回退旧脚本。
- [x] 2.3 删除上次成功配置缓存、旧重启 IPC 及 preload/renderer API 包装，保留防重复点击行为。

## 3. 回归验证

- [x] 3.1 增加状态级回归测试，覆盖物品/地图运行态隐藏、停止态显示、等待态显示以及完成态操作不变。
- [x] 3.2 增加重新开始回归测试，覆盖删除组合后使用最新配置、未修改配置、无效配置失败和重复点击保护。
- [x] 3.3 运行受影响的 Node 测试、Vite 开发构建转换和 OpenSpec 严格校验，不执行打包。
