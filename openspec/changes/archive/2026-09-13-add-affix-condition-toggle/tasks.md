## 1. 实现

- [x] 1.1 增加词条启停持久化和包含数、物等过滤，通过配置与匹配回归测试。
- [x] 1.2 共用词条行添加开关和淡化，保留编辑状态及空白行，通过开发转换和交互验证。

## 2. 验证

- [x] 2.1 覆盖保存、复制、导入导出及三种制作共用行为，运行相关测试和 npm test，记录结果。
- [x] 2.2 开发版检查页面并执行 OpenSpec 严格校验，不打包。

## 验证记录

- 相关测试：33 通过（包含新增 8 项行为回归）。
- npm test：2602 项，2600 通过，2 跳过，0 失败。
- OpenSpec 严格校验通过。
- 使用现有 electron:dev 的开发服务器检查三个制作标签页：新增默认开启，空白词条关闭不消失，关闭后编辑保持关闭，淡化和行首布局正常；挑选包含数从 2 降到 1，重新开启不调高，全部关闭后刷新仍保留词条与关闭状态。
- 未执行游戏内通货操作，未打包。
