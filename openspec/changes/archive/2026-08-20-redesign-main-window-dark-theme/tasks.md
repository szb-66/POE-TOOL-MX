## 1. 主题契约与测试基线

- [x] 1.1 新增主窗口主题作用域测试，覆盖主布局根类、全部 `noLayout` 路由隔离、无主题持久化以及 Element Plus 传送弹层继承边界
- [x] 1.2 更新主布局与侧栏契约测试，锁定 76px 宽度、入口名称/路由/顺序、开发入口、视觉分组、滚动、焦点和预加载行为
- [x] 1.3 更新首页与设置页契约测试，锁定紧凑汇总、非颜色状态表达、六标签顺序、持续挂载、会话恢复、切换回顶和反馈页特殊行为

## 2. 主窗口主题基础

- [x] 2.1 在应用启动与路由变化时仅为主布局文档挂载并清理 `main-window-theme`，确保首帧和页面切换不闪回浅色
- [x] 2.2 在主题作用域下实现固定色板、字体、圆角、焦点、减少动态效果和 Element Plus 语义变量映射，同时保留现有 `:root` 浮窗基线
- [x] 2.3 更新通用页面、表单、按钮、标签、卡片、弹层、滚动条和加载反馈样式，确保深色对比度与状态语义一致

## 3. 品牌与应用壳层

- [x] 3.1 使用图像编辑工具生成临时深炭底和品牌金 Logo，保持主要识别结构并统一开发版引用；精确品牌资产留待后续替换
- [x] 3.2 重设计 32px 标题栏的材质、边框、焦点、悬停、按下和关闭危险反馈，保留全部窗口行为
- [x] 3.3 将侧栏调整为 76px，通过区段起始类形成四个非交互视觉分组，并实现品牌金选中标记及键盘焦点反馈
- [x] 3.4 将路由加载骨架与错误反馈调整为深色最终布局的几何结构，保留延迟显示和并发导航复位逻辑

## 4. 首页视觉试点

- [x] 4.1 将四项状态汇总改为单一紧凑状态带，并将系统健康信息统一为一个可收起深色面板
- [x] 4.2 重设计模块分组与 `ModuleStatusCard` 表面、层级、指标、操作和状态表达，不修改状态数据源或业务操作
- [x] 4.3 完成首页宽窗口三列、窄窗口单列、加载、错误、禁用、运行中和需配置状态的响应式与可访问性检查

## 5. 设置页完整试点

- [x] 5.1 将六个标签及固定操作区统一为深色层级，保持标签顺序、持续挂载、会话恢复和切换回顶逻辑
- [x] 5.2 将全部设置区块统一为任务分组表面和表单层级，保持字段名称、DOM 顺序、保存、即时生效、确认和重置行为
- [x] 5.3 实现 1120px 内容上限、显式独立短字段双列和窄窗口单列，并检查说明、错误和长文本不截断
- [x] 5.4 完成问题反馈、诊断、附件、提交进度、失败状态以及反馈页隐藏重置操作的深色适配

## 6. 其余主窗口兼容审计

- [x] 6.1 搜索全部主窗口页面中的浅色硬编码、白色表面、默认灰字和不兼容阴影，按语义令牌修复可读性且不重排结构
- [x] 6.2 复核做装物品、稀有度、通货、运行、警告和危险颜色，确保领域语义没有被品牌金覆盖
- [x] 6.3 逐一检查查价、剧情、背包、商城配方、海图、调试与区域选择等无主布局窗口，确认透明度、尺寸、穿透和原有样式未变化

## 7. 开发版验证与清理

- [x] 7.1 运行首页、设置、主布局、路由加载、启动性能、反馈和应用更新相关 Node 测试并修复回归
- [x] 7.2 运行完整 `npm test`、`npm run build`、`git diff --check` 与 `openspec validate redesign-main-window-dark-theme --strict`
- [x] 7.3 使用 `npm run electron:dev` 检查 1200x800、1024x720、1440x900 及 Windows 100%、125%、150% DPI 下的键盘、焦点、滚动、截断和状态展示
- [x] 7.4 清理实现过程中失败方案留下的样式、变量、组件或测试代码，并记录透明浮窗与安装包图标的未打包验证边界

> 开发版已实际检查首页、设置通用、问题反馈、路由加载、键盘焦点与最大化/还原；通过 Electron 开发参数模拟 100%、125%、150% DPI，并在每档 DPI 下分别验证 1200x800、1024x720、1440x900。九组组合均无横向溢出、控件越界或按钮文字截断，设置页焦点环与滚动正常。全部 `noLayout` 路由由主题隔离契约覆盖且实现未改其样式；安装包图标因未获打包授权未验证。
