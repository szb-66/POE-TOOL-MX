## 1. 回归测试

- [x] 1.1 在 `test/junfengHighlight.test.js` 添加君锋镇运行级用例，验证高置信候选与不确定格共存时只点击高置信候选并保留不确定格统计。
- [x] 1.2 更新 `test/junfengIntegration.test.js` 的 manager 配置契约，要求君锋镇与普通仓库均跳过不确定格。
- [x] 1.3 添加执行态回归契约，验证普通仓库、君锋镇和自动入库启动后不再调用标题匹配，且控制浮窗在标题暂时不匹配时保持当前运行模式。

## 2. 根因修复

- [x] 2.1 在 `electron/modules/junfeng/manager.js` 关闭遇不确定格整轮中止的运行配置，复用现有高置信候选执行和逐格复检路径。
- [x] 2.2 将标题匹配限制在按钮显示和启动前校验，删除正式执行配置、点击循环及 manager 订阅中的标题运行门禁，并删除逐件停车匹配逻辑。

## 3. 开发版验证

- [x] 3.1 运行君锋镇受影响测试，确认新回归用例与现有安全门禁用例全部通过。
- [x] 3.2 运行全量 `npm test` 和 `openspec validate --strict`，并检查差异中没有临时调试或失败方案遗留。
- [x] 3.3 验证公共标题检测仍控制空闲态按钮，执行态仅由游戏前台、逐格复检、转移确认、互斥锁和用户停止控制。
