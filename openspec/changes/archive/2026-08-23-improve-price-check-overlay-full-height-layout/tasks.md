## 1. 窗口尺寸与持久化

- [x] 1.1 将查价浮窗尺寸归一化和控制器改为仅读取、钳制并持久化宽度，兼容旧宽高状态且保存时移除高度
- [x] 1.2 让浮窗首次创建和重复召唤都使用当前显示器工作区的顶部与完整高度，并锁定纵向缩放、保留横向缩放和鼠标邻近定位
- [x] 1.3 更新尺寸、定位和窗口状态测试，覆盖旧配置、无效宽度、小屏钳制、跨显示器满高与程序定位不覆盖偏好

## 2. 单滚动与属性卡片

- [x] 2.1 移除词缀和佣兵技能区域的固定高度与内部滚动，使真实浮窗和固定高度预览都只由内容区纵向滚动
- [x] 2.2 在现有两列属性网格中为每个属性行增加始终可见的中性卡片表面，并保留启用、悬浮、聚焦和输入阻止冒泡行为
- [x] 2.3 更新查价浮窗布局与预览测试，覆盖两列完整属性卡片、单滚动和预览固定高度

## 3. 验证与清理

- [x] 3.1 运行受影响的查价定向测试和 Vite 构建，修复发现的回归并检查没有失败方案遗留代码
- [x] 3.2 运行完整 `npm test`、`git diff --check` 和 OpenSpec 严格校验，并记录开发版手动验收边界且不执行打包
