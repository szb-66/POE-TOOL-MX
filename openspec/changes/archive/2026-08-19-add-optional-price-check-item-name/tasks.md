## 1. 可信物品大类与模型契约

- [x] 1.1 在共享查价元数据中加入国服物品类别到官方交易类别的闭集映射与规范化函数，覆盖当前武器、防具、饰品、珠宝、药剂和地图类别及已知别名
- [x] 1.2 扩展查价模型的 `identity`，在新物品、旧模型清理和未鉴定传奇候选解析中正确维护 `category`、`categoryLabel` 与默认启用的 `nameEnabled`
- [x] 1.3 让主进程重新查询以当前可信模型固定大类，拒绝伪造类别以及“名称关闭且没有合法大类”的请求

## 2. 官方查询与安全降级

- [x] 2.1 在官方请求序列化中始终写入 `type_filters.filters.category.option`，并按名称启用状态决定是否发送 `query.name` 与 `query.type`
- [x] 2.2 调整 `Unknown item base type` 单次重试，仅对实际发送名称和底材的路径移除底材，重试时保留名称、大类和其他过滤条件
- [x] 2.3 修正“意志交锋”回归样本类别为“头盔”，并添加表驱动映射、默认精确查询、取消名称、旧模型、非法大类、候选解析和重试保留大类测试

## 3. 浮窗身份交互与布局

- [x] 3.1 将具体名称实现为当前模型的可访问筛选行，复用词缀的选中、悬浮、焦点、鼠标及 Enter/Space 交互，并在缺少合法大类时禁止取消
- [x] 3.2 常驻显示“稀有度 · 物品大类 · 具体底材 · 赛季”，把手动模式说明与状态标签放入可换行的身份栏右侧并删除下方重复说明
- [x] 3.3 更新浮窗集成测试，覆盖名称默认选中、鼠标/键盘切换、非名称区域不切换、大类常驻、说明位置和搜索提交当前状态

## 4. 开发版验证

- [x] 4.1 运行查价相关 node 测试与 Vite 开发转换，修复所有回归并复查没有失败方案遗留代码
- [x] 4.2 运行完整 `npm test` 与 `openspec validate add-optional-price-check-item-name --strict`
- [x] 4.3 在开发版使用“意志交锋”验证名称选中请求包含名称/黄金之面/`armour.helmet`，取消后不含名称和底材但保留 `armour.helmet` 及其他已选条件；不执行打包
