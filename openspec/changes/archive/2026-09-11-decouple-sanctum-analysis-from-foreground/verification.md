## 实现与回归

- 服务层失焦、原生安全事件、guard 与通用取消错误均区分输入停止和整轮取消。
- 已冻结房间、状态效果与奖励、延后图标识别继续完成；截图槽位和输入锁幂等释放。
- 部分结果保留未采集项目和真实原因；保存路线按房间及效果缺口计算完整性。
- 新增 17 项失焦专项测试全部通过，涵盖保存恢复、冻结帧返回竞态、状态预检和恢复地图取消、主动停止、紧急停止、停用、重置、日志失效及退出。
- 圣所首轮完整回归 353/353 通过；最终改动另跑针对性回归通过。
- OpenSpec 严格校验通过。
- 最终全套 `npm test -- --test-concurrency=4`：2502 项，2500 通过、0 失败、2 跳过（约 220 秒）。

## 失败与清理记录

- 原有两项测试要求失焦取消整轮，与新行为冲突，更新失焦断言并保留其他安全中断断言。
- 新夹具最初缺少初始房间入口及 located 状态，导致无法生成路线和完整效果；修正夹具后通过，未为测试修改业务判定。
- 曾将最终诊断阶段统一改为 recognizing，会丢失原始失败阶段；已撤回，保留原始阶段并仅修正最终完成状态。
- 全套首轮 2500 项中 2497 通过、2 跳过、1 失败，失败为上述已撤回的诊断阶段改动；最终代码重跑已全部通过（2 项跳过）。

## 开发版实机验证

用户已确认后台分析总体正常，报告完成后按钮仍禁用、切换前台后恢复。原后台分析实机验证据此完成，按钮修复实机复测另行记录。未打包，未改写用户存档。

## 完成按钮后续修复

- 根因：startLive 的内部结果早于 startTask 清理生成，完成推送之后，IPC 返回旧结果将页面覆盖回 captureDraining=true。
- 修改：正常启动和等待重启路径在等待与清理完成后重新读取当前状态；不改变真实清理期间的禁用和输入预检。
- 修改前新增回归复现 5 项失败；修改后启动重试、后台分析、按钮展示及服务相关 36 项测试通过。
- OpenSpec 严格校验通过。最终 `npm test` 共 2506 项，2504 通过、0 失败、2 跳过（约 191 秒）。
- 用户于 2026-09-11 确认“可以了”，完成按钮修复的实机复测通过，并授权同步规范及归档。
