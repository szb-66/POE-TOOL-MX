## 1. 交互与控件实现

- [x] 1.1 将状态过滤标题改为整行可操作的语义按钮，接入方向图标、展开状态、悬浮和焦点反馈
- [x] 1.2 将查价浮窗中的查询设置、状态三态和属性枚举原生选择器替换为全局主题选择器，保持原有模型更新与设置同步
- [x] 1.3 将手动 DC、属性和词缀的原生数字输入替换为紧凑的全局主题数字控件，保持空值、约束和事件隔离
- [x] 1.4 调整查价浮窗领域级尺寸与栅格样式，确保控件和弹层复用全局主题且不复制无关颜色规则

## 2. 回归覆盖

- [x] 2.1 增加状态过滤整行热区、方向图标、ARIA 状态和无文字折叠按钮的组件回归测试
- [x] 2.2 增加选择器类型、弹层主题接入、数字控件约束、空值语义和行点击隔离的回归测试

## 3. 开发版验证与清理

- [x] 3.1 运行查价浮窗定向测试、Vite 开发转换、完整 `npm test`、`git diff --check` 和 OpenSpec 严格校验
- [x] 3.2 在开发版实际核对标题鼠标/键盘热区、三类选择器弹层、数字空值与紧凑布局，不进行打包
- [x] 3.3 检查最终差异并删除任何失败尝试留下的原生控件样式、重复事件处理或无用主题覆盖

## 4. 查询状态反馈精简

- [x] 4.1 删除正常读取后的身份栏操作提示及无用样式，将加载、错误和限流反馈移动到搜索按钮下方、结果内容之前
- [x] 4.2 更新组件回归测试，覆盖正常态无提示、加载与错误反馈位置，以及原有错误和限流文案保留
- [x] 4.3 重新运行查价浮窗定向测试、Vite 开发转换、完整 `npm test`、`git diff --check` 和 OpenSpec 严格校验
