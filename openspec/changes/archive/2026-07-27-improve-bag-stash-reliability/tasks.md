## 1. 配置与运行时同步

- [x] 1.1 为背包设置增加 transferDelayMs 默认值、范围规范化、持久化和运行配置映射
- [x] 1.2 增加预加载/API/主进程的入库节奏同步 IPC，并保证只影响后续轮次

## 2. 入库可靠性与统计

- [x] 2.1 在 Python 入库脚本中实现点击后原格确认、一次安全重试和 failedSlots 统计
- [x] 2.2 在 Store、背包页面和完成提示中接入延迟输入及失败统计

## 3. 验证与规范

- [x] 3.1 补充配置边界、IPC同步、确认成功、重试成功及失败关闭测试，并删除旧 120ms 约束测试
- [x] 3.2 更新主规范，运行 OpenSpec 校验、完整测试和生产构建

## 4. 复制漏识别修复

- [x] 4.1 将背包独立等待应用到移入稳定和剪贴板响应，并对无响应复制重试一次
- [x] 4.2 补充假空格回归测试、更新规范并重新运行完整验证

## 5. 统一延迟配置

- [x] 5.1 用 operationDelayMs 替代全局三项延迟并实现旧全局与背包设置迁移
- [x] 5.2 将设置页精简为一个真实毫秒输入框并删除延迟预设

## 6. 统一脚本行为

- [x] 6.1 让制作、地图洗练和背包直接使用相同的 operationDelayMs，删除隐藏倍率和废弃模板变量
- [x] 6.2 删除背包点击后确认、点击重试、failedSlots 统计及模块独立延迟配置

## 7. 回归验证

- [x] 7.1 更新迁移、脚本生成、背包单击及界面精简测试并清理旧逻辑
- [x] 7.2 更新主规范，运行 OpenSpec 校验、全量测试和生产构建
