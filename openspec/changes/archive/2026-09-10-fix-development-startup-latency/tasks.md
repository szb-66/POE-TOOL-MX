## 1. 日志与后台初始化

- [x] 1.1 合并启动进程探测并保护 Tailer/服务取消；回归测试覆盖并发、失败、PID 重用、停止和历史恢复。
- [x] 1.2 将日志与地图初始化移到窗口创建之后，增加共享就绪与 IPC 门禁；验证慢初始化不阻塞窗口、退出不残留资源。

## 2. 启动依赖与计时

- [x] 2.1 分离独特装备网页解析，做装更新按需加载解析器；验证解析结果与加载中取消，运行时不加载 Cheerio。
- [x] 2.2 增加进程探测、日志恢复及前端模块细分计时；验证白名单事件和 interactive 原有条件。

## 3. 验证与记录

- [x] 3.1 运行受影响测试、完整测试、Vite 开发转换、OpenSpec 严格校验；记录失败原因并清理临时方案。
- [ ] 3.2 空闲和游戏运行中分别采集三轮进程重启及 Electron 重启，并验证同时启动游戏；保存原始结果及 10/15 秒预算、旧目标差距。

采集及原始记录已完成，见 verification.md；首轮和全量测试后启动仍超预算，3.2 保持未验收，不以后续热启动结果替代。用户明确选择仅保留当前代码和记录，不扩展 SSD 缓存范围。
