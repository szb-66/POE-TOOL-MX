# 开发启动验证（2026-09-10）

## 已实现与自动验证

- 日志首轮恢复/轮询共享一次进程探测；并发任务复用；保存设置串行化；停止、禁用、再次启用及 Tailer I/O 边界检查代次。
- 日志及地图恢复移到窗口创建之后；地图状态可立即查询，依赖操作等待共享就绪；关闭后不得恢复地图草稿，初始化前的前台状态保留。
- 独特装备 HTML 解析迁至 `scripts/uniqueItemParser.js`；做装更新在实际执行时加载解析器。启动依赖图测试阻断 Cheerio 导入仍能构建运行时服务、读取本地快照。
- 新增固定模块启动阶段，仅首轮记录进程/日志恢复计时，不持续记录周期轮询；原 interactive 条件保持。
- 全量 `npm test`：2393 项，2391 通过，0 失败，2 跳过（真实 CloudBase 使用统计与反馈集成，未连接远端服务）。最后新增两项生命周期测试并修复后，相关 21 项再次全部通过。
- Vite 开发转换：mainRuntime、installFeatureRuntime、startupReporter、App.vue、DashboardRouteView.vue 五项 HTTP 200；所有实机样本均使用开发版，不打包。
- OpenSpec 严格校验、修改文件语法检查及 `git diff --check` 通过。

## 实机基准

三轮批次列中均按采集顺序列出秒数；Electron 重启批次先预热一次，预热原始事件也保留。没有重启电脑、清除缓存、终止游戏、调整系统优先级或修改启动预算。

| 场景 | 窗口可见（秒） | 完整可操作（秒） | 10/15 秒预算 |
| --- | --- | --- | --- |
| 游戏运行，完整进程重启 | 60.450 / 6.143 / 5.619 | 78.859 / 14.281 / 13.725 | 首轮失败 |
| 游戏运行，复用 Vite 重启 | 2.808 / 3.475 / 3.455 | 10.458 / 10.533 / 10.503 | 全部通过 |
| 游戏退出，完整进程重启 | 5.013 / 4.950 / 5.057 | 11.941 / 12.060 / 11.971 | 全部通过 |
| 游戏退出，复用 Vite 重启 | 2.420 / 2.385 / 2.325 | 9.667 / 9.783 / 9.509 | 全部通过 |
| 检测到游戏启动后立即启动助手 | 5.406 | 14.269 | 通过 |

同时启动场景：14:24:42.345 UTC 观察到游戏进程，14:24:42.785 UTC 开始基准，间隔约 440ms。该轮有已有系统缓存，不能代表重启电脑后的首次冷启动。

游戏运行两批次在最后的设置读取串行化保护前采集；空闲及同时启动批次包含该保护。所有批次均在最后两项地图生命周期边界修复之前完成，这两项由定向测试及下面的正常开发启动复核。

原始事件、失败样本和报告元数据保存在 [verification/startup-samples.json](verification/startup-samples.json)，没有将完整本机用户路径或业务输出写入工件。历史 109.092 秒日志仅作现象证据，不能据此计算受控性能提升比例。

## 尚未通过的验收

- 13 个正式采集样本中 12 个达标，游戏运行首轮仍为 78.859 秒；未隐藏或剔除。
- 完整测试和最终边界修复后的正常开发启动，再次出现窗口 34.434 秒、完整可操作 47.186 秒。该次还进行了五项 Vite 转换 HTTP 检查，单独保存为 smoke 样本，不混入正式批次。
- 该正常启动中，Electron spawn 完成到业务入口仍有 10.415 秒，主模块导入 4.735 秒；此时 Vite 监听只花约 0.11 秒。因此仅优化业务初始化不足以解决全部首启延迟。
- 本机 G 盘确认为机械硬盘（WDC WD10EZEX），失败首轮采样存在磁盘 I/O 队列。读盘竞争是待验证的剩余瓶颈推断，尚未进行 SSD 对照实验，不能宣称硬件因果已完全证实。
- 旧外壳 3 秒预算：完整进程重启的中位数仍为 4.532 秒（空闲）和 5.446 秒（游戏运行），未达标。复用 Vite 的外壳中位数为 1.626 / 2.590 秒，达标。
- 旧首页 8 秒指标：后续 dashboard 挂载均小于 8 秒，但完整 interactive 为 9.5～14.3 秒，二者不可混称可交互。首轮 dashboard 为 66.229 秒，仍失败。

## 失败与清理

- 新增五项日志回归测试先全部失败，修复后通过；没有删除失败断言以伪造通过。
- 首次完整测试的 21 项圣所失败，根因是 ensureStarted 对已启动服务又调用 initialize；修正为等待现有任务并复用监听后，完整测试通过。
- 启动顺序测试最初使用不存在的 app.whenReady 文本入口，改为真实 startApplication 入口；不涉及业务行为绕过。
- 最终检查又用两项失败测试复现前台状态丢失和停止后恢复草稿，已补正确异步边界保护。
- 移除旧网页解析静态导入及旧首屏串行等待；保留必要生命周期保护。无临时调试日志、失败替代实现或打包产物。

计划内代码修复与采集已完成，但首启性能尚未全面验收。用户已明确选择仅保留当前代码修复和验证记录，不扩展 SSD 开发缓存；没有迁移源码、依赖或运行时。未达标项保持记录；用户随后明确要求同步并归档，本次带性能未全面验收说明归档，不代表所有启动预算已通过。
