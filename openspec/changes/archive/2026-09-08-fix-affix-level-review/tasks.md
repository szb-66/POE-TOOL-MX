## 1. 修复与回归

- [x] 1.1 按实际词缀制作开关生成物等要求，用生成脚本回归验证停用、启用及专用制作行为。
- [x] 1.2 更新四项过时页面断言，运行 advancedAlchemy、chartRolling、gridLayout、helpContent 测试确认通过。

## 2. 集成验证

- [x] 2.1 运行 npm test 和 OpenSpec 严格校验，检查本次差异没有遗留试错代码。

验证结果：相关测试 42/42 通过；完整测试 2020 通过、0 失败、2 跳过；OpenSpec 严格校验和 git diff --check 通过。回归用例先复现停用词缀模块仍拦截，修复后通过；未打包，未修改物等阶级算法。
