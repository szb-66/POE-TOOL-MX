## 1. 建立回归反馈环

- [x] 1.1 扩展装备与地图运行模板测试，用临时结果文件确定性复现“响应在等待前写回却被删除并超时”，确认修复前失败
- [x] 1.2 增加双格外层循环测试，复现单张处理返回失败后仍移动并输入下一格，确认修复前失败
- [x] 1.3 增加共享解析协议测试，覆盖装备、地图、海图的当前请求与陈旧请求、成功与错误结果关联标识回传，以及无标识旧格式兼容

## 2. 修复解析请求与响应同步

- [x] 2.1 在装备模板和地图/海图共用模板中生成单调递增的 `requestId`，在发布请求前截断旧结果内容且不删除结果文件
- [x] 2.2 更新 Electron 文件监听和解析写回链路，使可选 `requestId` 在成功与错误响应中均被原样回传，同时保持旧输入格式可用
- [x] 2.3 修改两个模板的等待逻辑，只接受当前 `requestId` 的完整结果、忽略陈旧响应，并验证快速响应不会被删除

## 3. 收紧单张地图失败与换格控制流

- [x] 3.1 抽取当前格读取操作，在首次复制、解析或等待失败时仅重试读取一次，并保证重试不重复使用任何制作通货
- [x] 3.2 将 `process_single_map` 的布尔值/元组混合返回迁移为明确的合格终态、不合格终态和失败终态，保持传奇、不可改变、腐化及正常成功分支的既有统计语义
- [x] 3.3 修改外层网格循环，仅对明确完成终态更新统计并换格；连续解析失败时设置可区分原因、释放输入状态并停止，不移动或输入下一格
- [x] 3.4 覆盖点金和混沌模式下“可修改但未达标”的状态机测试，确认仍在当前格继续洗练直至进入明确终态

## 4. 验证与清理

- [x] 4.1 运行装备模板、地图模板、文件监听、六项地图匹配和物品制作协议的定向 Node 测试，修复所有回归
- [x] 4.2 运行全量 `npm test`，确认前台门禁、停止快捷键、通货预检、地图条件和旧文件协议均保持通过
- [x] 4.3 使用 `npm run electron:dev` 在开发版验证快速解析、一次读取重试、连续失败停止及正常达标换格；不执行打包
- [x] 4.4 检查并删除失败尝试遗留的调试日志、临时分支和无用兼容代码，再运行 `openspec validate fix-map-rolling-parse-race --strict`
