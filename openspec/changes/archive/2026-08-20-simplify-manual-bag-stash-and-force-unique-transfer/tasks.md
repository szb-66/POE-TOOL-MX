## 1. 配置与手动触发

- [x] 1.1 更新背包配置规范化、Pinia store 和运行配置：删除两个旧触发字段，新增默认关闭的 `forceUniqueStash`，并将缺失或非法额外列数默认设为 6
- [x] 1.2 重构背包会话和 Electron IPC，移除检测就绪后的自动启动分支及旧偏好同步，只保留手动启动门禁
- [x] 1.3 固定浮层为条件满足或正在入库时可见，并更新配置迁移、会话和浮层状态回归测试

## 2. 传奇强制入库

- [x] 2.1 扩展 Python 物品头解析，规范识别中文 `传奇` 与英文 `Unique` 并保持现有名称、基底、类别输出
- [x] 2.2 为 InputController 增加受控 Shift 输入和 Ctrl+Shift+左键动作，确保所有结束路径按鼠标、Shift、Ctrl 顺序释放
- [x] 2.3 实现传奇首次普通点击后的清空剪贴板确认、一次强制点击和 `transfer-unconfirmed` 安全停止，不对强制动作二次确认
- [x] 2.4 增加普通物品、开关关闭、首次成功、强制点击、确认失败、失焦/停止/异常输入清理的 Python 回归测试

## 3. 存取页面精简

- [x] 3.1 删除“立即执行入库”和“满足条件显示”，加入“传奇物品强制入库”基础开关，并保留现有页面布局改动
- [x] 3.2 将启用模块与物品黑名单说明迁入可访问的问号 tooltip，删除三处指定提示文案
- [x] 3.3 移除存取页面所有开关的 active/inactive 文案和 inline prompt，并增加 UI 静态回归测试

## 4. 验证与清理

- [x] 4.1 运行背包配置、浮层、自动入库和存取页面定向测试并修复根因
- [x] 4.2 运行 `npm test`、`npm run build` 和 `git diff --check`，不执行 Electron 打包
- [x] 4.3 检查最终 diff，删除失败尝试遗留代码并确认无关工作区改动未被覆盖
- [x] 4.4 运行严格 OpenSpec 校验并更新任务完成状态
