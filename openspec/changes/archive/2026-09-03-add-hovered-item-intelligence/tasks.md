## 1. 共享悬停捕获

- [x] 1.1 为普通复制捕获补充成功恢复剪贴板、并发用户写入保护、请求序号取消和脱敏会话单元测试
- [x] 1.2 提取主进程共享悬停捕获服务并让现有国服查价直达入口接入，保持前台门禁和查询行为
- [x] 1.3 为查价服务增加接收可信已解析物品的内部入口，验证菜单查价不再发送第二次 `Ctrl+C`

## 2. 本地物品体检

- [x] 2.1 实现普通复制词缀数值区间解析、逐值百分位、支持范围和确定性摘要纯函数及单元测试
- [x] 2.2 将交易 stat 身份和做装底材容量接入体检模型，覆盖唯一匹配、歧义、传奇和缺失字段降级
- [x] 2.3 实现版本化体检偏好仓库、按类别的 desired/avoid 更新和损坏数据安全回退测试
- [x] 2.4 实现体检主进程服务、受限 IPC/preload 接口和铺满当前显示器工作区高度的体检浮窗 manager
- [x] 2.5 新增体检功能配置页、偏好管理界面、体检浮窗路由和浮窗内即时偏好更新
- [x] 2.6 将体检加入 feature catalog、页面预加载、模块启停生命周期和帮助内容

## 3. 统一物品上下文菜单

- [x] 3.1 为全局设置增加默认留空的 `itemContextMenu` 并接入现有快捷键作用域、冲突验证和功能过滤
- [x] 3.2 实现只生成 inspect、price-check、poedb 三个动作的上下文控制器及一次性 `captureId` 校验测试
- [x] 3.3 实现鼠标附近菜单窗口、工作区裁剪、加载感知状态发布、键盘操作、真实失焦/离开关闭和程序隐藏焦点保护
- [x] 3.4 实现三个动作的主进程白名单执行：无焦点回抢地打开体检、复用捕获查价、以本地底材 `sourceId` 构造固定 PoEDB HTTPS 直达地址并在无法唯一匹配时禁用
- [x] 3.5 新增菜单路由和 Vue 界面，并验证菜单、体检和查价浮窗互斥及禁用原因展示

## 4. Client.txt 事件中心

- [x] 4.1 实现 Client.txt 纯解析器及 area-entered、character-level、client-state 白名单事件测试
- [x] 4.2 实现 500ms 增量 tailer，覆盖 EOF 起步、UTF-8 分段、半行、CRLF、截断、轮转、删除重建和访问错误
- [x] 4.3 实现版本化 Client 设置仓库、默认关闭、保存路径验证、进程目录候选推导和受限文件选择
- [x] 4.4 实现事件服务的 50 条内存队列、状态发布、生命周期清理以及无原始行落盘或外传测试
- [x] 4.5 注册 clientEvents IPC/preload 接口并在设置页系统区域增加开关、路径、连接状态、错误和最近事件列表

## 5. 集成与验证

- [x] 5.1 补充跨模块集成测试，覆盖非游戏前台零输入、并发捕获取消、动作白名单和旧查价回归
- [x] 5.2 运行受影响的 Node 测试并修复失败，检查并删除所有失败方案遗留代码
- [x] 5.3 运行完整 `npm test` 和 OpenSpec 严格校验
- [ ] 5.4 使用 `npm run electron:dev` 验证真实普通复制、菜单到体检/查价的焦点转交、体检全工作区高度、DPI、多屏和真实国服 Client.txt，不执行打包
