## 1. 设置与查价页面

- [x] 1.1 在查价设置规范化中增加默认关闭的 `queryImmediately`，验证旧设置缺失字段、非法值和跨重启持久化行为
- [x] 1.2 在查价页面查询设置区增加“立即查价”开关，更新使用说明并通过现有 store 保存设置

## 2. 手动查价状态机

- [x] 2.1 扩展捕获 IPC 请求，使其显式传递 `queryImmediately`，缺失时按 `false` 处理且不改变官方查询 options
- [x] 2.2 将主进程捕获流程拆分为可信解析与可选查询：手动模式建立 `ready-to-query` 浮窗状态，立即模式沿用捕获后查询
- [x] 2.3 让浮窗“搜索”提交待查询模型，并保证手动模式下身份/词缀候选选择只更新模型、不隐式请求官方接口
- [x] 2.4 补充手动与立即模式、候选解析、新物品取消旧请求及迟到结果隔离的服务和 IPC 回归测试

## 3. 官方限流与请求调度

- [x] 3.1 移除客户端固定 5 秒/750ms 间隔、成功响应头推导等待和全局 Promise 队列，保留 15 秒相同搜索缓存及每批最多 10 条校验
- [x] 3.2 解析官方 429 的数字或 HTTP 日期 `Retry-After`；有效时记录同组冷却并让后续请求立即返回剩余时间，无效时不得生成默认冷却
- [x] 3.3 将目录 stats/items 调整为目录流程内顺序请求但不阻塞交互 search/fetch，并验证后台未完成时交互查询仍可发出
- [x] 3.4 补充无官方限流时连续请求零主动等待、有效/无效 `Retry-After`、冷却期立即拒绝及目录与查价隔离的客户端测试

## 4. 限流展示与分布加载

- [x] 4.1 在初始查询浮窗中显示结构化官方限流原因、剩余秒数或未知恢复时间，且不自动重试
- [x] 4.2 让价格分布每批完成后立即更新结果，并按相邻请求起始时间至少 1 秒渐进请求；等待可被新查价取消
- [x] 4.3 补充一秒批次间隔、慢响应不额外等待、取消等待、限流提示及分布部分结果保留的 UI/服务测试

## 5. 开发版验证与清理

- [x] 5.1 运行查价相关 node 测试和 Vite 开发转换，验证手动模式、立即模式及价格分布一秒渐进加载
- [x] 5.2 运行完整 `npm test`，检查最终 diff 并删除任何失败方案遗留的等待、队列或无用状态代码
- [x] 5.3 运行 OpenSpec strict 校验并核对 proposal、delta specs、design 与实现行为一致；不执行打包
