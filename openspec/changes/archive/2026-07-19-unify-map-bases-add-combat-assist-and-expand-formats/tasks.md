## 1. 地图预设规范化

- [x] 1.1 提取可独立测试的地图基底迁移函数，将 Normal/T17 公共字段按已启用最大阈值合并并保留三个 `more*` 字段
- [x] 1.2 在地图预设初始化和加载路径调用幂等迁移，成功后移除 `tiers`、`strategy` 及带后缀旧键
- [x] 1.3 添加旧配置冲突、单侧启用、专用字段保留和重复迁移的单元测试

## 2. 统一地图页面与匹配器

- [x] 2.1 移除地图页普通/高级 tab，让必选和挑选区域始终绑定规范化六项基底
- [x] 2.2 更新默认地图预设和新建预设结构，继续保留用户手动选择点金石或混沌石
- [x] 2.3 重构地图 Python 模板的基底校验，删除地图类型分支并将缺失基底按零处理
- [x] 2.4 为六项基底、必选/挑选重复项和三字段/六字段地图增加运行时匹配测试

## 3. 战斗辅助后端

- [x] 3.1 定义战斗辅助默认配置与兼容加载逻辑，覆盖血蓝检测、安全限制、启停快捷键和回城流程
- [x] 3.2 新增自动喝药 Python 模板，实现红/蓝像素检测、独立回复模式、按键序列、频率保护和前台游戏窗口保护
- [x] 3.3 新增战斗辅助进程管理与 IPC，实现幂等开始、停止、状态查询、单像素采样、状态事件和异常退出清理
- [x] 3.4 实现一键回城 IPC，按“游戏内开启键、等待、左键点击坐标”执行并拒绝并发重复请求
- [x] 3.5 扩展 preload 和渲染端 Electron API，并在应用退出清理战斗辅助及回城子进程
- [x] 3.6 为阈值边界、两种回复模式、频率保护、失焦暂停、重复启停和回城时序增加自动化测试

## 4. 战斗辅助界面与快捷键

- [x] 4.1 新增战斗辅助路由、侧边栏入口、领域页面和运行状态展示
- [x] 4.2 实现生命与魔力配置卡片，支持坐标选取、阈值、按键序列、独立回复模式和间隔配置
- [x] 4.3 实现颜色采样测试，展示 RGB、判定分量及当前触发结果
- [x] 4.4 实现回城宏触发键、游戏内开启键、点击坐标、等待间隔和页面执行按钮
- [x] 4.5 注册默认 `Numpad7` 开始、`Numpad8` 停止和 `Numpad2` 回城快捷键，并将全部快捷键纳入格式与冲突校验
- [x] 4.6 同步页面按钮、全局快捷键和后端事件的运行状态，确认制作/洗图停止不会停止自动喝药

## 5. 物品格式、解析与领域文档

- [x] 5.1 扩展解析器的结构化 `modifiers` 与不可制作标志，正确归组详细多行词缀并保留现有匹配数组接口
- [x] 5.2 过滤括号机制说明、奖励、地图使用说明和出售信息，确保其不会进入显式词缀匹配
- [x] 5.3 将用户样本整理为按结构分类的普通、魔法、稀有、传奇、未鉴定、势力、腐化、不可改变及地图代表示例
- [x] 5.4 更新物品和地图格式面板，取消普通/高级地图措辞并说明统一六基底匹配
- [x] 5.5 添加表驱动解析回归测试，验证类别、稀有度、状态、势力、插槽、六项基底和多行词缀
- [x] 5.6 更新 README 与 OpenSpec 项目上下文，补充词缀数量、固定/复合词缀、等阶和物品等级概念及 PoEDB 来源链接

## 6. 集成验收与清理

- [x] 6.1 在 Electron 中验证坐标选取、颜色采样、快捷键冲突提示、前台窗口保护和一键回城流程
- [x] 6.2 验证自动喝药可与制作/洗图并行、各停止动作互不干扰且应用退出后没有残留进程
- [x] 6.3 运行完整 `npm test` 与 `npm run build`，修复回归并检查实现过程中遗留的废弃地图分支、失败尝试代码和无用配置
