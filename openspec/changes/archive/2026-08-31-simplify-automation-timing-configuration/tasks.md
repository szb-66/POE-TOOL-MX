## 1. 单字段设置与迁移

- [x] 1.1 为当前设置、旧版本设置和旧导入配置补充测试，覆盖保留有效 `operationDelayMs`、旧默认值升级、旧三字段/背包等待迁移以及高级字段剥离
- [x] 1.2 提升时序配置版本并将设置规范化为单字段白名单投影，确保保存、重置、导入和导出不再写回 `adaptiveTiming`、`adaptiveTimeoutMs` 或 `fixedTiming`
- [x] 1.3 删除 store、类型、默认值和配置服务中的高级用户时序状态，同时保留既有非时序设置与迁移兼容读取

## 2. 跨进程协议与内部时序

- [x] 2.1 将渲染端、Electron IPC、preload 和自动化宿主的用户时序载荷收敛为 `{ operationDelayMs }`
- [x] 2.2 将 Python 边界收敛为只接收 `operation_delay_ms`，删除 `timing_mode`、`adaptive_timeout_ms`、`fixed_timing` 及其子字段解析和生成
- [x] 2.3 建立共享的内部物理输入、轮询间隔、无信号安全边界和失败超时命名常量，并以定向测试约束其语义与使用位置
- [x] 2.4 对剪贴板、画面、仓库页签、存仓等可靠信号使用内部有界轮询，断言成功路径在结果出现时立即继续

## 3. 全部运行时消费者迁移

- [x] 3.1 迁移装备制作、地图制作和海图制作消费者，只在鼠标悬停边界使用 `operationDelayMs`
- [x] 3.2 迁移背包入库、普通仓库取件、君锋取件和商城配方取件消费者，并删除模块独立延时回退
- [x] 3.3 迁移仓库页签选择、海图自动放置和战斗辅助消费者，确保旧高级时序不再进入运行时载荷
- [x] 3.4 增加仓库级负向协议测试或审计，确保所有受支持消费者不会重新读取、透传或写回已删除字段

## 4. 自动检测收敛

- [x] 4.1 精简自动检测搜索与推荐逻辑，只测量 `operationDelayMs`，保留五格验证、5/5 候选、15/15 确认、安全余量和超时失败规则
- [x] 4.2 精简检测进度和结果契约，只返回单一推荐延时、非敏感证据、样本、耗时和置信度，不返回 `recommendedTiming` 或 `fixedTiming`
- [x] 4.3 将应用、失败回滚和当前会话撤销改为单字段原子更新，并覆盖手动修改、重置、再次检测和重启后的撤销失效
- [x] 4.4 保留并回归验证前台门禁、自动化互斥、紧急停止、输入释放和剪贴板恢复，确保检测不产生点击或通货消耗

## 5. 设置界面降维

- [x] 5.1 将设置页时序区域精简为一个“自动操作等待”毫秒输入、自动检测按钮、恢复默认和唯一用途说明
- [x] 5.2 删除自适应开关、统一超时、物理/结果时序字段、高级折叠区、快稳预设和十行检测结果表
- [x] 5.3 更新组件测试，断言页面只出现一个毫秒输入和自动检测入口，键盘可访问说明有效且已删除字段不再显示

## 6. 综合验证

- [x] 6.1 运行设置迁移、导入导出、IPC/Python 协议、所有运行时消费者、自动检测和设置页定向测试
- [x] 6.2 运行完整测试与开发构建，并检查差异和仓库搜索结果，确认无失败方案遗留代码或旧高级字段回写路径
- [ ] 6.3 与 `rebuild-crafting-transaction-flow` 一起在开发版连续验收装备、地图和海图至少 30 次，覆盖计划指定通货且无重复使用、提前换格、提前统计或面板实物不一致
- [ ] 6.4 严格校验两个 OpenSpec 变更并记录同机同场景 v1.2.1 性能基线结果；不执行打包、安装或发布

## 7. 失败验收与回退

- [x] 7.1 记录开发版验收结论：单一延时模型使多类自动操作明显变慢，配置降维未得到可接受的运行效果。
- [x] 7.2 撤销单一字段设置、校准服务、跨进程单字段协议和所有消费者迁移，恢复 v1.2.2 的高级时序设置、持久化迁移和运行时载荷。
- [x] 7.3 删除失败方案新增的校准、协议、脚本和测试残留，并完成定向测试、完整测试（1691 项、1689 通过、2 跳过、0 失败）、开发构建、差异检查和严格校验；不打包、不安装、不发布。
- [ ] 7.4 本变更不再继续实施；只有用户重新确认新的时序配置方案后，才建立独立变更，不复用本次失败实现。
