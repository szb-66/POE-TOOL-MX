## 1. 离线词缀目录

- [x] 1.1 根据 POE1 3.29 国服 POEDB 快照新增异界地图可洗显式词缀候选，为每个语义家族记录唯一稳定关键词、完整示例、前后缀类型、标签、代表档位文本及版本/来源元数据。
- [x] 1.2 实现海图候选构建器，仅处理 `FRAGMENT_CHART_MODS` 的前后缀，按已审核稳定关键词合并档位，并排除传奇条目、未揭示说明和奖励附加行。
- [x] 1.3 新增分类联想搜索工具，按当前 `atlas`/`chart` 类型在关键词、示例和标签中搜索，结果上限为 50，空输入、未知类型或异常时返回空数组。
- [x] 1.4 将 POE1 3.29 国服 POEDB Uber 地图可洗显式词缀并入异界地图离线目录，合并共有语义并保留 Uber 专属稳定关键词、示例、标签和来源元数据。
- [x] 1.5 收紧海图候选构建器，仅接受来源标签精确为 `prefix` 或 `suffix` 的条目，排除 `legendary`、空标签和未知类型，并保证输出不产生 `mixed` 候选。

## 2. 地图预设编辑界面

- [x] 2.1 将当前地图制作类型从 `MapView.vue` 传入共用预设面板，确保 Tab 切换后立即使用对应候选且只编辑当前预设。
- [x] 2.2 将黑名单与白名单的普通输入框替换为自动完成输入，禁止空值获焦时主动展开，支持键盘/鼠标选择并将候选 `value` 写回原字符串数组。
- [x] 2.3 在候选中显示稳定关键词、完整示例和前后缀标签，并为下拉层增加独立限高、滚动和焦点样式。
- [x] 2.4 保留直接编辑、清空和保存目录外文本的行为；候选为空或查询失败时不改变输入值、预设保存或制作启动。

## 3. 验证与回归

- [x] 3.1 增加目录契约测试：两类候选均非空，类型内 `value` 非空且唯一，不含可变数值，并且每个代表档位文本都包含对应 `value`。
- [x] 3.2 增加海图目录过滤与合并测试，覆盖跨档位合并、奖励行排除、传奇/未揭示排除和稳定关键词命中所有代表档位。
- [x] 3.3 增加搜索和界面测试，覆盖地图/海图严格隔离，按关键词、示例、标签查询，空输入不展开，黑白名单选中与自由输入均保持字符串数组。
- [x] 3.4 运行地图预设迁移、地图/海图匹配及相关前端测试，确认黑名单优先级、白名单语义、旧预设和统计不变。
- [x] 3.5 在开发版执行受影响的 Node 测试、Vite 前端构建检查和 `openspec validate add-map-chart-affix-autocomplete --strict`，不执行安装包打包或发布。
- [x] 3.6 增加 Uber 地图目录覆盖与搜索测试，并重新执行相关 Node 回归、Vite 前端构建和 OpenSpec 严格校验，不执行安装包打包或发布。
- [x] 3.7 增加海图传奇、空标签和未知类型过滤测试，校验真实候选均为前缀或后缀，并重新执行相关 Node 回归、Vite 前端构建及 OpenSpec 严格校验，不执行安装包打包或发布。
