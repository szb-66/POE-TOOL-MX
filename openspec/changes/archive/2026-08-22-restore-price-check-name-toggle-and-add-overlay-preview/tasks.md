## 1. 修复名称选择与类别约束

- [x] 1.1 为名称关闭后的查询、下一件物品默认选中以及“异界地图”保留 `map` 大类补充失败回归测试
- [x] 1.2 在共享查价类别映射中将“异界地图”归一化为官方 `map`，并确认未知大类仍禁止关闭名称
- [x] 1.3 将名称行替换为带明确选中标记的原生复选控件，保留整行点击、Space、Enter、当前物品级状态和禁用提示
- [x] 1.4 补充名称控件的鼠标、键盘、重新选择及非法大类禁用契约测试

## 2. 建立无副作用的查价预览模式

- [x] 2.1 新增确定性传奇头盔预览样例工厂，包含名称、大类、属性、状态和词缀，并测试每次调用返回深度独立状态
- [x] 2.2 为 `PriceCheckOverlayView` 增加显式预览状态和查询选项输入，在预览时跳过真实状态读取、IPC 监听、设置同步及渲染通知
- [x] 2.3 为全部 Electron 副作用入口增加预览守卫，使搜索、网页市集、关闭及其他外部操作在预览中不可调用
- [x] 2.4 在预览中保留并禁用搜索、网页市集和关闭操作，显示“预览模式”和明确提示，同时保留本地筛选、折叠及设置交互
- [x] 2.5 为预览模式增加固定高度、内部滚动和非窗口拖拽样式，避免使用真实浮窗的 `100vh` 尺寸
- [x] 2.6 补充预览生命周期、副作用隔离、禁用操作和内嵌尺寸的组件契约测试

## 3. 接入设置页覆盖层面板

- [x] 3.1 在“设置 → 覆盖层”现有制作覆盖层设置卡片后新增全宽“查价弹窗预览”卡片，并复用业务覆盖层主题
- [x] 3.2 接入独立预览样例和“重置预览”按钮，通过重新创建深拷贝状态及组件实例恢复全部初始交互状态
- [x] 3.3 增加预览用途、无真实挂单结果和修改不保存的说明，并补充设置页位置、重置连接及主题集成测试

## 4. 验证与规范收尾

- [x] 4.1 运行定向查价、设置页和覆盖层主题测试，并修复发现的回归及清理失败方案遗留代码
- [x] 4.2 运行完整 `npm test`、`npm run build` 和 `git diff --check`，记录验证结果且不执行打包
- [x] 4.3 在开发版打开设置页覆盖层面板，实际检查名称鼠标/Space/Enter、筛选交互、禁用操作、内部滚动和重置行为
- [x] 4.4 严格校验 OpenSpec 变更；实现验收后同步主规范并归档变更
