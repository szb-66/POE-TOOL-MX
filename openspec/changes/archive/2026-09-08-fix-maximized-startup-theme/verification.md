## 验证结果

- 新增事件顺序测试在修复前 1 通过、4 失败，复现最大化提前显示及状态提前恢复；修复后 5 项全部通过。
- 相关启动与主题测试共 23 项通过。
- 完整 npm test：2047 项，2045 通过、2 跳过、0 失败（约 88 秒）。
- OpenSpec 严格校验、manager.js 语法检查、git diff --check 通过。
- 旧位置的最大化和全屏恢复代码已删除，无重复恢复路径。

## 开发验证边界

执行 npm run electron:dev -- --diagnostic-exit-after-interactive 因端口 3000 已被占用退出，未关闭现有开发进程。未完成原生 Electron 冷启动的视觉复测；本次验证确认的是实际窗口创建函数在模拟 Electron 事件下的显示及恢复顺序。未打包。
