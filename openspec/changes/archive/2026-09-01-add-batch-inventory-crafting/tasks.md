## 1. 国服角色与背包数据

- [x] 1.1 实现复用现有账号会话的角色列表与角色背包客户端，统一未认证、会话过期、限流、网络和接口结构错误。
- [x] 1.2 归一化同赛季角色、当前角色及 `MainInventory` 物品的稳定 ID、可制作类别、底材、左上格和尺寸，并过滤所有不支持对象。
- [x] 1.3 接入账号 IPC、preload、renderer API 和账号状态清理，在退出、赛季或角色变化时清空批量快照。
- [x] 1.4 为角色/背包多种响应包裹、类别解析、坐标尺寸、会话错误及不兼容结构补充固定夹具测试。

## 2. 批量配置与制作页交互

- [x] 2.1 为物品预设增加规范化的 `batchCrafting.enabled=false` 与 `categoryIds`，验证旧预设迁移、复制、导入导出和重置行为。
- [x] 2.2 增加会话态批量 Store，管理角色选择、自动/手动同步、同步时间、类别计数、候选派生和过期清理，不持久化完整快照。
- [x] 2.3 在制作页增加批量模块、仅启用时显示的准备提示、角色切换、类别多选、候选格子预览、重新同步和可操作状态反馈。
- [x] 2.4 更新首页状态、共享启动入口、配置引导和制作帮助，使开关开启时统一执行批量门禁，关闭时保持单件行为。

## 3. 批量启动与整批安全预检

- [x] 3.1 在公共启动服务中冻结最小批次配置，按列优先顺序选择候选，并在脚本启动前验证账号、快照、类别、背包布局和当前预设兼容性。
- [x] 3.2 扩展脚本生成契约，安全传递批次 ID、快照指纹、候选最小身份、背包坐标和恢复状态，同时保留原单件脚本输出。
- [x] 3.3 在 Python 中实现全部剩余候选的游戏内读取与位置、类别、底材、尺寸核对，多格物品只检查左上格，任一失败统一释放输入并结构化停止。
- [x] 3.4 保证整批身份预检完成前不调用任何通货或物品变更动作，且只有整批通过后才执行一次现有固定通货预检并报告启动成功。

## 4. 顺序制作、进度与恢复

- [x] 4.1 抽取可参数化目标坐标的单件准备与制作事务，在同一 Python 进程内逐件复用首次识别、词缀、插槽或古灵流程并将完成物品留在原格。
- [x] 4.2 发布白名单化的批量开始、预检、进度、完成和停止事件，并在 script Store、制作页和浮层展示总数、已完成、当前、剩余与失败项。
- [x] 4.3 保存会话内批次成员、已完成稳定 ID、失败项和通货使用会话；用户主动停止清除续作资格，异常停止保留安全重试入口。
- [x] 4.4 实现重试前重新加载最新配置和同一角色快照，只重建原批次剩余 ID，拒绝缺失、重复或近似匹配并排除恢复时新增物品。
- [x] 4.5 验证通货只在成功使用点记账，预检、失败动作、已完成物品和异常续作不会重复计数。

## 5. 回归与开发版验收

- [x] 5.1 增加配置/UI/入口测试，覆盖默认关闭、启用自动同步、角色切换、类别多选、候选预览、重新同步和三类启动入口一致性。
- [x] 5.2 增加 Python 门禁测试，覆盖空格、错位、错误底材、错误尺寸、多格占位、越界、预设不兼容，并断言首次通货输入发生在全部候选预检之后。
- [x] 5.3 增加顺序制作与恢复测试，覆盖首次已达标、中途异常阻断后续、只续作原剩余 ID、新物品排除、身份无法对应和通货统计连续性。
- [x] 5.4 运行受影响测试、完整 `npm test`、开发构建、`git diff --check` 和 `openspec validate add-batch-inventory-crafting --strict`，清理失败尝试遗留代码且不执行打包。
- [x] 5.5 在开发版用已登录国服会话只读验证角色接口契约，再用低价值物品完成受控实机预检、顺序制作、异常停止和安全续作验收。
