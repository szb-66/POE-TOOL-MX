## 1. 选区与配置纯逻辑

- [x] 1.1 实现矩形规范化、单显示器边界裁剪、20×10 最小尺寸和 DIP/物理像素转换辅助函数
- [x] 1.2 实现模板选区向四周扩展 12 物理像素并裁剪到显示器边界的搜索区域计算
- [x] 1.3 扩展背包模板配置规范化，为仓库和背包分别保存/迁移采集元数据，手工修改时清除对应元数据
- [x] 1.4 实现采集环境与搜索区域校验，覆盖 display id、scale factor、物理尺寸和模板尺寸

## 2. 通用矩形选取与干净截图

- [x] 2.1 将现有坐标取点会话泛化为互斥的 point/region 会话，保持原单点 API 行为不变
- [x] 2.2 使用 `desktopCapturer` 在选取窗口显示前捕获并映射所有显示器的干净截图和物理元数据
- [x] 2.3 扩展选取路由与 preload IPC 上下文，使每个显示器窗口获知模式、显示器标识和选区约束
- [x] 2.4 实现 region 模式的 pointer 拖动、反向规范化、实时矩形/尺寸预览、Enter/按钮确认和 Esc 取消
- [x] 2.5 将物理选区换算到截图局部像素并裁剪为目标尺寸 PNG，拒绝空图、无效尺寸和低信息图像
- [x] 2.6 确保确认、取消、窗口关闭、加载失败和应用退出均只结算一次并清理全部选取窗口及截图 Buffer

## 3. 背包模板保存与启动校验

- [x] 3.1 新增仅接受 `stashTitle`/`inventoryTitle` 的 `capture-bag-template` IPC，并接入通用 region 选取结果
- [x] 3.2 实现 userData/templates 临时 PNG 写入、原子替换、失败清理及旧目标保留
- [x] 3.3 返回模板路径、12px 搜索区域和完整采集元数据，并通过 preload/renderer API 暴露受限调用
- [x] 3.4 在 `start-bag-detection` 边界校验当前显示环境、模板尺寸和搜索区域；不兼容时拒绝并指出具体标题
- [x] 3.5 对无采集元数据的旧模板允许兼容启动，并把 legacy warning 传递到应用级背包服务

## 4. 背包界面与匹配流程

- [x] 4.1 为背包 store 增加原子应用仓库/背包采集结果的方法，并保持旧 `bagSettings` 可读取
- [x] 4.2 将“框选仓库标题”和“框选背包标题”设为模板配置主入口，模块运行时禁用框选
- [x] 4.3 展示两类模板预览、模板尺寸、搜索区域和旧配置提示，将上传及手工坐标移入高级设置
- [x] 4.4 高级上传或手工区域修改后清除对应采集元数据，取消或失败时保持全部旧值
- [x] 4.5 将 Python 标题模板与实时截图统一转换为灰度图后匹配，并保留现有阈值及双界面稳定判定

## 5. 验证与清理

- [x] 5.1 添加选区规范化、边界裁剪、12px 边距、负坐标和混合 DPI 换算单元测试
- [x] 5.2 添加 point/region 会话互斥、跨 chunk/窗口确认、Esc、异常关闭和重复结算回归测试
- [x] 5.3 添加截图裁剪尺寸、thumbnail 比例、低信息图片拒绝、目标白名单和原子保存失败回滚测试
- [x] 5.4 添加旧配置迁移、元数据清除、环境变化阻止启动、legacy warning 和搜索区域过小测试
- [x] 5.5 运行完整测试与生产构建，并清理实现中被替代的上传/区域重复逻辑及失败尝试遗留代码
