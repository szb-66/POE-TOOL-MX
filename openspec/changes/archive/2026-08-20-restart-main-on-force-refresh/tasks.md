## 1. 应用重启生命周期

- [x] 1.1 为重启协调器添加单元测试，覆盖清理与缓存失效成功、清理失败/超时不拉起、失败后终止以及重复请求幂等
- [x] 1.2 实现独立的应用重启协调器和共享开发重启退出码，复用现有清理时限并保留原始失败根因
- [x] 1.3 接入主窗口 Session 页面缓存清理，确认实现不调用 Cookie、Storage 或用户数据清理接口

## 2. 开发版进程重拉

- [x] 2.1 为开发启动器的 Electron 子进程循环添加测试，覆盖专用退出码重拉、非重启退出关闭 Vite 和重拉后正常退出回收
- [x] 2.2 重构 `scripts/dev.js`，在同一 Vite 服务生命周期内串行管理 Electron 子进程并只在专用退出码时重新拉起

## 3. 快捷键与主进程接入

- [x] 3.1 更新窗口刷新测试，验证 `Ctrl+Shift+R` 只发出一次应用重启请求，`Ctrl+R` 与 `F5` 仍仅重载渲染页面
- [x] 3.2 让窗口管理器通过注入回调请求强制刷新，移除 `force-reload` 路径对 `reloadIgnoringCache()` 的直接调用
- [x] 3.3 在主进程组装重启协调器：正式版清理成功后调用 Electron relaunch，开发版清理成功后返回专用退出码，失败路径记录诊断且不拉起新实例
- [x] 3.4 检查并清理实现过程中被替代的重启、缓存或退出代码，确保关闭、应用更新安装和启动崩溃恢复路径保持原行为

## 4. 验证

- [x] 4.1 运行重启生命周期、退出控制、刷新快捷键、启动诊断和应用更新相关测试
- [x] 4.2 运行完整 `npm test` 并修复由本变更引入的回归
- [x] 4.3 使用 `npm run electron:dev` 验证强制刷新后 Electron PID 改变、Vite PID 保持、持久化状态保留，随后正常退出不遗留 Vite 或 Electron 进程
- [x] 4.4 运行 `openspec validate restart-main-on-force-refresh --strict`；不执行打包或发布验证
