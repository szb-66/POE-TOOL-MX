## 1. 解析修复

- [x] 1.1 修复传奇详细文本块外兜底解析，使用完整用户样本验证仅保留六条真实属性。
- [x] 1.2 添加解析与查价回归，覆盖描述变体、后续属性/状态、未知真实词缀和普通复制兼容性，并运行针对性测试。

## 2. 集成验证

- [x] 2.1 运行 npm test 与 OpenSpec 严格校验，记录结果并检查最终差异。
- [x] 2.2 在开发版入口复验样本与启动，不执行打包，记录结果。

## 验证记录

- 针对性测试：169 通过；新回归在修复前 3 失败、1 通过，修复后全部通过。
- 首次 npm test：2068 通过、1 失败、2 跳过；失败为 Python 运行时就绪探测，耗时 12.4 秒，探测设置为 10 秒超时。该文件单独重跑 5 项通过。
- npm test -- --test-concurrency=4：2069 通过、0 失败、2 跳过（2071 项）。未修改运行时检测代码。
- OpenSpec 严格校验及 git diff --check 通过。
- 复用现有 3000 端口开发服务器，通过独立隐藏 Electron 窗口将真实解析器与目录生成的模型送入浮窗预览；确认六条真实属性与综合项显示，三行描述消失。未发送交易请求，未打包。
- 预览脚本首次缺少 Vite 依赖版本参数导致加载失败，改用服务器返回的版本地址后通过；临时脚本已删除，截图与日志留在未跟踪的 .cache 中。

