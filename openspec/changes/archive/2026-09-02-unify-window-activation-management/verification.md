# 开发版验证记录

## 自动验证

- 定向 Node 测试：328 项通过，0 项失败。
- 浮士德 Python 自动化测试：23 项通过，0 项失败。
- 完整 `npm test`：1806 项中 1804 项通过，2 项按既有外部环境条件跳过，0 项失败。
- 架构扫描：Python 业务脚本未发现自带窗口枚举、置前或重新抢焦点实现；Win32 前台切换仅保留在公共游戏激活模块和公共助手恢复模块；业务中的直接 `focus()` 仅剩登录窗、查价浮窗、配置引导浮层等明确排除的辅助窗口。
- `openspec validate unify-window-activation-management --strict`：通过。

## 真实窗口场景

- 已确认本机同时存在真实 `PathOfExile_x64` 游戏窗口、Edge 浏览器及标题包含“流放之路”的浏览器页签。
- 从当前非管理员验证进程调用公共游戏激活服务时返回稳定原因码 `privilege-mismatch`，符合“权限不一致时失败关闭、不得退化为标题单独匹配”的设计。
- 用户已在与游戏相同权限运行的开发版助手中完成浮士德按钮端到端人工验收：含游戏名的浏览器页签未被选中，真实游戏被激活，测试结束后助手按原策略恢复前台。

## 交付约束

- 未执行打包验证。
- 未同步主规范。
- 未归档 OpenSpec 变更。
