## 验证结果

- 相关主题、控件、查价及海图测试通过；新增启动脚本与运行时偏好一致性测试单独通过。
- 最终全量 npm test：2021 项，2019 通过、2 跳过、0 失败（约 89 秒）；随后新增的启动一致性测试通过。
- 77 个 Vue 组件完成 Vite 开发转换；开发服务器清理时取消了仍在运行的依赖预优化，页面加载未报告编译错误。
- 浏览器连接现有 localhost:3000 开发服务，验证主页面亮暗模式路由切换；外观设置、标题栏 Enter 操作、手动模式不受系统变化影响、恢复跟随系统、刷新恢复、系统明暗实时变化均通过。
- 查价预览及其挂载到 body 的下拉菜单保持深色；独立查价窗口 html/body 背景透明；区域选择窗口不挂载任何主窗口主题类。
- OpenSpec 严格校验通过。未打包。

## 已修正的验证发现

- 整理共用深色令牌时错误的文本范围替换曾删除亮色令牌和浮窗密度变量；已恢复，并改为共用 Less mixin，新增编译结果完整性测试防回归。无重复整块令牌残留。
- 亮色占位文字在悬浮背景对比度不足，已加深并通过 4.5:1 检查。
- 浮窗预览下拉菜单脱离预览容器继承亮色，已显式附加深色主题作用域。
- 旧测试固定断言深色启动背景、深色选中文字和固定海图背景，已更新为新主题契约；保留了原有对比度验证。

## 验证限制

npm run electron:dev 因端口 3000 已被现有开发服务占用而退出，没有关闭用户进程。随后尝试启动使用临时数据目录的独立 Electron 开发诊断进程，被自动审批以 blocked by policy 拦截；未执行此进程，也未绕过拦截。因此原生 Electron 冷启动、重启以及游戏内实际浮窗未复测；验证覆盖开发页面和主题路由/启动逻辑。
