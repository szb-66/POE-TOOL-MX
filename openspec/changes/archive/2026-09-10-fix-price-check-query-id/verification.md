## 现场根因

2026-09-10 开发版失败快照：星团珠宝查询 ready、total=1703、shown=0。官方查询编号为 296 字符，包含连字符和下划线，命中旧客户端 fetch 和服务 openOfficial 的字母数字校验。客户端未发出挂单请求而返回空列表，网页错误在 IPC 返回后被界面忽略。

## 修复后开发版

- 相同星团珠宝：total=1703，首批 10 条，加载更多后 20 条，价格分布 fetched=100、complete=true。
- 珊瑚戒指：total=6339，首批 10 条。
- 实际网页打开成功，域名为官方域名，URL 编号与查询编号一致，浮窗关闭。
- 两次搜索和 11 次挂单请求均成功。
- Vite 开发转换 HTTP 200；未打包。临时采集、重放和启动钩子已撤除。

## 自动化验证

- 查价相关 193 项测试全部通过，包含真实客户端与服务组合、Vue 组件失败反馈和重试。
- OpenSpec 严格校验通过。
- git diff --check 通过。
- 完整 npm test：2401 项，2399 通过、2 跳过、0 失败（约 202 秒）。

## 失败尝试与清理

- 临时网络采集被 CSP 阻止，改用已有本地文件接口完成只读状态采集，两个临时钩子均已撤除。
- 新测试最初使用 URL pathname 导致中文目录无法读取，已改为 fileURLToPath；组件测试最初缺少状态字段，已改用已有完整预览模型。
- 既有错误文案断言要求“缺少查询编号”，修复文案保留该描述并涵盖无效编号，相关测试已重新通过。
- 删除了临时重放脚本、物品模型文件和截图，保留不含账号、凭据或卖家的验证摘要。
