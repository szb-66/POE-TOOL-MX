## 验证记录

- 新增账本、驱动、截图相关测试与既有回归：32 项通过。
- 圣所测试集 `node --test test/sanctum*.test.js`：409 项通过，0 失败。
- 项目全量 `npm test -- --test-concurrency=4`：2553 项中 2551 项通过、0 失败、2 项跳过。
- `SanctumRunObservation.vue` 的 Vite 开发转换通过；开发模式服务端渲染断言验证自动加入提示、痛苦分类文本、奖励未核对提示及手动重读入口。
- `npm run electron:dev -- --diagnostic-exit-after-interactive`：圣所模块加载成功、interactive 成功、正常退出（退出码 0）。未打包。
- OpenSpec 变更、sanctum-recognition 主规范及 sanctum-planning 主规范严格校验通过；`git diff --check` 通过。

## 尝试与边界

- 新增测试首次因解析函数导入名称错误未运行，已修正为已有 parseSanctumRoomTexts。第二次夹具缺少房间正文定义，导致解析器按标题排除；补齐与实际目录一致的正文定义后通过，未放宽业务校验。
- 项目全量测试初次并发过高，主动中断后以 `npm test -- --test-concurrency=4` 重跑；中断不记为通过。
- 独立 SSR 检查首次因 Node 直接加载 Element Plus CSS 失败；检查命令改为由 Vite 处理 Element Plus 后渲染断言通过。SSR 的 ID provider 与并行 HMR 端口提示仅出现在临时检查环境，未修改应用配置。
- 未执行真实游戏中的连续房间操作、截图视觉核对或实际键鼠扫描；扫描次数、推导状态和手动重读由使用真实驱动的模拟客户端测试验证。开发版启动与渲染不等同于游戏实机验收。

## 按触发条件修正的验证

- 2026-09-12 最近采集的四个位置已提取到 `test/fixtures/sanctum/conditional-effect-room-sequence.json`，只保留游戏词条文字及逻辑房间坐标；不包含进程、账号、整轮 ID 或机器路径。
- 使用真实词库与 SanctumLiveDriver、模拟原生客户端重放：完整基线持有毒水侵袭，普通次要宝藏沿用，下一间追加隐蔽异动，两次推进均不扫描；仁慈喷泉扫描，手动重读再扫描并替换结果。观察与最终结果一致，重复观察不重复追加。
- 修正后账本和驱动测试 33 项通过；连同入口及实用规划测试共 56 项通过。覆盖条件免疫、购买、喷泉、恩赐／痛苦转换、楼层到期、战斗次数、复活和未知机制；不完整基线不能恢复为完整。
- 首轮新增条件痛苦测试仅替换了 rule，残留旧效果 event，导致一项失败；改为独立构造目标效果后通过，未放宽业务判断。临时提取命令的括号笔误已修正，失败命令未写文件。
- 运行中的 Vite 开发服务成功转换实际状态页面；未重启正在使用的开发版、未操作游戏输入、未打包。Electron 主进程改动需重启开发版加载，实机连续房间操作仍待验收。
- 本次全量 `npm test -- --test-concurrency=4`：2561 项中 2559 通过、0 失败、2 跳过。变更及两个主规范严格校验通过，`git diff --check` 通过。原有全局特殊效果拦截已删除，未保留失败测试的旧事件数据或临时脚本。
