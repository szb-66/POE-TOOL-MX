## 1. 收窄配置传输契约

- [x] 1.1 将共享区块目录精确收窄为 `preset.item`、`preset.map`、`preset.story`、`preset.storySkill`、`settings.toolSites`，并保持格式版本与保留区块 schema 不变。
- [x] 1.2 删除海图、商城、其他便携设置和本机环境的配置传输描述器、normalizer 与目录元数据，确保直接请求已移除区块也不能创建导入计划。
- [x] 1.3 让旧配置包中的已移除区块按未知区块显示“不支持”并跳过，同时继续解析和选择五类兼容内容。
- [x] 1.4 保留四类预设的安全复制、嵌套 ID 重建、重名递增、当前预设不变，以及地图 `deviceGrid` 双端确认规则。
- [x] 1.5 保留工具站点按规范化 URL 合并、本地冲突项优先和冲突数量报告。

## 2. 简化设置页交互

- [x] 2.1 将导出与导入弹窗改为统一的大卡片容器和五个内容小卡片，不使用“预设 / 工具站点”标题分区，并删除其他内容的选择入口。
- [x] 2.2 在预设小卡片内部展开逐项列表，保留同类全选、仅选当前预设、全部选择和清空操作，移除完整备份及本机环境二次确认。
- [x] 2.3 导入预览默认选择五类兼容内容，并继续展示来源、数量、冲突、地图坐标和不支持区块提示。
- [x] 2.4 更新成功摘要与错误提示，只报告新增、跳过、冲突和持久事务错误，不再显示运行时同步或保持关闭统计。

## 3. 收敛事务与无用接口

- [x] 3.1 将导入事务收敛为全量预校验、持久数据快照、串行提交和失败逆序恢复，移除紧急停止、运行时同步及强制关闭分支。
- [x] 3.2 从配置传输卡片和服务中删除紧急停止、快捷键、设备设置及制作价格运行时依赖。
- [x] 3.3 检索制作价格批量替换 IPC、设备区块适配器和相关 store 辅助函数的全部调用方；只删除确认由配置传输独占的接口与代码，保留其他功能仍使用的公共路径。
- [x] 3.4 验证导入五类内容不会切换页面或当前预设、修改模块启用状态、启动任务或产生输入。

## 4. 自动化测试

- [x] 4.1 更新核心与区块测试，断言导出目录精确等于五个 ID、其他旧 ID 不支持且无法绕过目录应用。
- [x] 4.2 更新预设与工具站点测试，覆盖逐项导出、ID/重名/嵌套复制、重复导入、地图坐标双重选择和 URL 合并冲突。
- [x] 4.3 更新事务测试，覆盖预校验零写入、持久化中途失败、完整恢复和恢复异常，不再期待紧急停止或运行时同步。
- [x] 4.4 更新 Vue 测试，覆盖五项展示、逐预设选择、全部选择、默认导入、不支持旧区块预览和结果摘要。
- [x] 4.5 保留 Electron 文件测试对主窗口来源、取消、原子保存、5 MiB 限制和安全结果结构的覆盖，并移除已删除价格恢复接口的独占测试。

## 5. 开发版验证与交付

- [x] 5.1 运行全部配置传输专项测试及受影响的预设、工具站点兼容测试。
- [x] 5.2 运行 `npm test`、`npm run build`、严格 OpenSpec 校验和 `git diff --check`，如有并行未提交改动导致的失败则给出可复现证据并保持隔离。
- [x] 5.3 使用 `npm run electron:dev` 验收五类单独/组合导出导入、旧文件不支持区块、地图坐标默认剥离和显式接收；不执行打包。
- [x] 5.4 同步并归档本变更，精确暂存本次范围收窄文件，继续保留既有海图文件和其他活动 OpenSpec 变更。
