## 1. 实现

- [x] 1.1 移除公共组件及 renderer、preload、IPC 上传链路和上传专用 store 方法
- [x] 1.2 保留旧配置兼容及区域微调，调整缺少元数据提示并同步规范条款

## 2. 验证

- [x] 2.1 验证上传入口移除、四类框选成功/取消/失败、旧模板和区域微调及热重载
- [x] 2.2 运行相关测试、完整 npm test、Vite 开发转换及 OpenSpec 严格校验
- [ ] 2.3 在 Electron 开发版检查交互，不打包

## 验证记录

- 区域采集测试 18/18 通过；四类模板的 store 兼容、框选更新、区域微调及组件成功/取消/失败/异常检查通过。
- 相关测试首轮 98/100 通过，失败为未改动的协调器测试；完整测试 2192 通过、1 失败、2 跳过。剩余失败为 bagAutoStash 中“协调器不把启动竞态与干净退出误报为异常退出”：测试按首个 try 截取 close handler，协调器已有更早的 try，导致截取为空。本次未修改协调器或该断言。
- 四个 Vue 入口开发转换通过，OpenSpec 严格校验通过。
- 开发版背包页已观察到旧模板预览、坐标和“重新框选”，无上传按钮；主窗口当前处于模态禁用状态，未完成真实框选交互验收，保留 2.3 未完成。未替换用户现有模板，未打包。
