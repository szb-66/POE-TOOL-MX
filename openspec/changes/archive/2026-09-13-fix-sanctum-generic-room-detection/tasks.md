## 1. 实现

- [x] 1.1 保存墓场原图与证据，补充漏检回归测试
- [x] 1.2 实现通用边框恢复，替换首领模板识别
- [x] 1.3 解耦几何出口与类型，验证后续详情采集与失败保留

## 2. 验证

- [x] 2.1 回归既有楼层和遮挡、图标变化、背景、断线、重复候选
- [x] 2.2 清理无引用模板与失败方案，完成相关测试、项目测试及 OpenSpec 严格校验
- [x] 2.3 开发版验证识别链路与显示，不打包

## 验证记录

- 墓场原图：28 房间、45 条可达连线、initial，两个恢复中心与原图误差小于 8 像素。
- 单帧 NativeSession.observe → hover 坐标回放、显示状态与详情成功/失败保留测试通过；合成图覆盖图标变化、下沿遮挡、缺侧边、断线、背景、离列、重复候选。
- 圣所模块首轮 426/426 通过；最终 npm test -- --test-concurrency=4：2572 通过、0 失败、2 跳过。
- 开发版 Vite 转换测试通过；现有开发服务器圣所页面请求返回 200。未执行游戏内实时键鼠采集，未打包。
- OpenSpec 严格校验与 git diff --check 通过。两种首领模板及全部运行/测试引用已清理。
- 失败试验清理：去除由底边反推上沿的候选方式，改为真实内框上沿明暗交界，避免状态栏及装饰线引起偏移；无临时坐标补位或调试分支遗留。
