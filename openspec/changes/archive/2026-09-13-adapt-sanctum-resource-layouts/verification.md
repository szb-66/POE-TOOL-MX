# 验证记录

## 已完成

- 全量 `npm test`：2569 项，2567 通过、2 跳过、0 失败（约 188 秒）。
- 相关资源、校准、地图恢复和路线测试及 Vue 开发转换：40 项通过；独立恢复回归 22 项通过。
- `openspec validate adapt-sanctum-resource-layouts --strict` 通过。
- 用户截图原始与半尺寸真实 OCR：地图金币 270、坚毅 418/418、启迪 70；独立整栏金币 270、坚毅 416/418、启迪 0。
- 故意删除启迪 OCR 行的双行模块仍保持未知，不推断为零。入口唯一匹配、客户区负坐标、缺失、多匹配、DPI 变化、失焦、取消、按键接管、遮挡和超时均有原生回归。

## 修正与清理

- 初版以深色轮廓确认单行模块失败：状态栏黑色背景与场景连接。已删除该方案，改为边框、文字居中与下方区域证据。
- 全量初次发现当前位置未确认会触发资源绑定异常、阻断效果采集。已修正为不绑定资源而继续效果采集；全量复测通过。
- 旧三小框与 V 开启地图测试按新契约更新；遮挡及越界按资源区域局部失败，不丢弃其他有效字段。

## 待实机验收

开发版进程在运行，但只读原生预检返回“游戏不在前台”。未操作真实游戏，不能宣称恢复后选房实测通过。任务 3.3 保持未勾选。

重启开发版载入主进程变更，重新框选坚毅启迪完整模块、独立完整状态栏和禁域地图入口；原金币选区如果只有数字，需补入金币图标。完成一次需要读取独立状态栏的完整识别，确认最后点击入口恢复且房间可选择。未打包。

## 2026-09-13 完整采集资源传递修复

- 用户本次保存记录的 v6 三个资源区域均已配置，资源截图步骤完成，但最终快照及服务资源记录为空。此修复不需要重新框选。
- 根因：驱动 finalizeFloor 返回 runObservation，而采集协调器仅合并 currentEffects、effectScan，遗漏资源字段。只读复现中，驱动返回 416/418、启迪 0、金币 270，最终快照却无 runObservation。
- 新增完整采集→服务→页面摘要回归与并发房间完成回归，修改前两项均失败；按字段存在性合并资源后通过，保留 null、合法零及失败原因，过期绑定由原有检查拒收。
- 相关测试 61 项全部通过，OpenSpec 严格校验与修改范围差异检查通过。原有无启迪识别算法和校准未修改，无失败修复方案遗留代码。
- 本次全量 `npm test`：2576 项，2573 通过、1 失败、2 跳过，耗时约 267 秒。失败为 runtimeDistribution 的“开发环境优先使用已准备且满足模块要求的内置运行时”，约 12.5 秒后返回 ready=false；探测超时配置为 10 秒。该文件单独复跑 5 项全部通过，首项约 1 秒；与并发负载超时相符，但未将初次失败隐去，也未修改该测试或运行时实现。所有资源修复回归通过。
- 本次只读实机预检仍返回“游戏不在前台”，未宣称实际重识别通过。主进程修复需要重启开发版后再完整识别，以确认当前画面的单项识别结果；未打包。
