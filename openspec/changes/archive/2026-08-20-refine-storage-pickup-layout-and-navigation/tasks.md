## 1. 统一 Tab 持久化基础

- [x] 1.1 为合法值恢复、默认回退、独立存储键和存储异常容错补充共享 Tab 状态工具测试
- [x] 1.2 实现共享 Tab 状态持久化工具，并保持地图 `mapRollingKind` 与商城 `shopActiveTool` 既有数据兼容

## 2. 接入应用内全部 Tab

- [x] 2.1 将设置页从会话级记录迁移到跨应用重启的本机持久化，并保留切换回顶和内容持续挂载行为
- [x] 2.2 为存取页“入库/取件”和制作页动作目录补充稳定 Tab 名称、独立持久化键、合法恢复与默认值
- [x] 2.3 为制作目录中按业务组重复展示的“前缀/后缀”Tab 按稳定组 ID 分别持久化，并验证分组间状态不互相覆盖
- [x] 2.4 审核地图、商城及全部现有 Tab 页面，补充契约测试以确认页面往返、应用重启和无效记录回退均符合统一规范

## 3. 优化取件页信息层级

- [x] 3.1 将仓库自动取件和君锋镇取出高亮的原说明文案分别移入对应标题问号，并移除卡片内重复整行提示
- [x] 3.2 将共享素材总数以括号紧邻标题展示，并将“重建特征”“全部重置”作为标题行右侧带间距的两个独立普通按钮
- [x] 3.3 将共享素材表改为每页 10 条的客户端分页，移除内部最大高度，并在新增、删除、重建和重置后校正当前页
- [x] 3.4 更新存取页面契约测试，覆盖标题提示、数量、独立操作按钮、无内部滚动、分页切片及末页收敛

## 4. 验证与清理

- [x] 4.1 运行 Tab 工具、设置、存取、制作、地图和商城相关的受影响测试
- [x] 4.2 使用 Vite 开发转换验证受影响 Vue 页面，并运行完整 `npm test`
- [x] 4.3 运行 `openspec validate refine-storage-pickup-layout-and-navigation --strict`，检查最终差异并清理失败方案或调试遗留代码
