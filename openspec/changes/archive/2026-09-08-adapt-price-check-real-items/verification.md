# 验证记录

## 自动化

- 八件真实文本保存于 `test/fixtures/priceCheckRealItems.js`。六个行为测试覆盖解析、最终身份、同 ID 求和、来源、默认选择、主进程可信校验和服务重查。
- 首轮相关测试：183 项通过。
- 最终界面相关及新增行为测试：14 项通过。
- 全量 `npm test`：2061 项，2058 通过、2 跳过、1 失败。
- 失败项为未修改的 `singleInstance.test.js`：等待 20ms 后通知尚未到达。使用独立管道名；单独重跑该文件 3 项全部通过。可能是全量并发负载下的时序波动，不将全量结果记为通过。
- OpenSpec 严格校验通过；未改动已有无关工作区变更，未打包。

## 开发界面

使用当前运行的 Vite 开发服务加载实际浮窗组件，输入由当前主进程模型代码生成的真实样本模型。

- 日志显示 63%、28%、38%、37%、19%，三条区域及一条去重派系默认勾选；区域综合行无数值输入框。
- 手动取消灌木之地，其他条件保持不变；来源以悬停文本保留。
- 退化黄金之面提示无法精准区分退化版本，显式确认复选框可以切换。
- 服务回归使用替代客户端验证未经确认不会搜索、确认后只按传奇名称搜索，以及特殊身份经过重查不丢失。

本次没有重启用户已经运行的 Electron 主进程；游戏内查价需重启开发版加载后端改动。浏览器预览验证不等同于游戏内热键和真实挂单验证。

## 招募费用零结果回归（2026-09-08）

- 截图中最低减少 7% 被映射为 `explicit.stat_2257592286` 的正数下限 7；官方目录该 ID 的文本是“赏金猎人招募费用提高 #%”，语义相反。
- 根因是内置目录缺少“招募费用减少 #%”及“赏金猎人招募费用降低 #%”的 negatedMatchers 标记；修复显式与破裂版本。既有目录刷新会保留这些标记，无需新增词语猜测或修改通用序列化。
- 新增测试先复现正数 7 的错误，再验证用户设置最低减免时发送负数上限：工具 -7、胸针 -10、披风旅行报酬 -9；覆盖目录刷新与取消勾选。默认负向词缀仍沿用现有上限输入策略，测试明确设置截图中的最低减免。
- 相关测试 115/115 通过。旧测试仅验证词缀 ID，未覆盖这个中文别名的符号。最初目录文本替换因换行格式校验退出，未写入；改为按目标记录精确修改，无临时补丁逻辑遗留。
- 全量 2064 项中 2061 通过、2 跳过、1 失败：日志数值框新增 v-if 后，界面源码断言错误依赖属性顺序。修正该断言后，所属集成测试 32/32 通过；未再次运行全量。OpenSpec 严格校验通过。
- 需重新启动开发版以加载目录。此次未验证登录后的真实挂单，不将零结果承诺为必然消失；未打包。

## 硫磺喷灯与同类映射检查（2026-09-08）

- 原始查询使用 `explicit.stat_2978905446` 下限 15，与复制文本任务速度相符。用户补充说明放宽物品名称可查到其他结果，并指出工程学速度是另一词缀，因此不能断言原始查询有误。
- 在线对照：原始查询 O6PM7vlCE 为零结果；禁用速度条件有 5 件硫磺喷灯；替换为另一词缀工程学速度下限 15 后有 1 件。这不能证明两个词缀等价，60 混沌石挂单不能作为原物品匹配结果。没有购买或联系卖家。
- 曾错误推断并实现按工具职业替换任务速度；用户纠正后已完整删除该辅助函数、调用和对应错误测试，改为验证完整硫磺喷灯文本中的任务速度与工程学速度各自独立。浏览器恢复原查询的尝试因控制连接失效未完成。
- 检查发现任务速度减慢、夺宝掉落稀有度降低、夺宝掉落数量降低、任务经验获取率降低四类中文别名漏标反向数值；按对应官方正向 ID 修复目录中 9 个类型记录，未采用运行时词语猜测。
- 保留的负向别名修复相关测试 116/116 通过，全量 2063 通过、2 跳过。撤回错误职业替换后真实样本及集成测试 41/41 通过。错误语义的实现即使通过测试也不能证明正确，相关旧测试已删除。
- 全目录反义文案配对仅用于发现候选，不能把所有候选自动判定为缺陷；本轮确认范围为上述赏金猎人相关项，未声称全部物品已无映射问题。

## 官方接口

- 国服公开 `/api/trade/data/filters` 已验证 `logbook` 和四种 `heistequipment.*` 类别。
- 公开赛季目录可读取 S30赛季。
- 无登录凭据的搜索请求返回 HTTP 401 Unauthorized，立即停止后续请求。未读取或输出本地账号凭据，尚未验证登录后的真实搜索结果。
