## MODIFIED Requirements

### Requirement: 物品黑名单
系统 SHALL 支持按名称、基底名称或类别配置黑名单关键词，并允许每条规则选择不区分大小写的模糊匹配或精确匹配；任一规则命中的物品 SHALL 留在背包。

#### Scenario: 模糊匹配规则命中
- **WHEN** 规则模式为模糊匹配，且物品指定字段不区分大小写地包含规则关键词
- **THEN** 系统跳过该物品且 MUST NOT 执行 Ctrl+点击

#### Scenario: 精确匹配规则命中
- **WHEN** 规则模式为精确匹配，且去除首尾空白后的物品指定字段与规则关键词不区分大小写地完全相等
- **THEN** 系统跳过该物品且 MUST NOT 执行 Ctrl+点击

#### Scenario: 精确匹配拒绝部分命中
- **WHEN** 类别精确匹配规则的关键词为“地图”，而物品类别为“地图碎片”
- **THEN** 该规则 MUST NOT 命中该物品

#### Scenario: 没有规则命中
- **WHEN** 物品已成功识别且没有任何有效黑名单规则命中
- **THEN** 系统对当前格执行一次 Ctrl+左键并计入已入库，该格 MUST NOT 复查原格或自动执行第二次入库点击

#### Scenario: 加载无效规则
- **WHEN** 持久化配置包含空关键词或未知字段
- **THEN** 系统忽略该规则且不影响其他有效规则

#### Scenario: 加载旧规则或无效匹配模式
- **WHEN** 持久化规则缺少匹配模式或包含未知匹配模式
- **THEN** 系统将该规则规范化为模糊匹配并保持原有包含匹配行为
