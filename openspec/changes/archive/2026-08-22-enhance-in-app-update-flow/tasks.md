## 1. 主进程更新状态与持久化

- [x] 1.1 使用临时文件与原子替换实现待展示升级记录 repository，并覆盖版本/内容校验、不匹配和写入失败测试。
- [x] 1.2 扩展更新服务状态以暴露 `installing` 和匹配当前版本的待展示内容，并提供已读确认操作。
- [x] 1.3 将安装顺序调整为单次门禁、持久化、严格清理、静默安装并重启，区分记录失败与清理失败的可恢复边界。
- [x] 1.4 为已读确认增加仅主窗口可调用的 IPC/preload/API 能力，保持现有固定更新命令和发送方校验。

## 2. 共享渲染状态与启动检查

- [x] 2.1 新建 Pinia 应用更新 store，集中处理快照、事件订阅、busy 状态和检查/下载/安装/已读操作。
- [x] 2.2 调整主渲染启动编排，按“等待保存的 mode/source 配置→建立监听→读取快照→单次异步启动检查”执行。
- [x] 2.3 去除自动模式重复的首次延时检查，保留每六小时周期检查和自动下载语义，并测试手动/自动两种启动行为。
- [x] 2.4 由共享 store 在主窗口可用后以纯文本弹窗展示匹配版本的更新内容，并在弹窗实际显示后确认已读。

## 3. 标题栏与设置页

- [x] 3.1 在当前标题栏主题与图标改动上增加 `no-drag` 更新入口，映射可用版本、下载百分比、已就绪和安装中状态。
- [x] 3.2 让设置页改为消费共享更新 store，删除局部重复订阅，保留来源/模式、详细进度、纯文本说明、错误重试和未签名警告。
- [x] 3.3 移除设置页安装的二次确认，使标题栏与设置页的已就绪入口都通过同一 store 执行单次静默安装。
- [x] 3.4 增加标题栏文案/交互、共享状态同步、失败隔离和升级后单次弹窗的 UI/集成测试。

## 4. 发布说明元数据

- [x] 4.1 实现可单测的发布辅助脚本，将 `docs/release-notes/v<version>.md` 作为 YAML 多行纯文本注入已生成的 `latest.yml`。
- [x] 4.2 为发布说明文件缺失/空内容、YAML 特殊字符、版本不匹配和成功注入增加单元测试。
- [x] 4.3 把辅助脚本接入 GitHub Release 工作流，并扩展资产校验以拒绝不含非空 `releaseNotes` 的元数据，保持 GitHub/CNB 使用同一份 `latest.yml`。

## 5. 开发版验证与规范

- [x] 5.1 运行更新服务、IPC、启动编排、标题栏、设置页和发布合同相关的定向 Node 测试。
- [x] 5.2 运行 Vite 构建和完整 `npm test`，检查失败尝试是否留下无用代码，并修复所有回归。（Vite 构建通过；更新功能相关测试全部通过；全量测试中仅保留用户明确排除、由并行查价需求产生的既有失败。）
- [x] 5.3 严格验证当前 OpenSpec 变更；不打包或发布，真实 NSIS 双版本升级验证等待用户单独确认。
