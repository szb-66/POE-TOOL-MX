## 1. 分段重排事务

- [x] 1.1 在浮士德 store 中实现按稳定分段 ID 开始、预览、提交和取消的拖拽重排事务
- [x] 1.2 保证预览不写入配置仓库、成功放下只持久化一次、取消和重复结束可安全恢复或忽略
- [x] 1.3 增加 store 测试，覆盖跨多行预览、成功提交、取消回滚、无效目标、键盘单步移动和运行态禁用

## 2. 价格分段拖拽界面

- [x] 2.1 在序号前增加使用 Element Plus 线性图标的可聚焦拖拽抓手，并移除“上移/下移”文字按钮
- [x] 2.2 接入原生拖拽生命周期，使用整行拖动图像并实时更新拖动行的顺序、序号和视觉状态
- [x] 2.3 为抓手增加可访问名称、上下方向键排序和运行态禁用行为，保留行末删除操作
- [x] 2.4 更新分段网格列宽，固定“≤ 旧价 <”单行显示，并将百分比字段改为“降价 | 数值 | %”输入组
- [x] 2.5 更新浮士德 UI 测试，验证抓手、拖拽事件、键盘操作、删除按钮、单行区间关系与“降价百分比”可访问名称

## 3. 开发版验证

- [x] 3.1 运行浮士德配置、store 和 UI 相关测试，再运行完整测试集
- [x] 3.2 通过 Vite 开发服务热更新编译，并在开发版浏览器验证拖拽绑定、键盘排序和运行态禁用；事务测试覆盖实时预览、放下保存与取消恢复
- [x] 3.3 在支持的桌面宽度下确认“≤ 旧价 <”不换行、“降价 | 数值 | %”语义完整、控件不重叠且控制台无新增错误
- [x] 3.4 运行 `openspec validate drag-reorder-faustus-price-bands --strict` 并确认变更材料与实现一致
