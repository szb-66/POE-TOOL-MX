## 1. 配置与纯逻辑

- [x] 1.1 实现背包设置规范化，新增 `blacklist` 默认值、旧设置迁移和名称/基底/类别规则过滤
- [x] 1.2 实现可独立测试的物品头解析与黑名单包含匹配逻辑，覆盖中英文大小写、空关键词和未知字段
- [x] 1.3 扩展入库配置组装，将模板区域、阈值、背包网格、黑名单及全局操作/剪贴板延迟传入 IPC

## 2. Python 检测与入库安全流程

- [x] 2.1 修复模板加载和检测逻辑，同时匹配仓库与背包标题，并实现连续三次命中/丢失的稳定状态事件
- [x] 2.2 增加游戏前台检查、剪贴板序列号读取、普通 Ctrl+C 复制及合法物品头解析
- [x] 2.3 重写 12×5 扫描流程：空格和无法识别格安全跳过，黑名单格保留，其他格仅 Ctrl+点击一次
- [x] 2.4 在开始及每格动作前执行前台和双界面门禁，并统一处理停止信号及输入按键释放
- [x] 2.5 输出逐行 `EVENT` JSON，完整报告检测状态、扫描统计、完成、停止原因和错误

## 3. Electron 编排与接口

- [x] 3.1 为 Python stdout 实现跨 chunk 的逐行结构化事件解析，停止使用普通日志包含匹配驱动状态
- [x] 3.2 在背包 IPC 模块集中维护最新配置、ready 状态、检测/入库进程和单会话锁
- [x] 3.3 在首次 ready 时自动启动一轮入库，在稳定 not-ready 后解除会话锁，并防止并发或重复自动启动
- [x] 3.4 让按钮和 `stashStart` 快捷键复用同一手动补扫入口，同时校验 ready 状态和进程互斥
- [x] 3.5 扩展 preload 和渲染端 API 事件载荷，传递结构化进度、最终统计及可显示的停止/错误原因

## 4. 应用生命周期与界面

- [x] 4.1 将背包事件监听和状态同步迁移到应用级服务，保证离开 `BagView` 后仍可自动触发和接收结果
- [x] 4.2 应用启动时根据持久化启用状态和有效配置恢复检测，关闭模块时同时停止检测与入库
- [x] 4.3 在背包页面加入黑名单规则编辑器，支持选择名称/基底/类别、输入关键词、添加和删除规则
- [x] 4.4 更新状态与完成摘要展示，区分已入库、黑名单、空格、无法识别和中止原因
- [x] 4.5 移除无效按钮位置配置、页面级重复事件监听、遗留盲点逻辑和调试输出

## 5. 验证与回归

- [x] 5.1 添加设置迁移、规则规范化、物品头解析和黑名单匹配单元测试
- [x] 5.2 添加稳定双匹配、单会话锁、重新解锁、手动补扫及并发拒绝的编排测试
- [x] 5.3 添加空格、剪贴板未变化、无效文本、界面丢失、失去前台和用户停止的安全回归测试
- [x] 5.4 运行完整测试与构建，并检查和删除本次重构过程中被替代或失败尝试遗留的无用代码
