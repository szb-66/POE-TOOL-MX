## 1. 复制定型与图像方向候选

- [x] 1.1 扩展共享国服海图形状表和复制文本解析，覆盖五类形状、全半角冒号、空白、未知与非海图文本
- [x] 1.2 重构仓库图像分析器，只输出明确/疑似候选、原图特征及五类合法方向候选
- [x] 1.3 重构双页识别服务，逐候选复制并以复制类型选择方向，同时复用文本解析词缀
- [x] 1.4 保持单格复制失败可继续、未知类型排除求解及双页原子提交，补充对应识别测试

## 2. 本机校准 v2

- [x] 2.1 将校准仓储升级为 schema/feature v2，并实现首次遇到 v1 时无备份一次性清空
- [x] 2.2 将校准匹配限制为复制类型相同的方向校正，并实现仅无有效复制时可用的空格假阳性抑制
- [x] 2.3 保持同图覆盖、单条删除、全部重置和既有 IPC 名称兼容，补充迁移与隔离测试

## 3. 仓库状态与引导式校准

- [x] 3.1 扩展仓库格状态与持久化白名单，确保临时方向候选、原图和特征不进入 localStorage
- [x] 3.2 建立按页行列排序的校准队列，并实现单项保存成功后更新结果与重新求解、失败不前进
- [x] 3.3 用引导式「校准本机素材（待处理数）」弹窗替换批量保存入口，保留独立素材管理
- [x] 3.4 实现复制成功类型只读、复制失败可选空格或五类及合法方向，并补充 store/UI 行为测试

## 4. 自动放入来源安全复核

- [x] 4.1 从共享形状表向自动放入脚本传递别名到内部类型映射，不在 Python 重复中文业务常量
- [x] 4.2 在每个计划来源即将旋转/放置前执行独立复制校验，并实现两个稳定错误码及零危险输入
- [x] 4.3 用确认类型约束来源方向与旋转验证，在全部结束路径恢复剪贴板并保留紧急停止清理
- [x] 4.4 补充九来源逐个复核、类型不符、复制失败、方向约束、剪贴板恢复和停止测试

## 5. 验证与规范收尾

- [x] 5.1 运行相关海图 Node/Python 测试并修复回归，确认保留边缘顺时针扫描现有差异
- [x] 5.2 运行完整 `npm test`、Vite 开发构建和 OpenSpec 严格校验
- [x] 5.3 仅使用 `npm run electron:dev` 验证开发版启动及可执行的双页/校准/复用/自动放入安全路径，不构建安装包
- [x] 5.4 同步 delta specs 到主规范并归档 `improve-puzzle-recognition-calibration`
