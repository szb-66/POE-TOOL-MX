# 验证记录

## 用户实测

2026-09-10，用户反馈：“测试了，不会跳动了”。据此确认本次组合修复后，用户遇到的主窗口拖动跳动已消失。

未单独隔离鼠标转发与重复显隐的影响，因此不声称某一个调用是唯一根因。用户未说明测试轮数或单独验证鼠标穿透，不将反馈扩展为三轮完整验收。

## 自动化验证

- 新增生命周期测试在旧实现上 7 项中 6 项失败，暴露鼠标转发、重复显隐和销毁后执行截图回调问题。
- 修复后相关 18 项测试全部通过，覆盖重复同步、实际边界恢复、三轮模拟停止重启、嵌套截图异常及销毁。
- OpenSpec 严格校验通过。
- 上轮 npm test 日志未包含最终汇总；原执行会话已不可读取。现存日志未检出失败标记，但不能据此认定全量通过。

## 改动复核

仅修改圣所浮窗行为和测试，未改主窗口拖动机制；无失败修复方案或临时诊断代码遗留。未打包。

## 归档说明

用户在获知实测结果和全量测试缺少汇总后明确要求“同步并归档”。按该授权同步主规范并归档，保留任务 2.1、2.2 未勾选及上述验证限制，不视为所有验证已通过。
