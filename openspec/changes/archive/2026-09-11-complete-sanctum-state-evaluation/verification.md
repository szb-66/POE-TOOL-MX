# 实施与验证记录

日期：2026-09-11。仅开发环境，未打包。用户在了解实机验收未完成后要求同步并归档；归档保留该验收待办，不代表现场验证通过。

## 已完成

- 识别完成态区分确认无类别与读取失败，共享类别/目标/阶段/原因缺口用于页面、浮窗和保存路线；计算条件独立展示。
- 123 个目录条目（含次数变体）逐项对照当天 PoEDB 页面文本和审核指纹。清单见 catalog-coverage.json，审核来源见 scripts/sanctum/catalogReviews.json；重新生成目录。名称匹配不绕过文本数值和审核状态校验。
- 两套规划器共享规则和事件评估。确定事件改变资源，条件事件改变风险或机会排序并给出依据；当前资源不重复应用获取时变化。重点四项、免疫转换、购买消耗、剩余次数、过期和复活均有行为回归。
- 图片保存与内存缓存分离：仅保存处理后的裁剪图，按房间/浮窗保存最近成功图片和原文，原子保存新索引后清理旧图；失败保留旧图并标注上次采集。重启读取、重置清理与原始身份校验保留。

## 自动验证

- npm test：2474 项，2472 通过，2 跳过，0 失败，日志 .cache/sanctum-state-full.log。
- 后续收紧守卫房间条件、无伤奖励、连续浮窗重采保留逻辑，并删除旧规划器不再使用的声明。最终 node --test test/sanctum*.test.js：332 项全部通过，日志 .cache/sanctum-state-final.log。全项目测试先于这些局部修改，最终圣所回归覆盖了修改后的代码。
- 全量目录测试逐项验证完整名称加正文识别，以及至少一个适用事件下的资源、风险或机会实际变化；不是只检查规则名称存在。另有两套策略选路对照与事件组合断言。
- 图片回归覆盖 25 个目标、超出内存预算、磁盘原子替换失败、重启、连续失败/成功重采、重置与非法绑定；已有采集测试覆盖停止、迟到结果和楼层身份。
- 初次圣所回归中两项旧断言失败（未确认类别原先预期 unknown、房间图原先预期完整画面）；根据新的行为契约更新后复测通过。没有隐藏失败或跳过相关测试。
- Vite 对实际 Sanctum Vue 页面及组件完成开发转换；最终圣所测试也包含开发转换验证。
- openspec validate complete-sanctum-state-evaluation --strict：通过。

## 开发页面与实机边界

在现有 Vite 开发服务器中加载实际 SanctumView，使用本地示例状态核对页面：读取失败类别、四项效果支持说明、推荐区具体缺口，以及图片弹窗中的“上次采集”和对应原文。页面核对使用示例状态，不代表真实 OCR 成功率。

原生只读环境检查返回“游戏不在前台”，没有向游戏发送采集输入。以下仍待真实游戏与重新加载主进程修改后的 Electron 开发版核对，因此 tasks.md 的 3.2 保持未完成：

- 实际悬停多房间及状态浮窗，确认裁剪图与本次 OCR 结果一致。
- 真实连续重采失败后保留上次成功图，成功后替换。
- 退出并重启开发版后查看截图、重置圣所记录后清理。
- 两种策略在真实地图、资源、组合状态下的推荐依据对照。

主进程及 Python 采集修改需要重新启动 Electron 开发版加载；没有以浏览器示例或自动测试替代现场验收。
