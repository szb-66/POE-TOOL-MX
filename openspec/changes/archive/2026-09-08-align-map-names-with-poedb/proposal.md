## Why

地图目录包含直译和错配译名，历史记录保存旧名称，导致展示与编年史不一致。

## What Changes

- 以编年史异界舆图更新共享目录并补齐地图，保留页面未列出的旧条目。
- 在记录规范化时统一替换旧中文名称，使历史、筛选、统计、导出一致。
- 保持改名入口删除状态，不改变接口及记录结构。

## Capabilities

### New Capabilities

### Modified Capabilities
- `map-tracker`: 地图名称采用编年史译名，并兼容历史名称。

## Impact

共享地图目录、地图标签及记录模型；不增加运行时联网依赖，仅开发版验证。
