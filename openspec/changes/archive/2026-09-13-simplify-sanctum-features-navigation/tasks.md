## 1. 页面和策略
- [x] 1.1 拆分顶层页签并删除账本、圣物页面；验证页面结构与开发转换。
- [x] 1.2 删除旧策略和算法，保留实战设置；通过策略与规划回归。
## 2. 数据链路
- [x] 2.1 删除账本、圣物专属接口、采集、校准、求解及状态输入；检查无失效引用并回归采集。
- [x] 2.2 迁移旧配置，清理保存字段及失效路线；通过迁移、持久化与重启测试。
## 3. 验证
- [x] 3.1 清理专属测试与死代码，运行受影响测试及 npm test，记录结果。
- [ ] 3.2 严格校验 OpenSpec，验证 Vite 转换与 Electron 开发版，不打包。

## 验证记录
- 功能实现及数据迁移完成；圣所整组首轮 448/450，两个旧断言修正后针对性回归通过。
- 新增迁移、接口删除及复制效果回归；启动纠正记忆保存路径的迁移及持久化 31 项通过。
- `npm test` 首轮 2591 通过、1 失败、2 跳过；失败为高并发下运行时探测未就绪，该文件独立重跑 5/5 通过。
- `npm test -- --test-concurrency=4` 最终 2592 通过、0 失败、2 跳过（2594 项）。
- 离线目录生成与保留的 208 条目录一致；原生脚本语法检查、Vite 测试及现有开发服务器的修改页面转换通过。
- `openspec validate simplify-sanctum-features-navigation --strict` 和 `git diff --check` 通过。
- 3.2 的 OpenSpec 与 Vite 部分已完成。已尝试 `npm run electron:dev -- --diagnostic-exit-after-interactive --diagnostic-exit-on-unrecoverable`，因现有开发进程占用固定 3000 端口退出；保留现有实例，新 Electron 启动及游戏实机操作未验证。因此 3.2 保持未勾选。未打包。
