## 1. 焦点恢复基础能力

- [x] 1.1 先添加关闭原因策略和 Windows 游戏聚焦脚本的回归测试，覆盖标题/进程双重匹配、成功、找不到目标、系统拒绝及异常归一化
- [x] 1.2 在现有查价 Windows 输入辅助中实现可注入的最佳努力游戏聚焦函数，并验证目标成为前台后才返回成功

## 2. 查价浮窗生命周期

- [x] 2.1 为用户关闭、鼠标离开、普通失焦、外部动作和系统清理传递显式关闭原因，仅允许前两类请求焦点恢复
- [x] 2.2 在浮窗会话中记录外部动作抑制与焦点恢复去重状态，确保先隐藏再恢复且迟到 blur、重复关闭不会重复执行
- [x] 2.3 在主进程构造浮窗时注入游戏聚焦函数，保持现有 IPC 名称、Ctrl+D 门禁、尺寸、预热和查询行为不变
- [x] 2.4 打开网页市集成功后以外部跳转原因收起浮窗，保持浏览器前台且不恢复游戏焦点，并补充回归测试

## 3. 验证与收尾

- [x] 3.1 运行受影响的查价测试并检查相关 diff，删除失败方案遗留代码且不覆盖工作区既有改动
- [x] 3.2 运行 `openspec validate restore-game-focus-after-price-check --strict`、完整 `npm test`、`npm run build` 和 `git diff --check`
- [x] 3.3 在 `npm run electron:dev` 开发版中验证关闭按钮、鼠标离开、Alt+Tab、主窗口/其他应用、官方市集和连续 Ctrl+D 场景；不执行安装包构建
