## 1. 支持格式数据与回归测试基础

- [x] 1.1 建立纯 JavaScript 支持格式定义，加入物品普通/详细复制、普通地图和高级地图的中文剪贴板样例及页面摘要元数据
- [x] 1.2 使用 Node 内置测试逐个调用真实 `parseItemInfo`，断言展示样例的类别、阶级、基底属性、插槽和词缀等关键解析结果
- [x] 1.3 在 `package.json` 增加不引入第三方框架的 `npm test` 命令，并确认测试可独立运行

## 2. 物品与地图格式说明界面

- [x] 2.1 实现可复用的支持格式说明组件，始终显示摘要并支持展开、收起和等宽展示完整原文
- [x] 2.2 在物品页面接入物品普通/详细格式说明，覆盖详细头、隐式、工艺和显式词缀标记
- [x] 2.3 在地图页面接入普通/高级地图格式说明，覆盖类别、两种阶级写法和六类地图基底字段
- [x] 2.4 验证格式组件在 Electron 与无 preload 的 Vite 浏览器环境中均可显示，且不会读取用户剪贴板

## 3. 调试模式主进程与状态同步

- [x] 3.1 在主进程窗口管理和 IPC 中实现 DevTools 显示、隐藏、状态查询以及 `devtools-opened`/`devtools-closed` 状态通知
- [x] 3.2 打开 DevTools 时使用右侧停靠模式并以可降级方式尝试切换 Console，保证切换失败不关闭 DevTools 或抛出到用户流程
- [x] 3.3 扩展 preload 和渲染进程 Electron API 封装，提供 `setDevToolsVisible`、`getDevToolsVisible`、状态监听及非 Electron mock
- [x] 3.4 在设置 store 中加入默认关闭的 `debugMode`，完成加载、保存、外部状态更新和重置逻辑
- [x] 3.5 在应用根组件初始化持久化状态和全局监听，并在设置页系统设置区域接入调试模式开关
- [x] 3.6 验证设置开关、F12、Ctrl+Shift+I、手工关闭和重置设置都能同步实际 DevTools 状态且不会重复注册监听器

## 4. 跨显示器取点基础设施

- [x] 4.1 在窗口管理中实现单实例取点会话，为每个显示器创建无边框置顶取点窗口并加载专用路由
- [x] 4.2 实现基于发送窗口 bounds 的全局 DIP 计算和 `screen.dipToScreenPoint` 物理像素转换，返回 `{ canceled, x?, y? }`
- [x] 4.3 实现统一的会话结算与清理路径，覆盖单击确认、Esc、窗口关闭、加载失败、重复请求和应用退出
- [x] 4.4 在 IPC、preload 和 Electron API 封装中暴露 `pickScreenCoordinate()`，并为浏览器 mock 返回安全的取消结果
- [x] 4.5 新增无主布局取点路由和页面，显示十字光标、点击确认提示与 Esc 取消交互

## 5. 设置页取点与坐标契约统一

- [x] 5.1 为背包首格、每个通货和物品位置添加取点按钮及忙碌状态，成功后调用对应 store 更新方法立即保存，取消和失败时保留原值
- [x] 5.2 保留所有点位的手工输入，移除 X/Y 的非负限制，同时保持背包格子宽高必须大于零且不显示取点按钮
- [x] 5.3 更新配置验证与物品制作脚本，使 Windows 虚拟桌面的负坐标可用并继续拒绝未配置的 `(0, 0)`
- [x] 5.4 审计物品、地图和背包鼠标移动路径，将 Windows API 输入统一为物理像素，并移除背包脚本的重复 DPI 乘算
- [x] 5.5 保留 pynput 回退路径的边界 DPI 换算，并检查实现过程中不遗留旧坐标修正或无效兼容代码

## 6. 验证与交付

- [x] 6.1 运行 `npm test` 和 `npm run build`，修复所有解析回归与打包问题
- [x] 6.2 在 Electron 开发环境验证调试模式启动恢复、快捷键同步、Console 降级行为和监听器清理
- [x] 6.3 在单屏、右侧副屏、左侧或上方负坐标副屏及混合 DPI 环境验证取点回填与实际鼠标位置一致
- [x] 6.4 复查全部变更，删除调试或失败尝试遗留的冗余代码，并确认原有调试红框覆盖层、预设和旧 localStorage 设置不受影响
