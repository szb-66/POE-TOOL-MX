## 1. 反馈诊断契约

- [x] 1.1 扩展反馈输入校验，约束 `diagnosticCaptureId` 的 UUID 格式、诊断开关联动，并保持现有字段和附件校验兼容
- [x] 1.2 让反馈服务和 IPC 按会话 ID 构建诊断、拒绝不存在的会话，并仅在反馈保存成功后清理会话
- [x] 1.3 增加服务、IPC 和校验回归测试，覆盖普通快照、会话快照、非法/不存在会话及失败保留

## 2. 前端诊断工作流迁移

- [x] 2.1 新增白名单渲染器诊断上下文与共享诊断控制器，支持状态恢复、到期刷新、开始、结束、本地导出和取消
- [x] 2.2 从首页移除诊断按钮、会话提示和弹窗，将首页模块及健康状态持续提供给共享诊断上下文
- [x] 2.3 在“问题反馈”页整合诊断开关、当前诊断导出、会话选择与全部会话状态，并在提交前自动结束活动会话
- [x] 2.4 保证本地导出不清除会话、提交失败保留会话、提交成功清空反馈状态和已提交会话

## 3. 文档与验证

- [x] 3.1 更新帮助中心诊断入口说明及受影响的页面静态契约测试
- [x] 3.2 运行反馈和诊断定向测试、Vite 开发转换与 `openspec validate integrate-diagnostics-with-feedback --strict`
- [ ] 3.3 运行完整 `npm test` 并在 Electron 开发版手动验证普通反馈、当前诊断、会话提交和会话恢复，不执行打包
- [ ] 3.4 审查最终 diff，确认保留工作区原有修改且没有失败方案遗留或无用诊断代码
