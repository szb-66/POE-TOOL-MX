# 开发验收记录

日期：2026-09-13。仅开发版验证，未打包、上传构筑或保存历史构筑。

## 第三方复用

- 使用锁定的 `cn-poe-utils@0.0.9` 翻译和构筑转换引擎。检视 PoExport 使用方式及上游 0.0.10 后确认其数据仍为 3.28，不能单靠升级解决 S30 技能缺失。
- 补充固定 PoeCharm 提交 `ac05b82ca47009780c1ebd6518108e876ccd44b5` 的游戏文本。生成脚本与输出记录来源、SHA-256；保留原库规则优先级，跳过歧义与不兼容占位符。
- 官网新版词缀是含 `description` 的对象数组；转换前递归统一为上游字符串格式。对重复索引的辅助技能引用及三处官网文字差异使用小范围适配。
- [PoEDB PoB 页面](https://poedb.tw/cn/pob) 的服务用于构筑托管和原码读取，不提供国服角色转换；本功能不调用其上传接口。
- 用户示例含 `*` 等非 URL-safe Base64 字符，未猜测修复或声称成功解码。兼容性验收使用开发版从真实角色新生成的完整导入码。

## 自动验证

- 转换和状态测试 13 项通过：脱敏数据翻译、当前赛季技能、XML 转义、压缩往返、装备、技能连接/等级/品质、天赋/珠宝、未知数据、关键字段缺失、空列表、隐私、登录过期、限流、网络失败、账号变化、重复点击、切换与卸载后的迟到响应。
- 页面布局测试 7 项通过，新增页面沿用公共内容边距。
- Vite 开发服务器对两个 Vue 页面、状态模块、路由及前端 API 均返回 200，并已在 Electron 开发窗口实际渲染。
- `openspec validate add-cn-pob-export --strict` 通过。
- 完整 `npm test -- --test-concurrency=4` 通过：2618 项，2616 通过、2 跳过、0 失败，用时约 217 秒。未缩小测试文件范围。

## 实际开发版与 PoeCharm

- 当前账号加载到 24 个角色，选择 S30 的 98 级角色，点击页面导出成功；只读文本框、加载反馈、按钮禁用和生成时间正常。
- 点击复制按钮后读取系统剪贴板，在本机比对与返回导入码完全一致；浏览器剪贴板读取受限时使用系统读取核对。
- 论坛 ID 入口用同一账号的公开论坛 ID 验证，加载和导出成功，产出与本人入口一致。切换角色后清空结果。真实不同账号的权限差异由服务测试覆盖，没有绕过官网权限。
- 使用本机 PoeCharm 所带 PoB 2.67.2 的真实 Lua 引擎，无窗口运行实际 `ImportTab`：先创建空构筑，将原码填入导入控件，检查解码后 XML，触发“导入新构筑”按钮处理器并运行一帧计算，再检查载入对象。并非只检查解压成功，也没有人工操作 PoeCharm 窗口。
- 实测职业、升华和等级匹配；27 件物品的底材和装备栏位、7 组技能的 22 颗技能石名称/等级/品质、128 个已配置天赋节点以及 11 个珠宝插槽均与导出 XML 一致。
- 真实角色最终无未识别词缀提示，仅保留星团配置核对提醒。普通天赋树使用 3.29，与本机 PoB 核对 60 个珠宝插槽顺序一致。
- 验证脚本阻止写入设置与已有构筑；真实导入码和截图仅留在忽略的本地验收目录，不进入版本控制。

## 失败尝试处理

- 最初未适配官网 `description` 对象导致转换失败；已修复输入契约并增加回归测试，移除临时诊断和无效的属性数值处理。
- 第一轮完整测试的导航期望已更新；高并发下一个 Python 测试超时，独立运行通过，全量复测限制为 4 个测试进程。
- 后续全量运行扫描到临时上游源码中的 7 项 TypeScript 测试，项目自身 2616 项通过。已将研究源码移到系统临时目录，保持项目测试范围干净，重新执行完整命令。
- 验收脚本早期的 Python/Lua 回调方案不稳定，已替换为 PoB 自带 zlib 的 Lua FFI 调用，并删除不再使用的回调声明。

## 适用范围

支持国服 PoE 1 当前构筑。未推断战斗配置。未知词缀原文和警告保留，关键底材、技能或天赋数据无法识别时拒绝生成；字典和天赋版本需随以后赛季维护。星团配置仍建议导入后核对。
