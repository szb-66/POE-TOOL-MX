## 1. 时序模型与迁移

- [x] 1.1 更新自动化时序单元测试，覆盖九项截图确认默认值、当前设置、旧设置和旧导入配置只保留 `operationDelayMs` 与完整 `fixedTiming`，并忽略两个自适应字段
- [x] 1.2 提升时序配置版本，删除自适应默认值与规范化函数，将默认表改为 40/20/15/15/10ms 与四项 10ms，并使用封闭投影保存、重置、导入和导出当前时序
- [x] 1.3 更新设置 store 的状态、事务提交、失败回滚和撤销快照，确保不再读取或写入 `adaptiveTiming`、`adaptiveTimeoutMs`
- [x] 1.4 增加迁移回归测试，证明新建、缺失、无效和恢复默认使用新默认表，已有有效用户值不被版本升级覆盖

## 2. 跨进程与 Python 协议

- [x] 2.1 将渲染端、Electron IPC、preload 和自动化宿主的完整时序载荷收敛为 `{ operationDelayMs, fixedTiming }`
- [x] 2.2 将 Python 边界收敛为 `operation_delay_ms` 与 `fixed_timing`，删除 `timing_mode`、`adaptive_timeout_ms` 及自适应分支解析
- [x] 2.3 增加协议负向测试，断言持久化、配置传输和运行时载荷不会重新出现已删除字段

## 3. 固定结果等待消费者

- [x] 3.1 迁移装备制作、地图和海图制作的剪贴板与画面确认，使每个既有检查点完整使用对应固定结果等待
- [x] 3.2 迁移背包入库、普通仓库取件、君锋取件和商城配方取件的剪贴板、选仓与存仓确认，删除提前完成轮询
- [x] 3.3 迁移仓库页签选择、海图自动放置和战斗辅助消费者，保留既有重试次数、错误码和安全停止边界
- [x] 3.4 审计每个动作边界，确保没有重复消费固定等待、隐藏数字下限或遗留自适应专用轮询

## 4. 设置界面与自动检测

- [x] 4.1 删除设置页“自适应等待”和“自适应等待上限”及其说明、事件和本地状态，四项固定结果等待改为始终显示
- [x] 4.2 从自动检测的输入、推荐结果、证据、应用和撤销中删除自适应上限，同时保留其他实测与推导字段及安全门禁
- [x] 4.3 为九项延迟字段建立统一说明元数据，在每个标题右侧显示鼠标悬停和键盘聚焦均可用的问号入口
- [x] 4.4 更新组件与自动检测测试，断言已删除项不可见、固定结果等待始终可配置、九项说明内容准确且问号可通过键盘访问

## 5. 清理与开发版验证

- [x] 5.1 搜索并清理自适应字段、模式分支和失败单字段方案的无用遗留代码，确认业务轮询、OCR、前台检测和安全超时未被误删
- [x] 5.2 运行时序迁移、配置传输、IPC/Python 协议、全部消费者、自动检测和设置页定向测试
- [x] 5.3 运行完整测试、开发构建和严格 OpenSpec 校验，检查现有未提交改动未被覆盖；不执行打包、安装或发布
- [ ] 5.4 在开发版手工验收制作、地图、海图、背包和仓库链路，确认等待值可复现、停止机制有效且固定等待结束前不会提前继续
