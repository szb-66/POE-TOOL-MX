## 1. 原图回归

- [x] 1.1 保存本次原图及证据说明，添加 32 房间、完整已走链和位置的失败回归。

## 2. 单帧修复

- [x] 2.1 实现独立低填充候选、可信列结构和路径证据验证、完整框恢复及去重。
- [x] 2.2 覆盖列首中尾、重复碎片、背景装饰与交叉路径反例，回放既有样本。

## 3. 验证

- [x] 3.1 运行受影响及完整测试、OpenSpec 严格校验，检查失败尝试遗留代码。
- [x] 3.2 在开发版验证原图到地图显示的数据链路，记录实际验证范围，不打包。

## 验证记录

- 原图失败回归先运行：旧流程实际首列 2 间，期望 3 间，测试失败。
- 修复后受影响测试 25/25 通过；完整 npm test：2556 项，2554 通过、0 失败、2 跳过。
- OpenSpec 严格校验通过，识别脚本 diff 空白检查通过。
- 复用运行中的 electron:dev 对应 Vite 开发服务，通过临时回放页面加载实际 SanctumMapGraph 和 sanctumPageDisplay；浏览器核对 32 个节点、2 段完成线和唯一当前位置 2:1。回放文件位于忽略目录 artifacts/sanctum-low-fill-review。
- 原生 NativeSession.observe 的回归验证每次只获取一帧，结果进入实际显示模块后保留位置和完成链。未执行实时游戏键鼠采集、未打包。
- 粗线支持率平台曾使合成样本恢复中心偏移，已通过实际线条中心对齐修复，未放宽测试；全局降门槛试验仅在内存执行，没有遗留该方案。保留原有高填充碎片逻辑与其他工作区修改。
