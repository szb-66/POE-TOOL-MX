## 1. 数据与权限契约

- [x] 1.1 新增 `20260826090000_add_feedback_conversations.sql`，定义消息表、索引、幂等唯一约束、父子归属校验、消息限流、列级 GRANT/RLS 和回复附件存储策略；与后台仓库迁移保持字节一致。
- [x] 1.2 增加迁移静态测试，覆盖本人安全字段读取、跨身份拒绝、用户作者限制、20 条/小时与 200 条/会话限制、对象路径及现有存储总量上限。

## 2. Electron 会话服务

- [x] 2.1 扩展 CloudBase 客户端，支持读取本人反馈摘要、读取单条会话、生成本人附件短期下载地址和幂等写入用户消息，并保持 401 后仅重建身份一次。
- [x] 2.2 扩展反馈服务以复用附件选择令牌，校验 1–2000 字正文和每消息附件限额，按消息作用域上传附件，并在上传或写入失败时逆序回滚。
- [x] 2.3 增加回复进度模型和安全错误映射，确保返回渲染进程的数据不含状态、UID、对象 key、Authorization 或本地路径。

## 3. IPC 与渲染边界

- [x] 3.1 新增反馈列表、会话详情、回复附件选择和发送 IPC handler，所有 handler 复用主窗口 sender 校验并阻止重复进行中的写操作。
- [x] 3.2 更新 preload 与渲染层 Electron API 的真实实现和非 Electron 回退，实现结构化入参与结果序列化。
- [x] 3.3 增加 IPC、preload 和服务单元测试，覆盖越权 sender、重复客户端消息 ID、附件失败回滚和安全错误脱敏。

## 4. 问题反馈界面

- [x] 4.1 在现有反馈组件中增加“提交反馈 / 我的反馈”视图，保留当前表单、诊断和未提交设置页修改，提交成功后切换并打开新会话。
- [x] 4.2 实现本人反馈列表、三种回复提示、手动刷新、原始问题与消息时间线、短期附件下载和空/加载/失败状态，不增加轮询或处理状态展示。
- [x] 4.3 实现文字及最多 5 个回复附件的编辑、进度、单次发送和成功后刷新，发送失败时保留可重试输入且不伪装成功。
- [x] 4.4 增加界面结构与行为测试，覆盖进入加载、旧反馈空时间线、连续多轮回复、三种提示、手动刷新和双向附件。

## 5. 开发版验证与云端闭环

- [x] 5.1 运行反馈定向测试、Vue/Vite 转换检查、完整 `npm test`、`npm run build`、`git diff --check` 和严格 OpenSpec 校验，并清理失败尝试遗留代码。
- [x] 5.2 在后台迁移完成后使用真实开发版匿名身份验证本人读取、交叉身份拒绝、用户回复、管理员附件读取、限流和失败回滚；不打包安装程序。
- [x] 5.3 对比两个仓库迁移 SHA-256 与最终契约，确认桌面端未覆盖或带入无关工作区修改。
