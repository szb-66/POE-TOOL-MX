## 1. 剪贴板解析

- [x] 1.1 为普通连珠射手、恶名残酷女王和恶名焰矢射手三份真实复制文本建立可复用测试样本
- [x] 1.2 在通用词缀处理前实现佣兵凭证段落解析，输出构建、等级、有序技能组和结构化辅助等阶
- [x] 1.3 确保佣兵姓名、分隔线和末尾帮助文本不进入技能、普通词缀或未识别项

## 2. 可信查价模型

- [x] 2.1 使用完整 `佣兵凭证（{BD构建}）` 文本唯一解析 `mercenary_warrant` 身份，保留地图碎片大类并对缺失/歧义身份返回可诊断错误
- [x] 2.2 规范化国服辅技能括号、冒号、空白和等阶文本，将主动/辅助技能仅映射为活动目录的 `mercenary` ID
- [x] 2.3 在查价模型中新增默认关闭的独立 `mercenarySkillGroups`，并将佣兵等级建为默认关闭的可编辑物等属性
- [x] 2.4 扩展主进程模型清理，从 trusted model 重建身份、技能 ID、组关系与顺序，仅接受合法布尔选择和等级范围

## 3. 官方查询与错误

- [x] 3.1 扩展查询序列化器，为每个已启用技能组生成包含主动技能和已选辅助技能的独立 `mercenary` 组，并将 `min` 设为过滤项数量
- [x] 3.2 确保默认凭证查询仅包含官方 BD 身份，只在用户启用时添加佣兵等级和技能组
- [x] 3.3 识别官方查询复杂度拒绝，返回稳定中文提示、保留当前模型且不发送宽松重试

## 4. 查价浮窗

- [x] 4.1 在属性与普通词缀之间添加佣兵技能组卡片，按剪贴板顺序显示主动技能、辅助技能和等阶
- [x] 4.2 实现整组启停、辅助技能逐项切换、子项自动启用所属组及关闭组时保留子选择
- [x] 4.3 为技能组和辅助技能补充选中、悬浮、键盘焦点和 Enter/Space 交互，并保证窄浮窗中可换行、可内部滚动
- [x] 4.4 在属性区以“佣兵等级”展示默认未选中的可编辑范围，并在复杂度拒绝后保留全部分组选择

## 5. 测试与开发版验证

- [x] 5.1 增加解析、普通/恶名身份、全部技能映射、无辅助组、帮助文本排除与默认选择的针对性测试
- [x] 5.2 增加严格单组/多组请求、同名辅助跨组隔离、等级属性、复杂度错误和渲染端篡改的回归测试
- [x] 5.3 增加浮窗源码/集成断言，覆盖分组渲染、鼠标/键盘切换、子选择保留与佣兵等级展示
- [x] 5.4 运行受影响的 Node 测试、完整 `npm test`、`npm run build` 和 OpenSpec 严格校验
- [x] 5.5 使用 `npm run electron:dev` 在开发版中手动验证三份凭证的分组交互与官方查询，不执行安装包构建
