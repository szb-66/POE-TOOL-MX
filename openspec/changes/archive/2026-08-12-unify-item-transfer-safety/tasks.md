## 1. 共享契约回归测试

- [x] 1.1 为共享单次转移函数添加只点击一次、不执行点击后复制、失去前台和输入释放测试
- [x] 1.2 为普通仓库添加“视觉只生成候选，逐候选复制判空且多格占位不点击”的失败复现
- [x] 1.3 为背包入库添加“点击一次后直接继续统计和占位”的回归测试
- [x] 1.4 为君锋镇添加“只用视觉生成候选，逐候选复制判空且不重复点击”的失败复现
- [x] 1.5 删除“点击后复制旧文本即停止”的过度理想化回归测试

## 2. 统一转移实现

- [x] 2.1 在背包 Python 输入控制器中实现可由入库和取件共同调用的单次 Ctrl+点击函数
- [x] 2.2 将背包入库接入共享函数，并在点击成功发出后提交统计和多格占位
- [x] 2.3 将普通仓库取件接入共享输入控制器和确认函数，模型只生成初始候选，逐候选复制判空
- [x] 2.4 删除普通仓库逐格视觉复检、同格两次点击、点击后模型判空和鼠标停车确认等失败方案遗留代码
- [x] 2.5 统一三条路径的 `transfer-unconfirmed` 状态映射、进度载荷和最终输入释放
- [x] 2.6 将君锋镇接入同一剪贴板确认路径，并删除视觉判空、停车、二次点击和确认模式分支
- [x] 2.7 删除点击后剪贴板确认窗口，避免游戏状态延迟导致三条路径中途停止

## 3. 开发版验证

- [x] 3.1 运行背包入库、普通仓库取件、君锋镇取件、共享输入时序和相关集成测试
- [x] 3.2 运行完整 `npm test` 并确认没有调试日志、废弃分支或失败尝试残留
- [ ] 3.3 在开发版分别验证连续入库、普通取件、君锋镇取件、转移失败和运行中失焦；不执行打包
- [x] 3.4 运行 `openspec validate unify-item-transfer-safety --strict` 并记录结果
