# 2026-09-09 离线阶段验证

当前累计完成 **6/16**。最新的目录与圣物解析验证见本文末尾「第二阶段」。

用户本轮明确：暂不能配合国服实机，先完成可离线验证部分。未改变完整变更的实时采集和验收要求；未打包。

## 实现与证据

- 2.2：`sanctumState.test.js` 验证跨轮/切层清理、历史隔离、迟到版本拒收、重启不恢复运行或位置确认、配置保留。
- 2.3：`sanctumPlanner.test.js` 覆盖分叉汇合、不可达、禁选优先、未知路径、领取折扣、三套共用策略、沿途无法恢复及未知效果。页面提供评分拆解和备选展示；离线回放身份未确认时不显示实际路线。
- 3.3：`sanctumLoadout.test.js` 覆盖后台取消/关闭、预算、固定朝向、解锁格、位置、实例数量、传奇指定和未知效果；与独立一格子集及混合尺寸网格穷举比较结果。
- 4.4：`electron/modules/sanctum/README.md`、模板 README 和页面使用说明记录数据来源、开发步骤及未支持项。
- 部分 2.1：新增独立目录刷新、离线重建和精确别名/歧义/历史/PTR/版本隔离测试；快照含 231 条待核对记录。底材、词缀、传奇数据和逐项赛季核对仍待补全，因此不勾选整个任务。
- 部分 4.1/4.2：接入主进程服务、主窗口受限 IPC、preload、开发版导航/功能生命周期、End、三页签、回放及编辑。`sanctumService.test.js` 验证取消后迟到结果不覆盖状态、参数校验、策略持久化和预览不改实际祭坛；实时输入互斥和校准未接入，任务保持未完成。

## 失败分析与清理

首次全量测试：2120 项，2117 通过、1 失败、2 跳过。失败是导航断言使用旧列表，新增圣所后预期未同步；已补齐预期，并把圣所加入公共页面结构检查。

首次独立 Vite 转换：四个模块均转换成功，但关闭服务时与依赖预构建竞争，退出失败。验证改为读取项目 Vite 配置并关闭测试场景的依赖发现/预构建；正式开发配置未更改。新的 `sanctumView.test.js` 和页面布局共 8 项通过且正常退出。原临时命令未加入项目。

## 最终验证

- `npm test`：2125 项，2123 通过、0 失败、2 跳过，正常退出。全量日志暂存 `.cache/sanctum-npm-test.log`。
- `sanctumView.test.js` 与公共页面布局：8 项通过，Vite 开发转换及服务关闭正常。
- 使用真实 Python 运行时，通过 `SanctumService.rescan` 回放 `vault-progress.png`：27 个房间、70 条候选连线（包含未知及不可达边）、状态 `partial`，实际推荐为空，运行结束释放状态。此处不代表 70 条边全部识别正确。
- 最后固定位置属性访问修正后重跑搭配测试：7 项通过。
- `npm run sanctum:data:refresh` 和 `npm run sanctum:data` 均成功生成 231 条待核对记录；回放/求解路径不联网。
- `npm run electron:dev -- --diagnostic-exit-after-interactive`：主页面加载成功，启动诊断正常结束，退出码 0。该检查是开发版启动验证，不是圣所页面实机交互或四层验收。日志暂存 `.cache/sanctum-electron-dev.log`。
- `openspec validate add-poe1-sanctum-planner --strict` 和 `git diff --check` 通过。5.3 保持未勾选，待完整实现后再核对全部规格，而非把严格语法校验当成完整功能验收。

第一阶段完成任务清单 5/16。其余任务的部分进度保留在以上记录中，未缩减任务验收标准。

## 第二阶段：目录、圣物文本与扫描结果联动

完成 2.1，累计 6/16。用户此前选择先处理离线部分的约束继续有效；未触发实时键鼠扫描。

- 目录增至 285 条：包括七种固定尺寸底材、33 类数值词缀、十件传奇及四条楼层名称。已核对的 52 条由 `scripts/sanctum/catalogReviews.json` 记录赛季、内容指纹和证据；其余数据保留未知。底材交叉核对列表与元数据；传奇名称与既有 3.29 快照交叉核对；档案室/宝库中文别名必须来源链接一致，显示名称再与用户样本清单核对。
- 下载不等于审核。刷新变化的记录不会沿用旧的支持状态；赛季变化也会使审核失效。社区历史正文、PTR、歧义名称和未核对记录不进入当前规则。
- 新增复制文本解析：普通/高级复制、固定占格、数值范围、底材歧义、显式与腐化同类词缀、重复词缀、未知词缀和缺失传奇限制都有测试。已识别数值与已实现效果分别标记。
- `mergeSanctumTextScan` 从采集观察重新解析文本；起点未确认时保持未知，多格越界和重叠拒绝。相同文本按位置保留两个实例；局部扫描不误删旧库存；完整祭坛扫描才能更新已装备效果和解锁格，部分扫描撤销确认。真实采集器尚未接入，3.1 保持未完成。
- 修正服务层把所有词缀并入基础分的问题：恢复、恢复偏好及启迪独立计分，禁止恢复/启迪会抑制对应分项。传奇最低区域等级作为硬约束；已确认圣物的房间/首领/喷泉恢复与路线联动。单独权重优先于预设默认权重，包括零值。
- 开发版新增只读文本预览及按中文词缀选择权重，不通过预览写库存或确认装备。

验证结果：

- 圣所针对性测试先后全部通过；最新全量 `npm test` 为 **2136 项、2134 通过、0 失败、2 跳过**。日志 `.cache/sanctum-npm-test-continued.log`。
- `npm run sanctum:data:refresh` 和离线 `npm run sanctum:data` 均成功，285 条/52 条审核计数一致。初始个别下载发生 TLS `ECONNRESET`，通过重试补齐缓存；正式刷新加入最多三次尝试后完整执行成功。未留下绕过来源检查或自动批准新数据的逻辑。
- `sanctumView.test.js` 通过 Vite 开发转换；`npm run electron:dev -- --diagnostic-exit-after-interactive` 正常启动并退出，退出码 0。日志 `.cache/sanctum-electron-dev-continued.log`。此检查不等于实机圣所交互验收。
- `openspec validate add-poe1-sanctum-planner --strict`、`git diff --check` 通过。未打包、未发布，未修改其他未提交业务变更。

## 第三阶段：采集控制离线验证

本阶段推进 1.2、1.3、4.1 的控制层，完整任务仍为 6/16。原生采集驱动、校准界面及实机验收未完成，不新增勾选。

- 新增 `SanctumCapture`，接入 `SanctumService` 停止、End、禁用、重置及退出路径。主进程传入共享自动化锁。未安装原生驱动，开发版 `liveCaptureAvailable` 仍为 false，没有开放实机开始采集 IPC。
- 校验窗口身份、客户区尺寸及 DPI；前台、接管、地图打开、锚点和截图排除证据缺失均拒绝输入。每次输入边界通过驱动 guard 重新核对环境、锁及地图指纹。
- 相同地图至少稳定 300 毫秒才采集，按房间详情指纹增量读取；相同画面不重复悬停，跨层或身份未知清除缓存。文字结果不得覆盖图结构标识和坐标。
- 同层变化期间撤下推荐、保留目标/避让标记；采集停止或自然退出后撤销活动楼层确认，避免修改策略时重新生成旧推荐。
- 每轮使用独立锁所有者。停止立即取消，迟到结果丢弃；原生操作退出前保持锁，拒绝重入，不释放其他任务的锁。驱动必须响应取消并在每次物理输入前调用 guard；当前测试仅验证这一合同下的控制行为。
- 新增 10 项控制层测试，连同既有服务测试共 15 项通过，覆盖稳定/变化帧、未知身份、环境失配、互斥、输入前地图变化、悬停中接管、迟到结果、End/禁用/退出以及同层约束保留。
- 两轮全量 `npm test` 均为 2146 项、2144 通过、0 失败、2 跳过。第二轮包含输入前地图指纹核对；最后的采集结束状态收紧另经上述 15 项针对性测试通过。日志 `.cache/sanctum-npm-test-capture.log`。
- `npm run electron:dev -- --diagnostic-exit-after-interactive` 退出码 0，日志 `.cache/sanctum-electron-dev-capture.log`。仅代表开发版启动检查，不代表圣所实机界面验收。严格 OpenSpec 校验及 `git diff --check` 通过；未打包。

## 第四阶段：样本校准与回放联动

继续推进 1.2 和 4.2，完整任务保持 6/16；新增的是离线样本校准，未替代实时环境校准和实机验收。

- 策略与校准页新增样本参数编辑：识别区域、房间内框宽高、路径 HSV 上下限。可使用楼层页选中房间的尺寸，保存后回放，也可恢复自动参数。
- 校准经过共享参数校验、主窗口专用 IPC、服务和仓库持久化，绑定清单中的样本标识和裁剪图尺寸。尺寸失配拒绝应用；DPI 固定保留未知，忽略外部传入的实时确认标志。清除和保存均取消旧回放并撤下旧图。
- Python CLI 接收独立 JSON 参数，不经过 shell 拼接；再次核对实际图像尺寸与参数边界。区域遮罩保留原始坐标系，房间尺寸参与候选筛选，HSV 范围参与连线证据识别。参数非法或失配输出未知。
- 真实宝库样本对照：完整区域和默认 HSV 与未校准结果完全一致；使用已识别内框尺寸仍得到 27 个房间；错误尺寸、过小区域不能补造房间；改变 HSV 范围会撤下原金色连线。此处只验证参数对已有样本的作用，不证明全部连线和四层识别正确。
- 新增 5 项校准测试，覆盖参数边界、存储恢复、尺寸失配、迟到结果取消、真实 Python CLI 参数链路。校准、识别及新 Vue 子组件开发转换合计 8 项通过。
- `npm test`：2151 项、2149 通过、0 失败、2 跳过，日志 `.cache/sanctum-npm-test-calibration.log`。最后补充的微小房间空区域保护和新子组件转换另经上述 8 项针对性检查通过。
- `npm run electron:dev -- --diagnostic-exit-after-interactive` 退出码 0，日志 `.cache/sanctum-electron-dev-calibration.log`。仅为启动检查；`openspec validate add-poe1-sanctum-planner --strict` 和 `git diff --check` 通过。未打包、未执行真实游戏键鼠采集。

## 第五阶段：游戏浮窗与位置高亮

用户目标明确为完成实现、实机部分留给用户验证。本阶段推进 3.2 和 4.3 的实现，原生采集驱动仍需继续完成；没有将仅有展示层误记为完整实时功能。

- 新增默认穿透、不可聚焦的全客户区浮窗及独立 Vue 路由，接入主进程、全局前台状态和退出清理。按地图裁剪偏移绘制路线、下一房、避让及未知，复用物理像素到 DIP 转换；负坐标和 150% 缩放以窗口替身验证。
- 只显示新鲜的实时证据。离线图、停用、停止、前台丢失、地图关闭、尺寸不符或证据超过 1.5 秒隐藏；页面加载完成后的迟到事件不会恢复已失效快照。
- 圣物高亮由主窗口 IPC 传入实例 ID，要求已确认物品与当前区域的扫描批次一致，拒绝任意坐标、旧批次和越界；五秒后结束。扫描结果边界增加批次透传，尚待原生扫描器产生实际批次。
- 设置内容截图保护，并提供截图期间隐藏窗口的异步边界；测试覆盖嵌套、截图失败、停止和退出。原生截图调用尚未接入，不能认定 Windows 排除行为已经实机通过。
- 22 项浮窗、采集控制、服务及 Vite 转换测试通过。全量 `npm test` 为 2157 项、2155 通过、0 失败、2 跳过，日志 `.cache/sanctum-npm-test-overlay.log`。随后补充独立主题和首帧透明路由，15 项主题/浮窗/页面检查通过。
- `npm run electron:dev -- --diagnostic-exit-after-interactive` 退出码 0，日志 `.cache/sanctum-electron-dev-overlay.log`；OpenSpec 严格校验通过。未执行真实鼠标采集，未打包。

## 第六阶段：原生通道、截图和悬停基础

本阶段是实现进展，目标仍未完成。新原生通道尚未装配进页面开始入口，实时校准/详情转换/圣物扫描仍需继续实现。

- 新增 `SanctumNativeClient` 持续 JSON 管道，复用开发 Python 运行时和无 shell 启动器；逐请求 ID 匹配，限制响应大小，处理超时、取消、进程错误和接管事件。取消后等待旧进程实际退出，再完成请求；旧响应不能写入新会话。不向用户日志或状态复制标准错误中的本地路径。
- `sanctum_native.py` 实现前台窗口/客户区/DPI 查询、锚点截取、地图截图及 OCR、悬停详情读取；复用项目窗口白名单与 OCR 引擎。原生输入前复核地图指纹、锚点、窗口环境及鼠标位置；End、前台丢失和鼠标接管由监视线程中断。详情结束仅移动鼠标解除悬停，不点击房间。
- 指纹包含位置、尺寸、揭示状态及连线证据；标题和详情文字不改变结构指纹。裁剪和锚点受范围、尺寸及图像内容检查约束。
- 首次真实启动失败：隔离 Python 的 `_pth` 不自动加入脚本目录，导致 `ModuleNotFoundError: sanctum_recognition`。已在入口显式添加自身目录，随后真实进程成功响应“游戏不在前台”，正常退出。未遗留绕过导入或预检的方案。
- 新增 6 项测试，包含真实隔离运行时启动、管道协议、等待退出、迟到响应，以及原生控制替身的零输入门禁和地图关闭行为。与控制层合计 16 项通过；后续实际线程 DPI 模式核对经 6 项原生测试通过。
- 全量 `npm test`：2163 项、2161 通过、0 失败、2 跳过，日志 `.cache/sanctum-npm-test-native.log`。OpenSpec 严格校验及 `git diff --check` 通过。本轮真实进程只调用环境读取与未知命令拒绝，未产生游戏键鼠输入，未打包。

## 第七阶段：实时校准与采集入口装配

- 开发版装配 `SanctumLiveDriver`、原生通道、控制器和浮窗截图隐藏边界，加入读取窗口、采集锚点校准、开始实时采集的主窗口专用 IPC 与页面操作。启动前等待用户切回游戏；关闭功能或 End 可以取消准备阶段，迟到数据不写入配置。
- 实时校准独立于离线样本，校验客户区区域、尺寸及 DPI。锚点只由原生截图生成，存储经主进程规范化；再次启动重新验证环境及锚点。校准草稿仅在数据内容变化时刷新，切出/切回的状态更新不重置用户编辑。
- 房间 OCR 扣除悬停前背景文字，解析明确匹配的名称、恢复量、货币数量/领取时机及恩赐痛苦；未知、超大数值及未核对目录条目保留原文。痛苦标识进入约束过滤。当前位置高亮候选和结构指纹联动，未知楼层保留候选图。
- 当前恩赐/痛苦尚未完整读取，主动标记计算缺口；圣物复制扫描仍待实现。未将已装配入口等同于国服四层效果验收。
- 新增 5 项共享参数、详情解析、准备取消、尺寸失配以及服务到原生驱动的完整替身链路测试。24 项受影响测试通过。全量 `npm test` 为 2168 项、2166 通过、0 失败、2 跳过，日志 `.cache/sanctum-npm-test-live.log`。
- 最后收紧当前位置指纹和校准草稿刷新后，原生/实时/Vite 共 12 项通过。`npm run electron:dev -- --diagnostic-exit-after-interactive` 退出码 0，日志 `.cache/sanctum-electron-dev-live.log`。OpenSpec 严格校验通过；未自动启动游戏采集，未打包。

## 第八阶段：圣物复制扫描与高亮重验

- 祭坛、仓库分别保存网格区域、行列、稳定锚点及可选空格/锁格模板，接入共享参数校验、主窗口 IPC、持久化与校准界面。扫描入口等待切回游戏，核对环境后取得独立自动化锁，稳定网格至少 300 毫秒才复制。
- 原生复制使用完整 Ctrl+C 按下/释放序列；有用户按键时不发送，部分发送失败补发释放并终止。只有剪贴板序列变化才读取新文本，不把旧剪贴板当作本次结果。复制前后、解除悬停后分别核对界面及网格。
- 悬停差异的视觉占格与解析底材固定尺寸交叉确认，覆盖格跳过，文本相同的两件物品保留两个实例；复制或起点不明确则保留未知。空格/锁格须匹配用户指定模板，歧义不认定为空。只有完整祭坛扫描更新装备确认，部分扫描保留历史并撤下确认。
- 加入失锁订阅，中途失锁立即取消原生请求；截图隐藏等待后、复制返回后及最终快照后再检查取消和锁。退出等待原生进程关闭才释放自己的锁。修正成功扫描内部停止后残留的控制器引用。
- 高亮原先依赖扫描后 1.5 秒内的旧证据，返回助手时容易过期。现在点击高亮后等待切回对应界面，只读重新验证锚点、客户区和整格指纹，连续刷新约五秒；不发送移动或复制命令。重新校准、重启或网格变化后必须重扫。停用或 End 清除高亮，迟到回调不能恢复。
- 新增 7 项扫描/高亮测试，包含真实 Python 纯图像与 Win32 输入替身测试，覆盖多格去重、独立实例、部分失败保留、失锁、等待退出、只读重验及禁用取消。32 项扫描/解析/状态/实时/原生测试通过；浮窗与 Vue 开发转换亦通过。任务 3.2 已有实现及测试证据，勾选完成；包含实机验收的 3.1 和 4.3 保持未勾选。
- `npm test`：2175 项、2173 通过、0 失败、2 跳过，日志 `.cache/sanctum-npm-test-relic-capture.log`。`npm run electron:dev -- --diagnostic-exit-after-interactive` 退出码 0，日志 `.cache/sanctum-electron-dev-relic-capture.log`。OpenSpec 严格校验与 `git diff --check` 通过。没有打包，也没有执行真实游戏悬停/复制。
- 真实悬停边框能否稳定提供占格证据、空格模板区分率、国服复制文本、完整四层及 DPI 尚待实机验收；当前恩赐/痛苦完整读取仍是实现待办，不能将本阶段记为整个目标完成。

## 第九阶段：实时识别参数和当前效果文字

- 补齐实时校准的房间内框尺寸及路径 HSV，校准页可启用或恢复自动参数。主进程按实际地图裁剪范围重建图像尺寸和局部区域，不信任传入的坐标或尺寸元数据；原生识别器接受经过验证的实时参数，环境仍由原生前台、DPI 及锚点检查。
- 新增可选的当前效果文字区域，每帧独立裁剪 OCR。明确的禁止恢复、禁止启迪、禁止恩赐、隐藏房间文字及经过审核的目录名称进入当前效果；其他文字保留未知。没有把空 OCR 当作无效果，也没有把可见文字当作所有图标均已完整读取。未展开/遮挡内容继续列为缺口，完整实际覆盖仍待验收与后续核对。
- 兼容环境下重新读取窗口会恢复已保存校准草稿，实时识别参数和效果区域随配置重启恢复，运行任务与位置确认不恢复。新增原生观察替身测试验证地图参数传递及独立裁剪，不产生键鼠输入。
- 检查发现第八阶段高亮按钮引用未定义的 `busy`，开发转换测试未捕获该运行时问题；已改为实际的 `store.busy`，避免运行中重复触发。没有保留错误变量或绕过忙状态检查。
- 新增 3 项参数、效果与原生观察测试，扩展配置恢复测试覆盖实时参数及祭坛校准。实时/原生/圣物扫描 21 项通过；最后的配置恢复和 Vue 开发转换 5 项通过。
- 对照当前服务、IPC、preload、自动化锁、紧急停止和页面实现，以及生命周期、取消、参数边界、求解、配置恢复与 Vite 测试证据，勾选 4.1、4.2。包含实机部分的任务仍不勾选，当前 9/16 项完成。
- `npm test`：2178 项、2176 通过、0 失败、2 跳过，日志 `.cache/sanctum-npm-test-current-effects.log`。全量运行后仅修正上述页面变量、扩展恢复测试和文档；相应 5 项测试通过。开发版诊断启动退出码 0，日志 `.cache/sanctum-electron-dev-current-effects.log`。OpenSpec 严格校验通过。未打包，未进行真实游戏键鼠采集。

## 第十阶段：目录重复标识与房间类型

- 完成度核对发现公共房间按四层重复生成同一 ID，导致喷泉、商人等名称精确匹配时出现假歧义。生成器现仅合并 ID 和全部内容均相同的记录；同图标不同效果内容使用带内容指纹的独立 ID，名称仍保持歧义。
- 第一次生成因新增的全局 ID 冲突拒绝而失败，旧目录未覆盖。进一步检查确认恩赐层数和痛苦倒计时等确实共用图标；删除了过度拒绝的实现，改为保留独立变体，未丢弃不同层数记录。
- 核对 https://poedb.tw/cn/Sanctum_league#Rooms 的图标、名称和包含描述后，为 9 类公共房间登记内容指纹与类型。名称及明确的包含描述均可进入类型识别，多个冲突类型不确认。没有由类型推断恢复或奖励数值，同名不同首领房仍未知。
- 离线重新生成目录为 258 条、61 条内容审核通过。补充生成器重复/变体回归、发布目录 ID 唯一性以及实际目录到房间文本解析的测试。25 项目录、实时、圣物解析和研究文本测试通过。
- `npm test`：2180 项、2178 通过、0 失败、2 跳过，日志 `.cache/sanctum-npm-test-catalog-identity.log`。OpenSpec 严格校验和 `git diff --check` 通过。未打包，未操作真实游戏。
- 完成度核对仍发现实时楼层未提取区域等级，最低区域等级传奇只能保守拒绝；此项需继续补齐。实机样本、DPI、完整效果文字和同名房间上下文仍未验证，目标保持进行中。

## 第十一阶段：等级约束与离线交付核对

- 增加独立区域等级校准及原生 OCR，只解析完整“区域等级：数值”字段，不使用角色/物品等级；空白、损坏、冲突及越界都返回未知，每帧重新赋值，不沿用旧等级。
- 修复求解前停止采集清掉楼层、导致等级丢失的问题：仅在本次求解开始前读取已确认等级，后续求解不复用。提前规划可填写独立的搭配目标等级，结果注明 target/observed/unknown；目标不改写楼层、效果或实际祭坛。服务到真实纯求解器的测试验证 79/80 级硬约束及未知拒绝。
- 地图采集增加等待原生悬停时的失锁订阅，立即取消；截图隐藏等待后重新检查 guard，关闭请求完成前不发布迟到详情，不释放新所有者。相关原生/实时/控制测试 26 项通过。
- 评分核对发现未知详情但带类型字段时仍计入条件恢复，与缺口说明不一致。现在只有 matched/manual 详情才计入对应首领/喷泉恢复；增加回归测试，规划/求解/服务 26 项通过。
- 页面补齐房间类型、奖励数量和领取时机手动修正，复用已有参数校验及完整快照语义。追加服务与 Vite 开发转换 7 项通过。
- 新增 `acceptance.md`，逐项对应四份规格的实现、离线验证证据与未确认边界，并提供国服实机检查步骤。核对完成后勾选 5.3；当前 10/16 项勾选，剩余 6 项均含用户已允许后续完成的实机验收，不归档。
- 首轮全量：2182 项、2180 通过、0 失败、2 跳过，日志 `.cache/sanctum-npm-test-area-level.log`；开发版诊断启动退出码 0，日志 `.cache/sanctum-electron-dev-area-level.log`。
- 最终全量：2184 项、2182 通过、0 失败、2 跳过，日志 `.cache/sanctum-npm-test-final-offline.log`。最终全量后新增的手动修正页面入口另经上述 7 项通过。OpenSpec 严格校验通过。未打包，未执行真实游戏悬停或复制，未将完整效果、四层或 DPI 验收记为完成。

## 第十二阶段：公共标题和截图校准改造

本阶段替代前文历史实现中的稳定锚点、手填实时坐标和固定 End 逻辑。历史阶段记录保留用于追溯，当前操作说明以 README、acceptance 和本阶段为准。

- 公共协调器持有地图四层、祭坛、圣物仓库标题注册表，其他消费者更新配置不会丢掉这些模板。公共检测和原生输入前检查共同调用 `interface_titles.py`；楼层 OCR 使用匹配标题矩形。结果必须新鲜，丢失、过期、未来时间或前台变化均拒绝；失效会锁住本次会话，标题恢复不能自动续跑。
- 公共选择器增加有边界校验的动态行列与提示，原有背包/仓库用途不变。地图、提示框、效果、等级、房间内框、路径颜色独立截图、预览、清除；截图前后核对真实客户区、DPI、显示器，负坐标客户区正确转换。取消或失败保留旧值，保存失败回滚；旧区域保留，无预览需重框，旧锚点不转成标题。
- 祭坛固定 5×4，仓库默认 12×12，可在重框前修改。截图按钮格支持三态循环、图例、全部恢复及独立空格/锁格样本模式；重框重置属性与样本。禁用格不输入、不删除历史、不进入布局；物品跨禁用格、忽略格和未知可用格阻止完整祭坛确认。修改撤销推荐、高亮和祭坛确认。
- 原生固定 End 检测已删除；页面停止调用同一紧急停止协调器。主页面从设置 store 读取当前快捷键，浮窗通过设置存储事件同步变化。准备、悬停、复制、高亮停止后，迟到结果不会应用。
- 34 项实时/扫描/原生/Vite 针对性测试通过；新增校准编辑测试覆盖事务、样本和 HSV，9 项通过；标题停止、选择器和公共检测回归通过。Vite 转换加入公共选择器页面。
- `npm run electron:dev -- --diagnostic-exit-after-interactive` 因已有开发版占用 3000 端口退出 1，保留现有实例，不改端口或终止用户进程。独立隐藏 Electron 开发验证窗口使用已有 Vite、独立临时用户目录：12 个校准项渲染、20 格祭坛三态点击及样本选择、真实 nativeImage 裁剪、公共 Python 检测器启动均通过；无真实游戏输入，测试进程已清理。脚本与结果见 `.cache/sanctum-calibration-smoke.cjs`、`.cache/sanctum-calibration-smoke.html`、`.cache/sanctum-smoke-result.log`。
- 验证中失败均保留：旧公共签名断言已更新；停止测试替身缺少 close 已补齐；独立渲染验证首次直接导入无版本依赖造成重复 Pinia，改为让 Vite 解析统一依赖后通过。全量第一次 2191 项中 1 项旧签名断言失败；后一次运行暴露旧源码断言按文件首个 try 截取关闭回调、在新增回滚后错误得到空字符串，以及日志轮询并发竞争，Client 日志单独 9 项通过。修正源码断言的查找起点，冻结代码并降低测试并发再次执行全量，结果另记下方。
- 国服完整四层、实际 DPI/多显示器及游戏输入效果仍待实机验收，不打包、不归档。

- 最终全量 `npm test -- --test-concurrency=4` 退出码 0：ℹ tests 2198；ℹ pass 2196；ℹ fail 0；ℹ skipped 2；ℹ duration_ms 135155.5368。日志 `.cache/sanctum-calibration-acceptance.log`。本阶段 6.1–6.5 完成；历史六项实机验收仍未勾选。主进程入口及 preload 语法、变更空白检查和 OpenSpec 严格校验通过。

## 日志楼层与等级接入（2026-09-09）

本节取代先前“多楼层标题/标题 OCR 判层/等级截图”的设计结论；此前记录保留为历史验证过程。

- 新增 SanctumLogContext：共享监听就绪、已进入区域、前台进程、来源事件及会话核验；四层/编号入口精确映射，不依赖目录审核。每次采集独立 runId；日志长期无新增不使上下文过期。
- 日志状态变化即时撤销当前楼层、等级与推荐，取消原生请求；截图排除浮窗的异步间隙及悬停前后均复核绑定，恢复日志不恢复旧任务。关闭圣所不会停止共享监听。
- 原生环境补充 processId 并参与原生 arm 环境一致性检查。地图标题合并 sanctum-map，按有效新键优先/旧编号最小优先迁移；移除等级配置、标题 OCR 和等级 OCR。页面显示日志来源与设置入口；圣物扫描、样本回放和独立目标等级行为保持。
- 首轮 33 项通过（`.cache/sanctum-log-targeted.log`）；扩展回归 213 项通过（`.cache/sanctum-log-regression.log`）；补充首次启动及恢复后日志专项 12 项通过（`.cache/sanctum-log-context-tests.log`）。
- 全量 `npm test -- --test-concurrency=4` 退出码 0：2212 项，2210 通过、0 失败、2 跳过；日志 `.cache/sanctum-log-full.log`。包含 Vite 开发转换、圣物扫描、公共识别、日志和地图跟踪回归。
- 常规 `npm run electron:dev -- --diagnostic-exit-after-interactive` 因现有开发实例占用 3000 端口退出 1，日志 `.cache/sanctum-log-dev.log`；未终止现有实例，未改端口。使用现有 Vite、独立临时用户配置和隐藏 Electron 窗口验证：8 个校准项、20 格祭坛三态与样本选择、完整圣所页的第 2 层/83 级/游戏日志来源及设置链接、真实 nativeImage 裁剪和公共 Python 检测器启动均通过。脚本 `.cache/sanctum-log-smoke.cjs`、页面 `.cache/sanctum-log-smoke.html`、结果 `.cache/sanctum-log-smoke-result.log`；验证进程已退出。
- OpenSpec 严格校验及主进程/驱动语法校验通过；未打包、未执行真实游戏输入。国服日志与四层画面对应关系仍未实机验收，历史六项验收保持未勾选。

## 2026-09-09 动态提示框与分阶段采集

此节取代旧固定房间/效果文字区域的说明。开发版先补配公共状态栏识别和效果图标栏；状态栏框稳定装饰，图标栏框完整可能占用区域并留边距。开始后自动 V 关闭地图读取类别列表，再 V 打开地图读取房间。未出现或未完整识别的类别与房间必须显示未知。

实机待验：地图打开/关闭两种开始状态、无恩赐时痛苦第一位、全部实际类别、长列表及边缘房间、V 超时/接管/停止、四层国服真实日志和画面。当前附图只有未展开的状态栏，不证明弹框定位或列表完整性；不自动滚动翻页，不打包。

### 本次验证结果

- 相关回归 42 项通过（`.cache/sanctum-dynamic-targeted-final.log`）；补充后的动态专项 8 项通过（`.cache/sanctum-dynamic-final-cases.log`），原生安全回归 6 项通过。
- 全量 `npm test -- --test-concurrency=4`：2219 项，2217 通过、2 跳过、0 失败，130330ms（`.cache/sanctum-dynamic-full-final.log`）。首次全量失败源于旧原生测试缺少新增界面状态模拟，修正模拟并保留停止断言后通过。
- OpenSpec 严格校验通过，JS 语法检查通过。开发页面通过 Vite 转换；独立隐藏 Electron 开发检查通过：8 个校准入口、效果原文与截断原因、日志字段与设置入口、20 格圣物三态、nativeImage 裁剪及公共 Python 检测启动。
- 常规 `npm run electron:dev -- --diagnostic-exit-after-interactive` 因现有 3000 端口占用退出；未关闭用户实例，使用现有 Vite 和临时 userData 的隐藏 Electron 窗口验证（`.cache/sanctum-dynamic-smoke-result.log`）。未打包、未对真实游戏发送输入。
- 清理首次失败测试遗留的测试进程；旧固定文字 OCR 与重复鼠标归位代码已移除。真实游戏验收任务 8.5 与此前六项实机任务仍未勾选。
# 2026-09-09 圣所启动修复补充

- 根因：实际国服使用 `[LOADING SCREEN] (圣所教堂) Duration = 0.971189 seconds`，旧解析器不识别，区域一直 loading；`SanctumFoyer_3_3` 同时未被旧入口规则覆盖。现在严格识别加载完成并绑定同进程区域，实时及恢复共用解析，连接清除旧区域，断线或其他进程完成消息不复活上下文。
- 复用全局 WindowActivationService，仅手动开始实时采集时激活；配置失败不激活，激活失败不产生原生输入，激活及环境返回后检查取消。保留运行中失焦停止。
- 相关测试 52/52 通过（`.cache/sanctum-start-targeted.log`）；全量 2225 项，2223 通过、0 失败、2 跳过（`.cache/sanctum-start-full.log`）。OpenSpec 严格校验及 git diff --check 通过。
- `npm run electron:dev -- --diagnostic-exit-after-interactive` 因已有开发实例占用 3000 端口退出 1（`.cache/sanctum-start-dev.log`）。独立 Electron 开发验证进程读取临时配置副本及真实日志，确认 in-game、第 3 层、83 级；实际全局激活返回 privilege-mismatch，提示游戏权限高于助手，未发送游戏输入。脚本 `.cache/sanctum-start-smoke.cjs`、结果 `.cache/sanctum-start-smoke-result.log`。
- 完整实机启动与切区停止仍待同等权限开发版验收（任务 9.4）。未绕过权限门禁、未修改用户配置、未打包；没有保留失败修复方案的产品代码。


## 2026-09-09 游戏内入口、双布局与效果账本

- 校准 v4：新增地图内状态栏锚点和图标选区，旧配置保留为独立布局。原生按校准位置确认布局。
- 游戏内入口与应用内共用 startLive；点击即时标题检查，移除持续公共标题订阅/过期中断。原生界面、窗口、日志、互斥、取消仍保留。
- 地图和房间先采集，再依据实际唯一路径更新本轮账本；重复帧/重新开始不重复叠加，未知或不能衔接时状态校正覆盖基线。
- 游戏内控制浮窗样式沿用入库按钮，拖动位置独立存于 controlOverlayBounds；调用方限制、进度错误、截图隐藏与主题路由已接入。
- 首轮专项存在旧测试失败：替身仍返回互斥 mode、测试期待准备阶段立即读效果、旧断言要求公共标题过期停止。停止该轮测试，更新替身为独立可见字段、将效果校正断言移至地图完成后；没有保留旧模式兼容分支或空校验函数。
- 现有圣所专项 134/134 通过（.cache/sanctum-entry-targeted2.log）；新增入口/双布局/账本专项 15/15 通过（.cache/sanctum-entry-new.log）。
- 全量 npm test -- --test-concurrency=4：2232 项，2230 通过、2 跳过、0 失败，134411ms（.cache/sanctum-entry-full.log）。全量后另补实际选房→重新开始→跨层校正集成断言，动态专项 11/11 通过（.cache/sanctum-entry-ledger-integration.log），实现代码未变化。
- Vite 开发转换包含新控制浮窗；npm run electron:dev -- --diagnostic-exit-after-interactive 成功进入 interactive 并以 0 正常退出（.cache/sanctum-entry-dev.log）。
- OpenSpec 严格校验通过，git diff --check 无空白错误；未打包。
- 实机 10.5 未勾选：当前保存的配置具备独立状态栏，但没有地图内状态栏锚点和图标选区。不能将开发启动、合成图或替身测试宣称为双布局实机验收。


## 校准界面整理验证

移除圣所页面公共阈值入口及 store 依赖，按地图、状态栏、圣物、离线样本分组，保存状态和折叠截图就近展示；未修改识别或配置格式。相关校准与 Vite 开发转换 17/17 通过（.cache/sanctum-calibration-ui.log）。全量测试结果见 .cache/sanctum-calibration-full.log，退出码 0。OpenSpec 严格校验及空白检查通过。

使用开发 Vite + 隐藏 Electron 窗口渲染状态栏分组，内容与双列布局正常，无横向溢出（.cache/sanctum-calibration-preview.png）。首次预览脚本使用运行时 Vue 的字符串模板，造成空白，已改为 render 函数后重新检查；问题仅在临时预览脚本，无产品代码补丁。预览脚本已清理，保留结果截图。未打包。

## 路径定位与四类效果修正验证
真实档案室推进 1/2/3 图分别定位已走路径终点 1:1、2:1、3:2，宝库推进图定位 4:1；初始宝库图识别首房选择状态。此处 ID 是离线图编号，实时使用会话固定位置 ID。新增档案室首领模板来自用户提供 archives-progress-1.png 的 (1863,664,82,116) 裁剪，未使用上游素材。
专项测试覆盖连续路径、分叉/断链、末端、边框动画、漏检、内容版本、四类标题与缺口、手动位置版本及初始选房。独立运行 26 项通过。首轮全量测试暴露隔离 Python 导入和旧效果账本夹具问题，已修复后复测；不以旧结果作为最终通过证据。
开发启动曾因游戏进程占用 IPv4 3000 端口失败，使用 IPv6 localhost 同端口启动，不切换应用数据来源。
用户将进行真实游戏端到端验收，任务 12.6 在取得反馈前保持未完成。

最终全量 npm test：2238 项，2236 通过、2 跳过、0 失败、0 取消；日志 .cache/sanctum-path-full-tests-final.log。专项 26 项全部通过；OpenSpec 严格校验通过。开发版保持 localhost:3000，使用 NODE_OPTIONS=--dns-result-order=ipv6first 启动，启动追踪确认 renderer-runtime 与 interactive 成功。未打包，真实游戏采集结果等待用户反馈。

## 2026-09-10 实机失败修正

### 校准环境误报回归

- 修复前新增的 dispatch → arm 回归在环境一致且 captureWindows 为空时复现“校准环境已变化”；根因是将整个请求与环境快照比较。修复仅提取 environment/clientBounds，不删除截图排除元数据，不改写校准或添加重试。
- 修复后空/非空浮窗列表均通过；20 个环境缺失或变化用例拒绝启动且无原生输入；9 个运行环境字段变化用例经 watch 触发 unsafe 和取消。
- 原生、入口、实时采集及圣物采集相关测试 30/30 通过；全量 npm test 为 2249 项，2247 通过、2 跳过、0 失败，日志 `.cache/sanctum-arm-tests.log`。OpenSpec 严格校验通过。
- 开发服务 localhost:3000 返回 HTTP 200，未打包；真实游戏使用原有校准启动采集尚未实机操作确认，14.3 保持待办，自动化回归不能替代实机验收。

上一轮实机失败：旧路径 HSV 排除了暗金空心边；上下高度比例过滤漏掉边缘房间；房间样本颜色的全图扩张会合并背景；真实半透明弹框不形成旧算法要求的单个暗矩形。整段原生请求隐藏浮窗和每次读取前后移到屏幕角落造成闪烁和往返。以上旧逻辑已替换。

- 真实截图及实际保存校准均通过同一 NativeSession.observe 路径：30 房间、51 条连线逐边一致，当前位置 room:5，出口确认。真实弹框 OCR 读出四个候选房名及“完成后提供物品”；图标奖励保留未知、不计已知收益。时间基线仍为合成，不能代替游戏悬停验收。
- 153 项圣所相关测试通过，日志 `.cache/sanctum-real-related-verified.log`；最终全量 2248 项，2246 通过、2 跳过、0 失败，日志 `.cache/sanctum-real-full-verified.log`。覆盖部分文字不重扫、失败最多两次、停止保留、迟到进度隔离及截图降级。旧测试中“全程隐藏”“停止删除地图”“排他 HSV”的断言已按新行为替换，没有为通过旧测试保留兼容门禁。
- 真实 Windows/Electron/mss 排除验证：未保护窗口截图色块占比 0.974375；保护后 affinity=17、窗口仍可见且截图色块占比 0。首次探针受隐藏启动方式影响，未满足可见色块前置条件，不能算通过；修正探针显示顺序后重测通过。产品截图核验句柄所属进程及排除标志，降级仅作用于截图调用。
- 控制面板真实 Electron 渲染包含独立停止按钮，模拟点击仅调用 stop、不调用 start，无横向溢出，预览 `.cache/sanctum-control-preview.png`。初次临时预览未加载浮窗主题，已补齐真实主题入口后重新检查，产品不添加样式补丁。
- `npm run electron:dev` 已在 localhost:3000 启动，启动追踪的 renderer-runtime 与 interactive 成功；日志 `.cache/sanctum-real-dev-final.log`。未打包。
- 8.2、10.3 和 13.7 保留待实机复验；没有把截图回放、UI 探针和单元测试算成游戏闭环完成。

## 2026-09-10 停止恢复与房间弹框反馈

- 去掉页面与控制浮窗的采集停止按钮，保留已配置全局快捷键提示。删除原生鼠标位置中断判定及无用 expected_cursor 写入，不添加屏蔽或归位。
- 完整任务覆盖原生退出，停止后再次点击串行等待；等待期间再次停止取消重启。详情缓存按原日志上下文、客户区和校准隔离，并复核房间几何、内容摘要与版本，避免原生重启版本归零导致旧详情误用。已确认进度跳过当前及之前各列；未知位置不猜测。
- 弹框读取后保留遮挡，下一次读取建立真实无弹框基线；修复刚读完即清遮挡导致地图复核丢弃结果的问题。真实截图基线已替换原合成背景。
- 本地通货模板单独匹配，真实样本仅确认混沌石，其余保留未知，未确认数量不计分。最初宽松灰度匹配产生背景误报，已收紧并加入真实背景及纯色负样本；未保留宽松分支。模板补充请求遭官方 HTTP 403，当前13种已有模板，缺少神圣石等图标；文字神圣石仍支持。
- 诊断在系统临时目录 poe-sanctum-diagnostics 轮换20组局部前后截图、完整提示框局部图和阶段JSON；不保存全屏、日志或账号信息，读失败原因独立保留。
- 圣所相关172/172通过；全量2268项，2266通过、2跳过、0失败；随后最终受影响回归51/51通过，覆盖内容摘要、退出等待及真实基线。两个Vue页面开发转换通过，npm run electron:dev -- --diagnostic-exit-after-interactive 退出码0，OpenSpec严格校验通过。测试日志位于工作区 .cache/sanctum-feedback-*.log。
- 实时鼠标、快捷键停止再启动、更多国服通货模板与各楼层完整识别仍需用户开发版复验，16.5及原有实机待办未勾选；未打包。

## 2026-09-10 识别框、截图证据与最高相似度匹配

- 根因修正：不再用版本核验阻止文字身份匹配，不再丢弃低于 0.60 置信度的 OCR 输出；游戏内标注实际文字裁剪区域，页面按需读取同一目标的截图、文字框和匹配结果。最高候选无最低分门槛，计算支持与版本核验独立。
- 冻结截图最多 20 张、64 MB，仅驻留内存；进度只广播引用。改框使用无游戏输入的独立 OCR 进程，检查运行、楼层、房间、截图身份及操作代次，拒绝停止、切层、替换截图后的迟到结果。停止结果仍可查看，重新采集恢复自动定位，不改写全局校准。
- 真实弹框 fixture 经独立原生 OCR 读取：区域 (1281,518,1067,265)，5 个文字框，完整四房名和奖励说明。候选相似度包含 66.7%、60%、33.3% 和 100%；低分照实采用，并不证明名称正确。
- 相关测试 183/183 通过；全量 2278 项，2276 通过、2 跳过、0 失败。日志 .cache/sanctum-ocr-related.log、.cache/sanctum-ocr-full.log。覆盖错漏字、换行、多词缀、数字和否定词、低分、同分、空结果、未核验词条、截图身份及停止竞争。
- 初次相关回归发现数值解析不完整被误标为完整识别，已恢复结构化字段的 partial 判定，保留身份匹配；没有放宽回归断言。初次界面验证使用 runtime-only Vue 的字符串模板导致空页面，已改测试页面为 render 函数；产品未添加绕过代码。
- Electron 开发环境预览显示真实截图、蓝色区域和黄色文字框，实际鼠标拖动后提交冻结截图坐标，结果 .cache/sanctum-ocr-ui-result.json；预览 .cache/sanctum-ocr-preview.png。框选 API 在界面探针中模拟，原生 OCR 另经真实进程验证，二者不冒充游戏端到端验收。
- Vue 开发转换通过；electron:dev -- --diagnostic-exit-after-interactive 退出码 0，启动追踪 renderer-runtime、interactive 均成功。OpenSpec 严格校验通过。未打包；17.6 和之前实机待办继续保留。
