# 效果识别与截图核对验证（2026-09-11）

## 实现与根因
- 类别标题门槛与真实无标题效果浮窗不符，改为名称/描述匹配词条；从现有来源表提取 123 个效果的级别（次要恩赐 44、主要恩赐 15、主要痛苦 22、次要痛苦 42）。未重新批准未核验的机制。
- 房间装饰标题检测把地图大块区域误认为效果浮窗，改为效果四边透明度边界，并保留原有未知/截断/滚动缺口。
- 地图恢复原本只在成功路径；普通错误进入收尾检查，单步超时可恢复原生上下文，停止和环境失效仍禁止按键。
- 效果冻结事件、只读引用和失败阶段接入状态页；删除房间改框 UI、重识别 IPC/服务/驱动及其忙碌状态。图片仍只在有界内存中保存。

## 原始截图
完整原图定位并 OCR，第二张去除 33px 标题栏后与客户区对齐：
- 全知之眼：区域 x=1374、y=1236、w=558、h=161；词库分类 majorBoon，名称描述合并为一个条目。
- 钝化之剑、过门税款、巫毒人偶：区域 x=1332、y=962、w=796、h=437；三条分别匹配 minorAffliction，计算不支持仍显示已识别。
- 原文保留 OCR 装饰噪声；孤立关闭叉号 X 不作为词缀。三条痛苦中背景装饰会被 OCR 附加为数字，数值不一致时不得冒充计算支持。
- 两张局部夹具保留地图下方干扰背景，不保存完整客户区；按原始尺寸回放，悬停点上下偏移 6/12px 不改变结果，旧静止浮窗不视为新内容。

## 自动与开发验证
- 全量：2421 项，2419 通过、2 跳过、0 失败（.cache/sanctum-effects-verified-full.log）。
- 四边范围修复相关 41/41；最终关闭叉号过滤、数值冲突及改框状态清理后的针对性 32/32（.cache/sanctum-effects-verified-targeted.log）。
- 覆盖多条/混合类别、换行、未知和歧义、缺少级别、截图事件绑定、过期引用、普通扫描/OCR 失败恢复及安全中断。
- 现有 3000 端口 Vite 服务转换四个受影响组件均 HTTP 200。浏览器用实际 Vue 组件和隔离预览数据确认按钮展开、完整画面切换、原文与截图失败提示；临时预览文件和标签页已清理。
- OpenSpec 严格校验通过。未打包。

## 失败尝试与修正
首轮旧测试仍强制类别标题/调用已删除改框接口，替换为新行为断言后通过。首次透明度候选仅验证上下边界，局部夹具遗漏下方深色状态区；完整图复核暴露错误候选后补上左右边界、扩大夹具干扰区域并回归。宽松距离分桶已删除，采用实际距离。最后发现关闭叉号 X 被当未知词缀，保留原文而从语义匹配排除，并新增完整性断言。开发 HTTP 检查最初错误地匹配 Vite 客户端源码中的错误提示字符串，改为状态码和模块响应检查，实际服务正常。

## 实机限制
只读原生 environment 检查返回“游戏不在前台”。没有执行真实游戏闭环，也没有重启用户正在使用的 Electron 实例。主进程和 preload 改动需重启 Electron 开发版后生效；32.5 继续待现场验收，不能据自动测试宣称实际关图、悬停、开图已成功。
