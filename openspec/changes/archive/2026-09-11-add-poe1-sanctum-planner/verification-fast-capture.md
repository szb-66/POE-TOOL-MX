# 截图快路径与按钮全流程验证

## 已实现
- 采集进程复用MSS，房间及状态栏悬停仅等待150ms、抓取一次原图并复制到共享内存。定位、PNG/Base64、证据输出及局部诊断位于独立后处理进程，OCR仍使用独立进程。
- 后处理进程持有Windows命名共享内存，原图及版本基线总预算512MiB；槽位引用计数、容量等待取消、故障及结束回收已覆盖。
- 截图与分析独立计数，必要图标未完成不提前计数；后台事件不能回退按钮阶段。再次启动清除旧进度，截图查看在证据生成前显示处理中。
- 按钮加载动画及固定尺寸文案覆盖准备、截图、分析、回收、成功、部分、停止和失败。状态栏同时含效果和奖励，因此与第34节统一采用“截图状态栏”的文案。

## 自动验证
- 首轮全量2435项：2432通过、2跳过、1失败。失败为共享日志轮转测试在高并发负载下尚未观察到轮转后内容；单独复核通过，没有修改日志实现掩盖失败。
- 限并发全量：`node --test --test-reporter=tap --test-concurrency=4`，2433通过、2跳过、0失败。
- 最后收尾改动后的相关回归：61/61通过，包含真实后处理子进程、Windows共享内存、进度、服务生命周期、OCR、恢复与Vue开发转换。
- 新增验证包括后台阻塞不阻止第二次冻结、缓冲满额取消、工作进程故障拒绝全部排队任务、旧证据身份拒绝、生产者退出后像素仍有效及最后句柄释放、控制通道不含原始像素、所有截图结束后分析及图标仍继续、空队列和迟到事件。
- 原有三张状态栏截图四种宽度、房间正文和单次截图回放继续通过。
- `openspec validate add-poe1-sanctum-planner --strict`通过。

## 开发版验证与限制
使用`npm run electron:dev`成功启动。通过开发版组件预览核对260×56固定按钮：截图房间12/20、截图状态栏2/4、分析中0/24及15/24、正在完成、分析完成·再次采集。前五种加载动画可见且禁用，最终状态恢复点击。预览第一次使用带不同版本参数的Vue依赖URL导致重复Vue实例，改用Vite标准vue导入后正常；生产组件没有该问题。

原生只读预检先返回“游戏不在前台”。随后通过开发版正式startLive入口尝试采集，启动预检返回“游戏权限高于助手，请以相同权限运行开发版助手”，未执行本次鼠标截图。没有绕过权限或安全检查。

35.6保持待验收：需要以与游戏相同权限启动开发版，完成同分辨率至少三轮实机采集，记录moveIntervalMs中位数/P95、totalMs、capacityWaitMs、整轮截图时间和失败率，并现场确认截图结束后的按钮持续分析。500ms及降低50%是优化目标，当前没有实机结果证明达标；历史1.6–2.2秒记录不能当作本次成绩。

未打包。临时低权限开发实例在验证后关闭，避免占用开发端口。旧同步定位/编码/诊断及当前鼠标目标证据关联已移除，原有未提交工作保留。
