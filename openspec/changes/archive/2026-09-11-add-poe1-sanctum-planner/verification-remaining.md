# 剩余任务核查（2026-09-11）

## 独立开关

核查已有 `SanctumService`、存储、IPC、校准取消和页面实现：缺失偏好默认关闭，模块可用与偏好分离；重启、移除及重新添加不自动运行。关闭取消任务及框选，通过代次和取消信号拒绝迟到结果；保留已有数据供浏览。开启先保存，关闭先停止，失败不会恢复任务。

补充发现并修复：store 只记录 `{ success:false }`，IPC Promise 直接拒绝时没有写入错误，随后刷新成功会留下空提示。统一在调用边界捕获异常，保持原始错误供开关刷新后展示。新增回归先复现失败（实际错误为空），修复后开关五项全部通过。没有添加重试、恢复任务或异常吞噬分支。

## 开发版验证

通过 `npm run electron:dev -- --remote-debugging-port=9222` 启动实际 Electron 开发版，进入圣所页面。关闭开关后 `enabled/configuredEnabled/running` 均为 false；34 个已有房间保留，采集、重置、资源和账本编辑禁用，截图核对与设置浏览仍可使用。控制浮窗返回 `visible:false`。重新开启恢复用户原有偏好，不自动启动任务。

实际状态页检查：四类效果独立显示，历史资源保持待确认，失效地图不允许重读或纠正。用现有 `.cache/sanctum-status-preview.html` 加载实际 Vue 组件和隔离回放数据，核查次要恩赐及主要痛苦显示“无”，混沌石×14、机会石×30、点金石×40 分别出现在状态栏奖励及已选未领账本，不混入效果，不宣称全账本完整。预览只验证展示，不是游戏采集证据。

第一次开发检查的异步调用超时，后续页面就绪后重新读取成功；未向产品加入延时或超时补丁。开发版截图留在 `.cache/sanctum-remaining-state.png`。

## 现场限制

检测到运行中的游戏，但实际 `startLive` 返回“游戏权限高于助手，请以相同权限运行开发版助手”。结果为 error、running=false，保留原34个房间；没有进入悬停或状态栏采集，不绕过权限预检。

15.4 的页面开关与浮窗隐藏状态已验证，但真实可见浮窗的关闭还没有现场证据，因此保留未完成。四层、祭坛/仓库、多显示器、三种玩法连续选房、状态栏闭环、三轮性能和快捷键等实机待办继续未勾选。18.4 的开发启动阻碍本次已解决，旧正方形和有限补图行为仍由后续任务替代，不恢复过时实现。

下一次现场验收需要在与游戏相同权限的终端运行 `npm run electron:dev`，再按 `tasks.md` 的未勾选项目提供实际画面和运行证据。不打包、不归档。

## 验证日志

- 初次相关检查：28/28通过，包含开关、校准取消、浮窗生命周期、真实奖励截图 OCR 及 Vue/Less 开发转换：`.cache/sanctum-remaining-targeted.log`。
- 新增异常处理回归：5/5通过：`.cache/sanctum-remaining-enabled.log`。
- 修改前全量：2449项，2447通过、2跳过、0失败：`.cache/sanctum-remaining-full.log`。
- 最终全量：2450项，2448通过、2跳过、0失败，包含新增异常处理回归和Vue/Less开发转换：`.cache/sanctum-remaining-full-final.log`。OpenSpec 严格校验通过。

本次完成15.1、15.2、15.3、18.4、34.4，累计129/164；其余35项保留现场验收边界。开发版保持运行，用户原有开启偏好已恢复，当前没有圣所采集任务。
