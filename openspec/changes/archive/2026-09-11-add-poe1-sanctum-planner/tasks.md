## 21. 路径与房间状态区分

- [x] 21.1 同步规格并实现共享基础状态、已走链、可达传播与版本绑定推荐。
- [x] 21.2 主页面和浮窗共用图形与图例，独立显示识别、手动标记及选中。
- [x] 21.3 状态和浮窗回归、全量测试、Vite 开发转换及 OpenSpec 严格校验。
- [ ] 21.4 开发版检查两处图例、箭头与标签，真实游戏背景辨识度复验，不打包。

21.3 验证：相关 29 项通过；全量 2297 通过、2 跳过、0 失败。最后取景范围及字体调整后相关 8 项（含 Vue 和 Less 开发转换）通过，OpenSpec 严格校验通过。两处移除旧路线/识别颜色覆盖样式，未修改采集过滤、评分或持久化。

21.4 部分验证：复用已运行的 3000 端口开发服务器，使用 archives-progress-3 实机截图重新识别图结构，在共用 SVG 组件开发预览中检查主页面取景、图例、箭头、双框、当前圈、点击选中，以及真实截图背景叠加。预览首次使用直接依赖 URL 导致重复 Vue 实例、作用域样式缺失，改用 Vite 标准 vue 导入后复查正常；生产组件无该问题。预览位于 .cache/sanctum-display-preview.html。尚未在运行中的游戏里复验实际 Electron 浮窗；主进程快照改动需重启现有开发版后生效，不将静态截图预览作为实时验收，21.4 保持未完成。本次不打包。

## 17. 识别框、截图与最高相似词条（原有任务）

- [x] 17.1 实际OCR区域、完整文字框与置信度，按请求传递截图并有界内存保存。
- [x] 17.2 最高相似词条、换行段落、多条匹配与识别/计算分离。
- [x] 17.3 证据读取及冻结改框接口，生命周期和缓存隔离。
- [x] 17.4 游戏OCR边框、默认跟随预览、原文对照与手动框选。
- [x] 17.5 相关/全量测试、开发转换与开发启动、严格校验。
- [ ] 17.6 用户开发版复验实时区域与房间识别，不打包。

## 16. 停止恢复与房间详情修正

- [x] 16.1 移除停止按钮和鼠标移动中断，不添加输入屏蔽或鼠标归位；保留快捷键及环境检查。
- [x] 16.2 完整退出后串行再次启动；同日志、环境和校准复用成功详情，失败重读，跳过已确认已打列。
- [x] 16.3 修正弹框遮挡生命周期和悬停基线，保存有界本地采集证据，分离文字与通货图标识别；未确认数量不计分。
- [x] 16.4 回归停止重启竞争、鼠标轻触、进度过滤、真实弹框及图标；相关和全量测试、开发转换及严格校验。
- [ ] 16.5 用户开发版确认真实采集、轻触鼠标、快捷键停止再启动及更多通货图标，不打包。

## 15. 圣所独立总开关

- [x] 15.1 持久化用户偏好，区分模块生命周期与实际启用，关闭取消任务及框选并隔离迟到结果。
- [x] 15.2 页内总开关、只读浏览、前后端操作门禁、重复提交与保存失败反馈。
- [x] 15.3 覆盖旧配置、重启、模块移除恢复、运行中关闭及失败回归，执行相关及全量测试、开发转换和严格校验。
- [ ] 15.4 Electron 开发版验证页面开关及浮窗关闭，不打包。

15.1–15.3 续作核查（2026-09-11）：已有实现覆盖偏好与模块生命周期分离、关闭取消及迟到隔离；补上 IPC Promise 拒绝时页面丢失错误的根因修复，新增回归先失败后通过。相关28项及开关5项通过；最终全量2450项，2448通过、2跳过、0失败，含Vue/Less开发转换，严格校验通过。实际开发版开关关闭后保留34房间且编辑禁用，控制状态为隐藏；权限差异使游戏现场浮窗验证未完成，15.4保留待办。详见 [剩余任务验证](verification-remaining.md)。

## 1. 国服采样与识别基础（原有任务）

- [ ] 1.1 采集国服四层地图、房间浮窗、祭坛与仓库样本及标注，核对窗口尺寸和 DPI，形成可回放样本清单。
- [ ] 1.2 实现房间与连线识别、区域/房间/路径校准及未知输出，用实机样本验证图结构和缩放失配处理。
- [ ] 1.3 实现自动稳定检测及增量悬停采集，验证前台丢失、阶段预检界面不符、全局紧急停止和互斥均停止输入且不会自动抢回鼠标。

## 2. 数据与规划

- [x] 2.1 新增编年史离线目录刷新、版本与来源记录、中文别名及特殊规则支持状态，离线解析测试覆盖歧义与历史数据隔离。
- [x] 2.2 实现楼层快照、当前状态和跨轮隔离，测试切层、身份未知及重启后不恢复任务。
- [ ] 2.3 替换旧三套权重为实战策略、约束优先和理由拆解；以第 23 节场景重新验收，历史测试不证明新策略完成。

## 3. 圣物库存与求解

- [ ] 3.1 实现可见区域圣物复制采集及占格，实机验证多格物品、相同文本实例和部分失败；库存持久化与已确认祭坛独立。
- [x] 3.2 实现词缀汇总、筛选、位置高亮和固定/排除配置，测试重扫不误删未知位置。
- [x] 3.3 实现后台分支限界搭配求解、取消和预算结果，对照小规模穷举验证三套候选、锁定传奇、解锁格、固定朝向及规则缺口。

## 4. 应用集成

- [x] 4.1 接入 Electron 服务、preload/IPC、自动化锁、全局紧急停止和功能生命周期，测试参数校验与禁用资源清理。
- [x] 4.2 实现三个页签、策略编辑、手动修正、重扫、重置及推荐预览，验证 Vite 开发转换和配置恢复。
- [ ] 4.3 实现默认穿透游戏浮窗与位置高亮，开发版验证地图离开隐藏、多显示器/DPI 及截图排除浮窗。
- [x] 4.4 补充来源说明及使用帮助，核对所有数据与代码引用。

## 5. 综合验收

- [x] 5.1 运行受影响测试及 npm test，分析失败根因并清理失败实现遗留，保留验证记录。
- [ ] 5.2 在 npm run electron:dev 完成国服四层、圣物扫描、搭配及路线联动验收，关键识别未确认时仅显示部分结果，不打包。
- [x] 5.3 执行 openspec validate add-poe1-sanctum-planner --strict，确认规格与实现一致且所有已勾选任务有证据。

离线交付核对与用户实机验收步骤见 `acceptance.md`。用户允许实机部分自行验证，因此含实机验证的 1.1、1.2、1.3、3.1、4.3、5.2 保持未完成；本变更未归档。

## 6. 截图校准改造

- [x] 6.1 接入公共标题配置和新鲜状态，原生输入前复核同套规则，移除独立锚点。
- [x] 6.2 扩展公共选择器动态网格和提示，独立截图、清除、预览、元数据校验及旧配置迁移。
- [x] 6.3 应用内三态网格和点击样本选择，重框重置、历史保留、完整祭坛和合法布局约束。
- [x] 6.4 停止输入入口复用全局协调器，显示实时配置快捷键并移除固定 End 假设。
- [x] 6.5 补充离线回归测试、全量测试、Vite 开发转换、开发版启动检查、说明与 OpenSpec 严格校验。

## 7. 圣所日志楼层与等级

- [x] 7.1 同步 OpenSpec，接入共享日志、精确楼层映射和前台进程核验。
- [x] 7.2 截图及悬停前后验证上下文，切区与日志失效立即撤销状态并取消迟到结果。
- [x] 7.3 合并地图标题及迁移，删除标题/等级 OCR 与区域等级校准，显示日志来源和配置入口。
- [x] 7.4 补充离线回归、全量测试、开发版启动与 OpenSpec 严格校验，更新说明；国服日志与四层实机验收保持未完成。

## 8. 动态提示框与分阶段采集

- [x] 8.1 校准 v3、公共状态栏锚点及旧文字选区清理。
- [ ] 8.2 动态弹框定位、类别图标悬浮和完整性证据（实机失败，随第 13 节重新验证）。
- [x] 8.3 日志绑定的效果/地图阶段、V 切换与本次效果缓存。
- [x] 8.4 相关及全量测试、开发启动、严格校验与说明同步。
- [ ] 8.5 国服动态弹框、效果类别及 V 切换实机验收。

## 9. 圣所启动与国服日志修复

- [x] 9.1 识别国服加载完成系统消息，实时与恢复仅确认同进程区域；兼容双编号圣所入口并回归安全门禁。
- [x] 9.2 开始实时采集通过配置预检后调用全局窗口激活，保留失败及异步取消门禁，页面启用不抢焦点。
- [x] 9.3 完成相关及全量测试、开发版启动检查、OpenSpec 严格校验并记录结果。
- [ ] 9.4 开发版实机验证已处圣所启动、自动切回游戏及切区停止。

## 10. 游戏内入口与效果推算
- [x] 10.1 双布局校准、即时启动检查与移除持续公共标题中断。
- [x] 10.2 替换路径效果累计为当前位置实际快照；验证同位置复用、移动重读与未知隔离。
- [ ] 10.3 游戏内按钮、独立拖动持久化、进度错误与截图隐藏（实机闪烁，随第 13 节重新验证）。
- [x] 10.4 相关及全量测试、Vue 开发转换和严格校验。
- [ ] 10.5 开发版双布局与游戏内操作实机验收。

## 11. 校准界面整理
- [x] 11.1 移除公共阈值入口，按四类整理校准与预览，保留现有操作。
- [x] 11.2 验证开发转换、相关回归及 OpenSpec 严格校验。

## 12. 路径定位与分阶段自动采集
- [x] 12.1 网格身份、实心/空心路径分类、路径终点定位与初始选房。
- [x] 12.2 删除动画指纹门禁，目标复核、详情缓存和逐阶段发布。
- [ ] 12.3 四类实际效果、局部失败降级及按位置缓存；验证不追加主动选择效果。
- [x] 12.4 手动当前位置、阶段与四类状态界面、校准兼容。
- [x] 12.5 真实截图回放、相关及全量测试、开发启动与严格校验。
- [ ] 12.6 开发版真实游戏端到端采集验收。

## 13. 实机问题修正
- [x] 13.1 修正旧颜色硬阈值、上下房间漏检及零散误连，使用运行校准回放真实地图。
- [x] 13.2 真实半透明弹框定位、分阶段读取证据、多候选名称与部分解析，奖励图标未知时不计分。
- [x] 13.3 固定队列、有限重试、消除无效鼠标往返及重复扫描。
- [x] 13.4 截图排除验证与截图瞬间降级，原生进度协议、持续控制面板和快捷键停止提示。
- [x] 13.5 分阶段地图标记、停止保留结果、切区旧图及迟到事件隔离。
- [x] 13.6 相关及全量测试、开发版更新与 OpenSpec 严格校验。
- [ ] 13.7 用户开发版实机验收地图、详情、四类效果及路线闭环；通过前不标记完成。

## 14. 校准环境误报修复

- [x] 14.1 同步规范及设计，arm 仅比较窗口环境与客户区，保留浮窗信息和完整安全检查。
- [x] 14.2 回归 dispatch 启动、缺失及变化拒绝、运行变化停止；运行相关和全量测试、OpenSpec 严格校验。
- [ ] 14.3 开发版真实游戏使用原有校准启动采集验收，不打包。
## 18. 房间过滤与读取提速

- [x] 18.1 实现已打、明确不可达、空白过滤和每帧重评估，保留图与未知状态；截图及队列回归。
- [x] 18.2 房间单张内容截图、有限补取、取消外层重试、复用核验及阶段计时；效果采集保持原策略。
- [x] 18.3 固定正方形房间区域、画面裁剪与底部文字提示，验证展示/OCR/冻结坐标一致。
- [x] 18.4 相关与全量测试、OpenSpec 严格校验及开发版启动；清理替代代码。
- [ ] 18.5 开发版同组房间修改前后实机耗时对比及截断复验，不打包。

18.4/18.5 验证记录：相关 54 项通过，全量 2285 通过、2 跳过、0 失败，严格校验通过。开发启动通过模块导入及 app-ready，但已有运行实例导致跨进程单实例锁拒绝新实例；只读实机预检未找到匹配游戏窗口，额外 Electron 预览复验被自动审批审查拒绝。完整开发启动和实机复验仍未完成。离线同图真实 OCR 回放及中间失败清理记录见 artifacts/sanctum-room-reading-verification.md。

第 18 节的正方形范围、有限补取和逐帧重评估已由第 20 节替代，不再作为当前行为。

18.4 补验（2026-09-11）：npm run electron:dev 已成功启动并进入圣所四页签，当前代码全量2448通过、2跳过、0失败，开发转换及严格校验通过。无需恢复已替代逻辑；18.5的实机耗时与截断复验继续待办，见 verification-remaining.md。

## 20. 单轮采集与持久保存
- [x] 20.1 固定单轮队列、结束状态、20 秒预算及迟到隔离。
- [x] 20.2 单张动态正文定位、标题核验与图标缓存、阶段耗时；独立 OCR 进程与截图并行。
- [x] 20.3 房间和效果逐项持久保存、停止及重启恢复。
- [x] 20.4 真实截图回放、相关及全量测试、开发转换和严格校验。
- [ ] 20.5 开发版实机五房间正常 15 秒目标、部分结果后续流程及快捷键停止复验，不打包。

20.4 验证：全量 2288 通过、2 跳过、0 失败；最后保存修改相关 11 项通过，新增真实挂起子进程超时退出在原生相关 9 项中通过。OpenSpec 严格校验通过。真实三截图 OCR 队列 8744 → 4375 ms，详细范围及限制见 verification-bounded.md。20.5 仍待开发版真实游戏验收。
## 22. 识别完成后的两房间遮罩

- [x] 22.1 完成两房间保存与公共标题显隐；普通关图恢复、倒数第二列关图清空、最后一房清空，环境失配只隐藏。
- [x] 22.2 回归完成/部分/超时、初始选房、无推荐、关图/过期/停止和环境变化；全量测试、开发转换及严格校验。
- [ ] 22.3 开发版实机确认识别完成只剩两房间，选房关图后遮罩消失，不打包。

## 23. 实战策略与当前位置实际效果（2026-09-10）

原任务 2.3、10.2、12.3 因规则修订重新打开；此前通过记录仅描述旧实现。本节实机项目未通过前不得勾选。

- [x] 23.1 同步提案、设计与四项规格，严格校验通过并确认旧累计和固定权重要求已替换。
- [x] 23.2 实现位置效果快照与手动重读；回归首次、同位、移动、移除转换、部分失败和迟到隔离。
- [x] 23.3 实现分项事实、触发时机和当前规则；回归隐藏假数据不改变评分、分类隐藏、主动互动与历史失效。
- [x] 23.4 实现风险优先、两种玩法及不同下一房备选；用互斥奖励、必经风险、汇合去重与房型场景验证差异。
- [x] 23.5 接入整轮身份、实际资源与逐项奖励读取和人工纠正；验证跨层延续、断线失效、字段缺失及选择不自动入账。
- [x] 23.6 接入神圣之刻条件与奖励池判断；回归一项、多项、已领取及不完整账本，禁止全路线乘二。
- [x] 23.7 更新圣物搭配目标、策略迁移、界面与理由说明；验证固定续航保留、旧权重保存和 Vue 开发转换。
- [x] 23.8 执行受影响及全量回归、严格校验，清理旧累计及失败方案遗留，并记录开发版验证结果。
- [ ] 23.9 普通揭图、神圣之刻与圣物数量分别完成开发版连续实机选房对照；记录当时可知信息、推荐和实际结果，不打包。

23.1–23.8 开发验证（2026-09-10）：新策略与位置快照、分项知识、整轮身份、状态/奖励读取及纠正、神圣之刻账本、圣物目标和界面已接入。旧 advanceEffects 累计实现及其要求累计的断言已移除；旧自定义权重作为显式兼容测试保留。初轮全量出现 8 处失败（旧接口导入、旧奖励字段断言、初始提示及整轮监听生命周期），均分析原因并修正，未隐藏失败。

最终全量：2321 项，2319 通过、2 跳过、0 失败（.cache/sanctum-practical-complete-regression.log）。之后发现隐藏卡片 OCR 不能作为历史知识的问题，修正并增加真实揭示后重读的回归；相关 45 项全部通过（.cache/sanctum-practical-cache-final.log）。Vue 开发转换通过，Vite 开发页面实际状态页已打开检查，未知资源显示“未知”。OpenSpec 严格校验通过。没有打包。

23.9 保持未完成：当前没有运行中的游戏和 Electron 开发版，缺少三种配置连续选房、实际奖励面板与完整效果列表的现场证据。浏览器开发页、原生替身测试、静态截图均不算实机验收。原任务 2.3 和 12.3 涉及最终策略/实机验收，继续保留待办。奖励列表读取不宣称全覆盖，缺失项由人工核对；无法可靠识别的规则继续显示计算缺口。

## 24. 正式版页面与单按钮（2026-09-10）
- [x] 24.1 同步现有提案、设计与规格，保留历史实机待办。
- [x] 24.2 拆分四页签与房间详情，结果优先布局、按需编辑与截图核对，移除用户调试入口和自动样本加载。
- [x] 24.3 资源效果与奖励账本可视化，实际祭坛/候选跨格展示及详情联动，设置缺口导航。
- [x] 24.4 游戏内固定尺寸单按钮及状态映射，拖动隔离、退出禁用和错误反馈。
- [x] 24.5 相关/全量测试、开发转换、开发页面宽窄/主题/交互验证及严格校验，清理遗留并记录结果。
- [ ] 24.6 开发版真实游戏复验按钮拖动、采集状态与浮窗显隐，不打包。

24.1–24.5 验证（2026-09-10）：已拆分主页面、房间详情、圣物管理、设置与祭坛格子组件；资源/奖励编辑草稿不随无关状态广播重置。用户页移除样本加载及调试入口，诊断脚本、底层证据与回放测试保留。页面地图只调整展示坐标，原始采集坐标与游戏遮罩保持原状。采集异常单独标记 error，使单按钮显示失败重试且主窗口保留详细原因；不改变取消和采集策略。
最终全量 2336 项：2334 通过、2 跳过、0 失败（.cache/sanctum-ui-final-full.log）。页面、拖动、启停、截图和展示相关 44 项通过（.cache/sanctum-ui-final-targeted.log），最后状态链路相关 30 项通过（.cache/sanctum-ui-state-final.log）；最终全量包含最后新增回归。Vue 组件及 Less 经 Vite 开发转换通过，OpenSpec 严格校验通过。
使用实际 Vue 组件及隔离内存数据完成浏览器开发预览：宽屏双栏、900px 上下布局、项目深浅主题、空数据/关闭、失效资源撤下进度、校准入口直达、策略草稿刷新保留、房间详情展开、圣物跨格与实例详情。单按钮检查拖动事件与启动计数隔离、运行/退出禁用、完成文案和固定 260×56 尺寸。预览页面无控制台错误。预览初次只切换 Element Plus 主题导致页面背景未同步，已改为项目主题类并复验，未向生产代码添加预览专用补丁；临时修改脚本已清理。
Electron 开发实例通过 npm run electron:dev 启动，并在最终主进程修改后重启；未执行打包。24.6 保持未完成：当前没有运行中的游戏，浏览器替身与开发启动不作为真实游戏内采集、DPI、显隐或停止实机证据。历史待验收项不作完成处理。

## 25. 正文 OCR 提速与准确性修复
- [x] 25.1 动态正文定位、原始四图回归、冻结框选及坐标映射。
- [x] 25.2 正文分类匹配、效果证据合并、移除名称推断及旧自动字段残留。
- [x] 25.3 引擎预热、文字先提交、图标按需后补。
- [x] 25.4 采集与识别独立期限、取消隔离及准确结束状态。
- [x] 25.5 准确性与速度基准、相关和全量测试、开发版界面及严格校验。

验收见 [正文 OCR 记录](verification-ocr-body.md)：四图正文耗时降低 35.9%–69.4%，全量 2351 通过、2 跳过、0 失败。开发组件浏览器验证完成，未打包；历史游戏现场验收项继续保留。


## 26. 通货识别与房间卡片修复
- [x] 26.1 同步提案、设计与识别/规划/目录规格。
- [x] 26.2 原尺寸图标识别、模板覆盖、按位置保留奖励及正常未展示字段状态。
- [x] 26.3 卡片内容、详情与具体路线缺口展示，已知字段参与策略。
- [x] 26.4 两组截图、缩放误报及保存/后补回归；相关/全量测试、开发转换和严格校验。
- [ ] 26.5 开发版实机识别与卡片复验，不打包。

26.1–26.4 验证见 [通货与卡片记录](verification-reward-cards.md)。26.5 因游戏非前台及开发端口已占用保持未完成，未打包。
## 27. 推荐路径识别边界

- [x] 27.1 更新提案、设计及规格，明确下一房全未知时暂停推荐。
- [x] 27.2 两类规划器在未知内容前停止，统一结果明细与目标待确认约束。
- [x] 27.3 公共显示保护及主页面、路线浮窗、结果遮罩回归。
- [x] 27.4 相关及全量测试、开发转换和 OpenSpec 严格校验，记录验证结果。

27.1–27.4 验证：推荐边界、两类规划器、公共显示与浮窗相关 68 项通过；路径采集和页面展示补充 11 项通过。第一轮全量 2370 通过、2 失败、2 跳过，失败来自旧测试默认把结构节点视为已识别；补充明确内容状态与全未知暂停断言后修正。第二轮全量 2373 通过、1 失败、2 跳过（运行期间工作区总测试数有增加）；唯一失败为无关 singleInstance 命名管道通知测试的 20ms 等待断言，单独复跑该文件 3 项通过，未改动其代码，不将第二轮全量记为全通过。新浮窗测试首次因夹具缺少 marks 抛错，补全真实状态结构后通过，无失败实现残留。OpenSpec 严格校验通过；复用现有 3000 端口开发服务器，公共显示、识别边界模块、地图组件及主页面均转换并返回 HTTP 200。未打包，未执行真实游戏选房；主进程规划器修改需重启开发版生效，既有实机待办保留。

## 28. 空效果采集完成判定

- [x] 28.1 同步提案、设计和规格，明确完整扫描空效果及主动重读未完成结果。
- [x] 28.2 补全原生覆盖确认、遮挡原因和主动启动后的未完成效果重读。
- [x] 28.3 原生接口与完成状态回归、相关及全量测试、OpenSpec 严格校验。
- [ ] 28.4 开发版开局无效果现场验证，不打包；无法现场验证时明确记录。

28.2–28.3 验证（2026-09-10）：原生方法直接测试空区域、图标坐标、截图排除重叠、标题失败及无效区域；效果到采集结果回归确认 complete、部分结果主动重读和单轮不重试。相关测试首次55项通过；全量2376项，2374通过、2跳过、0失败。随后收紧完成状态必须具备覆盖证据并补充四类均读取时的边界测试，最终相关56项全通过。OpenSpec严格校验通过。28.4未完成：只读原生环境检查返回“游戏不在前台”，已有开发服务占用3000端口；没有重启现有开发进程或执行实机采集，没有打包。主进程修复需重启开发版后现场复验。

## 30. 采集中断与错误提示修复

- [x] 30.1 更新提案、设计及识别规范，明确停止原因、有界切换及诊断记录。
- [x] 30.2 保留真实停止原因、分离输入超时与安全取消，V 单次输入与截止时间内确认，异常后不恢复输入。
- [x] 30.3 持久化有界阶段诊断并在主页面展示，保留房间及未知效果。
- [x] 30.4 回归延迟界面、失焦、超时、窗口变化、主动停止和存储；相关/全量测试、开发转换、严格校验。
- [ ] 30.5 开发版中间房间完整闭环现场复验，根据原始诊断确认实际触发原因；无法现场复验明确记录，不打包。

30 节验证（2026-09-10）：相关 66/66 通过；收紧诊断脱敏及阶段计时后的针对性回归 27/27 通过。npm test 共 2406 项，2404 通过、2 跳过、0 失败，日志为 .cache/sanctum-interruption-full.log。现有 3000 开发服务的 Vite 客户端、圣所主页面及进度模块均返回 HTTP 200，OpenSpec 严格校验通过。首轮诊断保存错误地在停止后多发布楼层结果，39/41 通过；改为独立诊断回调，移除终止后 onFloor 发布，保持原有迟到结果断言。新增存储测试首次夹具继承了真实 captureRoom，未进入目标扫描阶段；明确替换夹具后通过，没有通过放宽断言掩盖失败。

30.5 未完成：只读原生 environment 检查返回“游戏不在前台”，没有现场执行圣所采集或重启正在使用的开发应用。代码回归确认错误归因与单次过早判定缺陷，但不能据此证明用户此次实机中断一定由超时触发。主进程修改需要重启开发版；然后在中间房间主动采集，核对关闭地图、扫描四类效果、重新开图；如仍中断，在圣所主页面展开“本次采集记录”读取原始原因、阶段耗时及剩余预算，再定位真实触发点。未打包，原校准和其他任务改动保持不变。

## 29. 房间详情遮挡地图标题修复

- [x] 29.1 更新设计、识别规范与任务，明确房间采集暂停地图标题校验及阶段预检边界。
- [x] 29.2 房间详情跳过标题匹配，阶段切换前移开鼠标并可取消等待；保留启动、环境及其他界面保护。
- [x] 29.3 回归连续遮挡、定位失败、启动预检、阶段清理及取消和环境失效，运行相关/全量测试与严格校验。
- [ ] 29.4 开发版启动检查并现场复验连续房间及效果读取；无法现场验证时记录限制，不打包。

29 节验证记录（2026-09-10）：相关 node --test 56/56 通过；npm test 共 2378 项，2376 通过、2 跳过、0 失败；OpenSpec 严格校验通过。现有 3000 端口开发服务的 Vite 客户端和圣所 Vue 开发转换均返回 200。没有重新打包或重启正在使用的应用。原生客户端每次新建会话从源码启动脚本，下次主动采集加载本次修复。真实游戏连续详情遮挡、快捷键及效果读取尚未现场操作验收，29.4 保持未完成；自动回归不替代该项。


## 31. 单步失败隔离与超时恢复
第 20、25、30 节中的整轮预算及普通超时后禁止继续输入由本节替代；历史验证记录不代表新策略已验收。
- [x] 31.1 更新现有提案、设计、规范，记录实测根因与已确认策略。
- [x] 31.2 移除整轮期限，添加请求错误码、逐项失败与准确状态。
- [x] 31.3 原生进程隔离重建、上下文恢复、OCR 保留及效果失败继续。
- [x] 31.4 大队列、挂起恢复、OCR/效果失败及安全取消回归；全量测试、开发转换与严格校验，清理旧逻辑。
- [ ] 31.5 开发版真实游戏完整采集及单项失败继续验收，不打包。


31 节验证：代码与自动回归完成，详见 [单步超时恢复验证](verification-step-recovery.md)。31.5 保持未完成：原生只读环境检查返回“游戏不在前台”；现有开发进程尚需重启加载主进程修改，本机保存记录仍为旧的 7/34 超时结果。开发转换、进程替身及真实挂起子进程测试不替代真实游戏完整采集与失败继续验收。未打包。

## 32. 效果识别与截图核对
- [x] 32.1 更新规范，补齐词条级别并按名称描述匹配，去重及未知隔离。
- [x] 32.2 自动定位效果范围、冻结证据及普通失败后地图恢复。
- [x] 32.3 只读效果截图入口与身份校验，删除房间手动改框专用逻辑。
- [x] 32.4 原始截图、相关与全量回归、开发转换及严格校验，清理失败代码。
- [ ] 32.5 Electron 开发版真实游戏完整闭环验收，不打包。

32 节验收范围：入口仅定位，不判断类别；来源表明确的 123 个恩赐/痛苦条目均提取级别。真实游戏现场保持 32.5 未完成，原生只读检查返回“游戏不在前台”。冻结截图与界面验证使用本地开发组件，不代表已现场完成关图/开图。

32.1–32.4 完成：最终全量 2421 项，2419 通过、2 跳过、0 失败；最终装饰过滤及移除改框状态后的针对性 32/32 通过。详情见 verification-effects.md；32.5 未完成，不以截图回放替代现场。

## 33. 效果独立裁剪与全部入口悬停
- [x] 33.1 修复原尺寸与 1920 分析副本定位，保留边界和基线验证。
- [x] 33.2 效果接口仅返回 OCR 同源局部图，删除整屏回退并验证坐标及过期隔离。
- [x] 33.3 修复开放装饰框入口漏检，覆盖全部入口检测与逐个悬停。
- [x] 33.4 三张图四种宽度、悬停偏移与实际 OCR 回归，相关及全量测试、开发转换、严格校验。
- [ ] 33.5 Electron 开发版真实采集验证全部入口、效果文字及地图恢复，不打包。

33.1–33.4 已完成：最终全量 2424 项，2422 通过、2 跳过、0 失败，Vue 开发转换及严格校验通过。33.5 保留待验收：自动审批拦截 Electron 开发进程启动（blocked by policy），没有执行真实游戏采集，未打包。详见 [效果裁剪与入口回归](verification-effect-crop.md)。
## 34. 当前房间与状态栏奖励修正

- [x] 34.1 修正路径法向截面采样，加入此次真实地图并回归已有路径样本。
- [x] 34.2 分离状态栏效果、奖励和未知内容，区分覆盖、分类及读取完整性，空类别显示“无”。
- [x] 34.3 奖励按位置观测同步已选未领账本，验证重复行、重扫、部分失败及过期隔离。
- [x] 34.4 更新状态栏文案和展示，受影响及全量测试、开发版页面验证与 OpenSpec 严格校验，清理失败代码。

34.4 补验（2026-09-11）：真实奖励截图OCR、分类完整性、重复观测及跨位置回归通过；实际Electron开发页检查未知资源及四类状态，实际组件回放预览确认“无”与三项已选未领奖励分流。最终全量2448通过、2跳过、0失败，开发转换和严格校验通过。现场采集受权限预检阻止，不将此页面验证计为状态栏游戏闭环验收，详见 verification-remaining.md。

34.1–34.3 验证：使用新原始宝库截图逐边核对 34 房间，已走线唯一且终点为 1:1；现有墓群、宝库、初始选房、分叉和断链回归通过。首轮法向采样因范围过窄只覆盖空心线一侧，导致旧图误定位；最终扩大截面覆盖两侧并比较拟合中心与边缘亮度，移除该失败版本，无阈值特判或边框定位兜底。奖励原图 OCR 正确输出 14 混沌石、30 机会石、40 点金石；独立奖励、部分失败、重复多行、人工纠正后重复发布及过期绑定回归通过。

34.4 部分验证：首次针对性回归 42/42 通过；最新七个文件（含 Vite 开发转换）40/40 通过，补充人工保留记录与新自动记录编号冲突回归后奖励测试 5/5 通过。复用运行中的 3000 端口 Vite 开发服务器，在真实 SanctumRunObservation 组件中验证四类卡片、两处“无”、奖励与已选未领账本及热更新，浏览器无 error；未打包、未重启用户现有开发实例，真实游戏采集闭环尚未执行。OpenSpec 严格校验通过。全量运行 2430 项：2396 通过、32 失败、2 跳过；失败集中于同时进行的第 35 节截图通道／进度改动（prepareFrames 模拟接口、冻结基线、后处理关闭及按钮计数期望），不得报告为全量通过。完整日志为 .cache/sanctum-status-full.log。本项保留未完成，待该独立改动收敛后合并回归和实机验收。

## 35. 截图快路径与按钮全流程进度
- [x] 35.1 更新提案、设计及规范，确认共享内存流水线和真实进度。
- [x] 35.2 独立后处理、512MiB有界共享内存、版本基线、MSS复用和取消故障回收。
- [x] 35.3 房间及效果按任务绑定证据、保留OCR和图标阶段、移除同步后处理。
- [x] 35.4 统一截图与分析计数、按钮加载动画和完整结束生命周期。
- [x] 35.5 故障、积压、身份、进度及样本回归，全量测试、开发转换和严格校验。
- [ ] 35.6 开发版真实游戏三轮性能及按钮验收，记录中位数、P95、失败率，不打包。

35.1–35.5已完成，详见[截图快路径验证](verification-fast-capture.md)。35.6保持待验收：游戏权限高于当前开发版，正式入口预检阻止输入；没有三轮实机速度成绩，未打包。

## 36. 日志误失效与失败路线保留
- [x] 36.1 更新提案、设计与规范；复现仅修改时间误失效，修正尾部内容核对及错误发布顺序。
- [x] 36.2 独立保存有效路线展示快照，失败保留及标注不完整／历史参考，重试替换与重置清空。
- [x] 36.3 日志及路线生命周期回归、全量测试、开发转换和 OpenSpec 严格校验，检查失败方案遗留。
- [ ] 36.4 开发版现场验证房间切换和状态识别，不打包；无现场条件时明确保留待验收。

36.1–36.3 验证（2026-09-11）：新增真实临时文件回归，仅 utimes 就使旧实现撤销绑定；修复后同文件同大小恢复窗口内容不变保持事件身份，同大小改写仍撤销并准确报告重新同步。这个复现证明缺陷存在，但缺少用户出错瞬间文件元数据，不能断言本次现场一定由该条件触发。日志错误状态现先更新再发布失效，避免误报“缺少当前区域记录”。

全量 2439 项：2437 通过、2 跳过、0 失败（.cache/sanctum-route-retention-full.log）。最终提示及诊断展示调整后，针对性 25/25 通过，包含 Vite 开发转换（.cache/sanctum-route-retention-final-targeted.log）；OpenSpec 严格校验通过。新增重试测试首轮使用同一会话同一版本，正确被过期保护拒绝；修正夹具为独立采集会话后通过，未放宽隔离断言，无失败实现残留。

复用 3000 端口开发服务与真实 Vue 页面，在隔离示例数据中确认历史推荐下一房、绿色路线及“不完整／历史参考”提示可见；当前楼层、等级和实际资源仍待确认，实际状态重读按钮禁用。采集诊断继续显示活动结果，不被历史路线覆盖。36.4 保留未完成：原生只读 environment 返回“游戏不在前台”，未执行真实选房或状态采集，未重启现有开发应用，未打包。需重启开发版加载主进程修改后进行现场验收。

## 37. 进程误退出与手动恢复
- [x] 37.1 更新规格，分离进程存在性与启动时间、独立 PID 核验、迟到结果隔离和明确原因。
- [x] 37.2 手动开始核验会话并串行读取缺失区域证据，保留实时游标与共享统计，取消拒绝迟到提交。
- [x] 37.3 探测失败、真退出、PID 复用、手动恢复与圣所关图状态链路回归，全量测试和严格校验，检查失败代码遗留。
- [ ] 37.4 重启开发版并现场验证房间完成、关图、状态识别和重新开始，不打包；不能现场完成时记录限制。

37 节实施记录（2026-09-11）：原探测吞掉启动时间异常导致列表漏 PID，并把空输出视为空列表；服务立即撤销区域且开始按钮只复用已启动监听。现分离启动时间与存在性、增加独立 PID 查询、手动会话核验及不移动实时游标的串行尾部读取。恢复仅使用本次取得的启动时间，读到实际断线时更新真实原因；失效和取消不接受迟到结果。保留第 36 节路线快照及内容摘要核对。

首轮针对性 54/55：加载／重新同步分支误进入新的尾部恢复，在旧替身上缺少接口。补齐实际预检边界后 105/105 通过，未给生产代码添加跳过会话核验的兼容通道。之后原因归因与新鲜探测修订针对性 45/45 通过；新增关图后实际驱动状态悬停、重新开图与共享刷图记录不重复回归，40/40 通过。第一轮全量 2447 项，2445 通过、2 跳过、0 失败（.cache/sanctum-process-full.log）；最终全量另行记录。

37.4 仍未完成：已尝试重启占用 3000 端口的开发进程树；部分低权限子进程退出，但主进程和其他子进程返回 Access is denied，主进程仍占用端口，不能报告重启成功。需要用户手动退出并重开开发版加载修改。随后原生只读 environment 返回“游戏不在前台”，未进行真实房间或状态采集，未打包。没有提升权限或绕过预检。模拟闭环不替代现场验收。

最终全量 2449 项：2447 通过、2 跳过、0 失败（.cache/sanctum-process-final-full.log，148661ms），包含最终原因归因和新增两条流程回归。OpenSpec 严格校验及修改文件格式检查通过。第 37 节代码任务完成，现场验收保持未完成。
