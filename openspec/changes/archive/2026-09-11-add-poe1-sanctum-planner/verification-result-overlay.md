# 识别后遮罩保留验证

- 单轮结束后保存独立展示快照，采集进程关闭后复用公共标题检测。普通关图隐藏再开恢复；新有效推荐替换，失败不覆盖；确认出口及显式重置清空。
- 八列0–7完整且第7列出口确认、下一房为第6列时，标记关图后清空。本次结果提交前的检测不能触发清空；失焦、过期、环境变化、缺少标题输出只隐藏。
- 专项25项通过：`.cache/sanctum-result-final-related.log`。全量2309项，2307通过、2跳过、0失败：`.cache/sanctum-result-full.log`，包含最终新增的采集完成链路测试及Vite开发转换。OpenSpec严格校验通过。
- 首轮专项有1项失败：替换路线测试在stop清除活动观察后，仅替换楼层却未提供新观察坐标，导致正确保留旧结果。测试已补齐新采集观察，并增加服务采集结束到结果保存的链路验证；没有为测试放宽坐标检查。
- 复用已运行的Vite开发服务，在隐藏Electron窗口加载实际SanctumOverlayView。使用已有实机地图回放数据与模拟标题事件，DOM验证打开2房间1连线、关闭0、重开2房间1连线；截图已检查。记录：`.cache/sanctum-result-preview-check.json`、`.cache/sanctum-result-preview.png`。此项不是游戏端到端验收。
- npm run electron:dev诊断启动因3000端口已有开发服务而退出，未关闭既有实例。原生只读环境预检返回“游戏不在前台”，没有执行游戏键鼠操作；22.3实机验收保持未完成。未打包。
- 清理了前一版结果依赖运行状态/日志上下文以及停止清除绑定的实现假设，保留原采集安全检查和其他任务的路径样式变更；OpenSpec已同步最新关图保留规则。
