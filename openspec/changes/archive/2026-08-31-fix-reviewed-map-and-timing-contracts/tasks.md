## 1. 地图崇高石事务确认

- [x] 1.1 在每颗崇高石动作前后验证稀有度、结构化显式词缀计数范围和严格递增，异常时沿当前格失败恢复路径立即停止。
- [x] 1.2 扩展地图崇高石定向测试，覆盖正常逐颗补满、计数未增加、稀有度变化、动作后计数缺失及计数越界，确认失败后不再使用下一颗通货。

## 2. 时序校准残留清理

- [x] 2.1 从 Python 运行时清单移除已删除的 `timing_calibration_probe.py`，并确认不存在其他自动时序校准代码、协议或测试残留。
- [x] 2.2 按本变更增量从 `automation-operation-delay` 主规范删除本机校准、时序推荐和原子应用/会话撤销三项要求，保留固定时序契约。

## 3. 开发版验证

- [x] 3.1 运行地图崇高石定向测试及相关时序/运行时测试，确认两个 P1 的回归覆盖通过。
- [x] 3.2 运行 `npm run runtime:verify`、完整 `npm test`、`npm run build`、`git diff --check` 和 OpenSpec 严格校验；如实记录失败且不进行 Electron 打包。
