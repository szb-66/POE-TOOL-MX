## 1. 配置与界面

- [x] 1.1 在通货常量、全局坐标默认值和共享通货目录中增加 `binding`，并接入本地图标与配置引导。
- [x] 1.2 为物品制作预设增加默认关闭的 `moduleTwo.enableBinding`，并在点金石模式显示开关。
- [x] 1.3 为地图与海图预设增加默认关闭的 `binding.enabled`，并在点金石模式显示开关。

## 2. 预检与自动化

- [x] 2.1 修改物品和地图通货清单，使点金石模式在 `alchemy` 与 `binding` 之间二选一，混沌石模式保持 `alchemy`。
- [x] 2.2 修改物品脚本生成逻辑和制作模板，让启用选项的点金预处理与重铸循环使用 `binding`。
- [x] 2.3 修改地图脚本和模板状态机，让异界地图与航海海图在启用选项时对所有点金分支使用 `binding`。
- [x] 2.4 将 `binding` 接入两套模板的严格名称预检、失败上下文和消耗统计。

## 3. 文档与验证

- [x] 3.1 更新制作与地图帮助内容，说明高阶点金石开关只影响点金石模式。
- [x] 3.2 增加预设迁移、界面显示、通货清单、脚本替换、坐标失败和消耗账本测试。
- [x] 3.3 运行受影响测试、Vite 开发转换和 OpenSpec 严格校验，并检查最终 diff 没有回退或遗留无关代码。
