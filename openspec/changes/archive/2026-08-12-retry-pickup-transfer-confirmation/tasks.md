## 1. 三轮确认回归测试

- [x] 1.1 为共享取件确认函数添加第一、第二、第三轮确认清空时立即成功且不再点击的测试
- [x] 1.2 添加三轮均复制到物品时返回 `inventory-full`、最多点击三次和释放输入的测试
- [x] 1.3 添加复制不可读、点击失败和运行中失焦时安全停止且不继续重试的测试
- [x] 1.4 更新普通仓库与君锋镇行为测试，删除“点击后不复制、单次点击直接成功”的冲突断言

## 2. 共用取件实现

- [x] 2.1 在共享 Python 输入模块增加固定三轮的取件专用点击后复制确认函数，并保留入库单次函数不变
- [x] 2.2 将普通仓库与君锋镇共用候选循环接入三轮确认，只在确认清空后增加 `pickedItems`
- [x] 2.3 三轮仍有物品时发送 `inventory-full` 和准确剩余数量，并确认所有停止路径释放输入
- [x] 2.4 清理与旧单次取件或此前失败确认方案相关的废弃函数、分支、常量和测试残留

## 3. 开发版验证

- [x] 3.1 运行共享输入、背包入库、普通仓库和君锋镇相关测试
- [x] 3.2 运行完整 `npm test` 并检查没有调试日志或失败方案残留
- [x] 3.3 运行 OpenSpec 严格校验
- [x] 3.4 在开发版手动验证首次成功、重试成功、三次失败、失焦停止和输入释放；不执行打包
