## 1. 回归反馈环与数据契约

- [x] 1.1 为同文案多 ID 被丢弃、隐式错误回退 explicit 两个根因添加先失败的目录/匹配回归测试
- [x] 1.2 增加多行复合词缀、数字占位组合、类别消歧、合并规则和未决候选的代表性国服剪贴板夹具
- [x] 1.3 升级交易目录 schema 与验证器，使记录键和类型 stat ID 唯一且允许 matcher 对应多个候选
- [x] 1.4 添加当前版本 `Ctrl+C` 详细复制夹具，覆盖 `Fractured Prefix/Suffix Modifier` 及国服对应翻译；保留 `(fractured)` 旧格式兼容夹具

## 2. 完整目录与覆盖审计

- [x] 2.1 修改官方目录构建器以逐条保留所有结构有效的物品、词缀类型和 stat ID，并合并内置 resolver 元数据
- [x] 2.2 扩展目录生成器以保存最小必要 matcher、候选上下文和 resolver 字段，并生成版本化的完整内置目录
- [x] 2.3 实现可重复的 stats/items 覆盖审计，报告保留、唯一、歧义、明确拒绝和静默丢弃数量，并让静默丢弃导致失败
- [x] 2.4 为内置目录设置有效记录数、来源、赛季版本和非陈旧性契约测试，防止再次退化为少量手写词缀
- [x] 2.5 以当前国际服官方目录审计通用 stat ID/类型，并按 ID 与腾讯国服 matcher 分层合并；历史国服项目只作带真实版本元数据的翻译兜底

## 3. 解析、消歧与查询模型

- [x] 3.1 实现最长多行优先和有上限的数字占位组合匹配，并保留原文与掷值
- [x] 3.2 删除跨类型 ID 回退，按 modifier 类型严格筛选候选并对不匹配项返回可解释状态
- [x] 3.3 实现基于物品类别、完整文本、词缀元数据和目录 resolver 的确定性选择及声明式聚合
- [x] 3.4 扩展查价模型与主进程清理器以携带未决候选，并校验候选 ID 确实存在于当前目录且类型匹配
- [x] 3.5 让详细复制元数据成为当前版本的类型真源：破碎/工艺/附魔/隐式语义优先于 Prefix/Suffix 位置，自动生成正确类型查询且不要求用户判断

## 4. 运行态与浮窗恢复体验

- [x] 4.1 让官方 stats/items 刷新仅在完整验证成功后原子替换目录，失败时保留内置或最近成功目录
- [x] 4.2 增加目录来源、版本、加载/降级原因和重试状态的服务与 IPC 契约，并保持账号 Session 和限流逻辑不变
- [x] 4.3 在查价浮窗中为歧义词缀提供候选展开、选择后重新查询、跳过继续查价和打开官方市集操作
- [x] 4.4 保持普通已映射词缀的整行选择、数值范围和初始勾选体验，并明确标识本次查询排除的条件

## 5. 开发版覆盖验证与清理

- [x] 5.1 运行查价单元/集成测试和官方目录覆盖审计，确认根因夹具转绿且静默丢弃为零
- [x] 5.2 回放覆盖装备类别、品质、词缀类型和特殊状态的当前 `Ctrl+C` 详细复制国服真实语料，确认每件官方可查物品均生成有效查询且类型自动确定
- [x] 5.3 验证查价 Vue 页面与浮窗的 Vite 开发转换，再运行完整 `npm test` 与 OpenSpec strict 校验
- [ ] 5.4 在 `npm run electron:dev` 中验证游戏快捷键捕获、候选选择/跳过、重试和官方页面跳转；不执行打包
- [x] 5.5 搜索并删除所有临时调试日志、失败方案和一次性探针，记录实时官方接口无法访问时使用的夹具与限制
