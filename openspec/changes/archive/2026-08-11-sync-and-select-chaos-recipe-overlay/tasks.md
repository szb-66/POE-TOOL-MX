## 1. 回归信号与根因确认

- [x] 1.1 在正确的主界面刷新 → store 派生选择 → 主进程控制状态调用链上建立失败测试，复现刷新完成后浮窗仍保留旧数量或旧可执行状态
- [x] 1.2 用测试调用顺序确认快照发布与完整运行时提交之间的根因，并记录最终采用的单一提交边界

## 2. 刷新状态一致性

- [x] 2.1 让商城配方运行时载荷携带按配方分组且经过快照过滤的单件选择，同时兼容现有当前配方的一维选择字段
- [x] 2.2 调整刷新提交顺序，使页面快照、仓库页、目标套数、配方选择和控制浮窗在刷新 Promise 完成时处于同一最终状态
- [x] 2.3 覆盖首页、商城详情页和浮窗刷新入口，确认全部入口复用同一状态提交逻辑且没有重复刷新副作用

## 3. 浮窗配方切换

- [x] 3.1 在控制状态中复用现有配方目录生成带当前可取数量的选项，并输出切换可用性和禁用原因
- [x] 3.2 增加经过配方 ID 与活动计划校验的选择 IPC/preload/API，并在主进程即时更新后通知主窗口持久化现有 `activeRecipeId`
- [x] 3.3 在商城配方 store 接收浮窗选择，调整目标套数、保存原设置并通过既有串行运行时同步完成状态收敛
- [x] 3.4 在取件按钮旁加入原生配方选择控件，运行、暂停、预览和处理中禁用，并继续使用内容尺寸观测自适应窗口
- [x] 3.5 将空闲按钮文案改为“取出配方”，保留“停止取件”和“继续取件”状态文案

## 4. 开发版验证与清理

- [x] 4.1 运行目标 Node 测试，覆盖首页刷新同步、各配方数量、单件选择隔离、选择往返、活动计划保护和按钮文案
- [x] 4.2 使用 Vite `transformRequest` 验证浮窗 Vue 组件开发转换，并运行完整 `npm test`
- [x] 4.3 运行 `openspec validate sync-and-select-chaos-recipe-overlay --strict`，检查并删除诊断日志、失败方案与重复同步代码，不执行打包

## 5. 无焦点浮窗切换回归

- [x] 5.1 增加失败测试，证明 `focusable: false` 控制浮窗不得使用依赖焦点的原生选择框
- [x] 5.2 将配方选择改为复用现有 `pointerdown` 交互的可展开按钮菜单，并保留活动计划禁用、数量展示和窗口自适应
- [x] 5.3 运行目标测试、Vite 开发转换、完整测试和 OpenSpec 严格校验，清理失败方案且不打包
