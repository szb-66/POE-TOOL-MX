## 1. 规则模型与匹配行为

- [x] 1.1 扩展 JavaScript 黑名单规范化，使规则保留 `enabled`、旧规则默认启用，并让匹配器忽略停用规则且运行配置保留完整状态
- [x] 1.2 扩展 Python 黑名单规范化与匹配逻辑，采用相同的默认启用和停用跳过语义

## 2. 黑名单开关界面

- [x] 2.1 在背包黑名单表格增加单项“生效”开关，新增规则显式默认启用，并通过现有 store 保存切换后的完整规则
- [x] 2.2 保持停用规则内容可见，并在自动入库模块运行时禁用生效开关及保留现有修改提示

## 3. 回归测试与验证

- [x] 3.1 增加 JavaScript 测试，覆盖旧规则迁移、启停匹配行为和运行配置中的状态保留
- [x] 3.2 增加 Python 测试，覆盖旧规则默认启用、显式停用不匹配及重新启用恢复匹配
- [x] 3.3 增加页面契约测试并运行黑名单相关测试、项目检查及 OpenSpec 严格验证
