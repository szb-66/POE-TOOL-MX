## 1. 目录模型与规则数据

- [x] 1.1 将交易目录 schema 升级并为 stat 记录增加受校验、可去重的稳定 refs
- [x] 1.2 将 Awakened PoE Trade 3.29.102 的综合规则转换为版本化纯数据，记录固定提交与第三方许可
- [x] 1.3 扩展目录生成器，把固定 Awakened ref 与国际服、腾讯国服官方 ID及中文 matcher关联，由 Sidekick 补充当前剪贴板 matcher并保留综合规则
- [x] 1.4 增加综合规则结构、目标 namespace、来源存在性和重复身份校验，并输出规则覆盖审计
- [x] 1.5 重建内置国服交易目录并验证在线刷新和内置降级目录使用同版本规则

## 2. 状态事实与官方过滤

- [x] 2.1 扩展物品解析器，可靠识别秽生并复用现有未鉴定、腐化、复制、分裂、忆境和古灵势力事实
- [x] 2.2 建立集中状态白名单和官方字段映射，创建 `stateFilters` 三态默认值
- [x] 2.3 扩展查价模型清理，拒绝未知键和非法枚举并兼容迁移旧布尔模型
- [x] 2.4 将非 `any` 状态序列化到对应官方 `misc_filters`，验证已鉴定正向语义和复制默认任意

## 3. 综合条件生成与安全查询

- [x] 3.1 让目录匹配结果和 stat sources 保留稳定 ref、真实 namespace、文本、数值与贡献来源
- [x] 3.2 实现综合规则求值，覆盖倍率、required、group/replaces、默认启用和来源追踪
- [x] 3.3 仅使用活动目录中可唯一解析的 `pseudo.*` 目标创建综合 stat，缺失目标计入审计并安全省略
- [x] 3.4 复用现有数值范围与模型白名单清理综合 stat，保证类型与 ID namespace 一致
- [x] 3.5 保留原始来源词缀，并在综合项默认启用时取消被吸收来源的自动启用
- [x] 3.6 验证启用综合项通过统一 stat 序列化进入 `query.stats[0].filters`，不存在仅展示不查询的旁路

## 4. 浮窗交互与展示

- [x] 4.1 在查价浮窗增加紧凑、可换行且支持键盘焦点的状态三态下拉区域
- [x] 4.2 建立官方 stat 类型中文标签与稳定颜色映射，覆盖全部受支持 namespace 和安全回退
- [x] 4.3 将来源标签放入原始、综合和未识别词缀行，保持整行选择、数字编辑、折叠和滚动行为
- [x] 4.4 验证状态修改只作用于当前物品模型，新捕获物品重新初始化且不污染全局设置

## 5. 回归验证与收尾

- [x] 5.1 增加目录生成、schema、规则覆盖和可重复生成测试
- [x] 5.2 增加所有状态解析、三态清理、旧模型迁移及官方字段序列化测试
- [x] 5.3 增加单抗、双抗、全抗、属性、生命、能量护盾、速度、伤害、暴击、恢复和偷取综合规则测试
- [x] 5.4 增加合法 `pseudo.*` 查询 JSON、缺失目标安全省略、伪造 namespace 拒绝及避免默认重复约束测试
- [x] 5.5 增加浮窗状态控件、来源标签、综合行和键盘交互契约测试并通过 Vite 开发转换
- [x] 5.6 运行查价定向测试、完整 `npm test`、OpenSpec strict 校验和开发版官方查询等价验证，不构建安装包
- [x] 5.7 检查最终 diff，清除失败尝试遗留代码并确认未覆盖两个既有 OpenSpec 变更或其他用户修改
