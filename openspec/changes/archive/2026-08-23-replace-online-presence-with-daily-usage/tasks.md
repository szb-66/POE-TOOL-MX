## 1. 每日使用数据契约

- [x] 1.1 添加版本化 PostgreSQL migration，保留并重命名已有安装记录，创建每日使用只写入口、服务端身份/时间触发函数和北京时间正式版汇总视图，同时移除旧在线对象并写明回滚。
- [x] 1.2 更新数据库策略测试与真实 CloudBase 集成测试，覆盖记录保留、同安装 upsert、当日正式版统计、开发版排除、匿名不可读和受保护字段不可伪造。

## 2. 桌面端每日上报

- [x] 2.1 将在线心跳配置和服务替换为正式版每日使用上报，开发版在认证前短路，使用北京时间日期与 00:05 跨日调度。
- [x] 2.2 持久化当天成功日期并实现 15 分钟、1 小时、6 小时的受限失败重试，保留 401 单次重新认证、请求合并、超时与退出清理。
- [x] 2.3 更新主进程接线和测试，覆盖正式版每天一次、重复启动、跨日/休眠、开发版零请求、失败重试上限、401 和计时器清理。

## 3. 开发版验证与规范

- [x] 3.1 运行每日使用相关 Node 测试、完整 npm test 和严格 OpenSpec 校验，检查最终 diff 未包含旧心跳实现或失败方案残留。
- [x] 3.2 同步主规范但保持变更未归档，记录打包、migration 与远端部署均等待用户另行确认。

验证记录（2026-08-22）：主规范已同步；未执行 Electron 打包、远端 migration 或 CloudBase 部署，change 保持 active，等待用户另行确认。
