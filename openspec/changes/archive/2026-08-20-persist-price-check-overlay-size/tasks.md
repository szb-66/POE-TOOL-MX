## 1. 尺寸状态与归一化

- [x] 1.1 为查价浮窗尺寸增加可单测的有限正整数校验、默认尺寸选择和当前显示器工作区钳制逻辑。
- [x] 1.2 扩展现有窗口状态读写，以独立 `priceCheckOverlaySize` 字段合并保存宽高，并兼容字段缺失或配置损坏的旧数据。

## 2. 查价浮窗生命周期

- [x] 2.1 创建查价浮窗时恢复保存的偏好宽高，按当前显示器限制本次实际尺寸，并继续根据本次鼠标位置计算坐标。
- [x] 2.2 监听 Windows 用户缩放会话，防抖保存最终宽高，并确保程序创建、自动定位和工作区钳制不会触发保存。
- [x] 2.3 在浮窗关闭和管理器清理时刷新待保存的用户尺寸并释放定时器，保持现有失焦关闭和鼠标离开行为不变。

## 3. 回归验证

- [x] 3.1 增加尺寸归一化测试，覆盖无配置、无效值、合法值和大于当前工作区的偏好尺寸。
- [x] 3.2 增加查价浮窗生命周期测试，覆盖用户缩放保存、关闭后恢复、应用重建后恢复、程序化 `setBounds` 不覆盖偏好以及关闭时刷新待保存值。
- [x] 3.3 运行受影响的 Node 测试、完整 `npm test` 和 `openspec validate persist-price-check-overlay-size --strict`，确认没有破坏现有查价定位与窗口状态行为。
- [x] 3.4 在 `npm run electron:dev` 开发版中手动拖动查价浮窗，验证同一进程再次打开及重启开发版后的尺寸恢复；不执行打包。
