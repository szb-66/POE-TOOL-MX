## 1. 配置、分段与价格计算

- [x] 1.1 先添加浮士德配置与价格规则单元测试，覆盖上下限语义、最后一段无上限、跨单位重叠、非法比例/折扣、无匹配、零价、向下取整和折后未真正降低
- [x] 1.2 实现版本化配置归一化、分段排序校验、混沌石基准换算和稳定原因码，使 1.1 测试通过
- [x] 1.3 实现浮士德独立本地配置持久化与运行快照，确保空白或损坏配置默认不可运行且运行中编辑只影响下一次启动

## 2. 一级页面与用户控制

- [x] 2.1 先添加路由、页面预加载、侧栏入口和主布局源码契约测试，锁定独立“浮士德”一级页面及商人语义图标
- [x] 2.2 实现浮士德路由、侧栏入口、页面和 Pinia store，提供比例、可增删排序的分段表单及逐字段验证反馈
- [x] 2.3 实现独立市集网格校准、价格窗口识别测试、开始、停止、运行状态和脱敏逐件日志界面，并在运行期间正确禁用冲突操作

## 3. Electron IPC 与自动化生命周期

- [x] 3.1 先添加 preload/IPC 合约和 manager 生命周期测试，覆盖固定状态/配置/校准/识别测试/开始/停止接口、sender 校验、单实例运行和结构化事件
- [x] 3.2 实现最小 preload 与 IPC 表面、浮士德单实例 manager 和配置 schema 校验，禁止 renderer 传入脚本路径、权限类型或通用输入原语
- [x] 3.3 接入游戏前台、DPI、页面/网格、OCR 依赖与 AutomationLock 启动预检，确保任一失败发生在键鼠输入之前
- [x] 3.4 由当前管理员 Electron 主进程通过固定浮士德 IPC 启动固定脚本，并接入 EmergencyStopCoordinator、应用退出/窗口销毁清理、幂等停止和键鼠释放

## 4. 价格窗口识别与逐件事务

- [x] 4.1 从用户提供的国服截图裁剪并脱敏保存测试夹具，覆盖当前混沌石单位、展开列表中的混沌石/神圣石和视觉“000”场景
- [x] 4.2 先添加识别与输入协议测试，覆盖页面/网格锚点、物品去重、双击→Ctrl+A→Ctrl+C、完整 1000 读取、OCR 唯一/低置信度/多候选和安全关闭
- [x] 4.3 实现固定浮士德 Python 脚本的页面预检、候选格扫描、物品简称/运行期指纹读取、价格窗口锚点和完整正整数剪贴板读取
- [x] 4.4 实现当前单位白名单 OCR、选择器展开后按精确中文文字定位目标单位，以及不依赖图标、固定坐标或列表序号的点击
- [x] 4.5 实现逐件事务状态机，将已验证配置快照的价格计划写入游戏，并严格执行换单位后重新复制数值、重新识别单位和计划一致性确认
- [x] 4.6 实现单次“上架物品”提交、警告/冷却/异常不重试、单件失败继续和页面级安全条件失效立即终止

## 5. 事件、隐私与回归测试

- [x] 5.1 添加 manager 与脚本事件集成测试，覆盖结构化成功/跳过/停止事件、稳定原因码和日志字段白名单
- [x] 5.2 实现事件解析与脱敏转发，确保不保存或输出完整物品剪贴板、运行期指纹、账号、Cookie、Authorization 或完整用户路径
- [x] 5.3 添加前台丢失、用户停止、全局紧急停止、价格窗口无法安全关闭、换单位后二次确认失败和单次提交计数的回归测试
- [x] 5.4 检查本次实现期间失败方案遗留代码、未使用资源和重复安全逻辑，删除不再需要的路径并确认无关工作未被覆盖

## 6. 开发版验证与规范收尾

- [x] 6.1 运行浮士德受影响的 Node/Python 测试与 Vue 开发转换，修复全部失败
- [x] 6.2 运行完整 `npm test`、Vite 构建和 `openspec validate add-faustus-market-repricing --strict`，且不执行 Electron 打包
- [x] 6.3 在用户已准备当前浮士德页时先执行只读“价格窗口识别测试”并确认自动取消，再用低价值物品验证混沌石同单位、神圣石转混沌石和跨分段改价
- [x] 6.4 实施和验证完成后使用 `openspec-sync-specs` 同步主规范；保留变更为未归档状态，等待用户另行确认归档
