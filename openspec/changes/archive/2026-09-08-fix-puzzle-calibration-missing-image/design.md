## Context

形状与词缀共享复制文本，但应独立解析。词缀采集不应推迟到校准阶段。

## Goals / Non-Goals

自动识别阶段完成候选词缀采集；不增加菜单或校准后采集动作。

## Decisions

复用既有候选复制流程，对词缀未知项在同一识别任务内重试一次。重试文本只填补未知形状或词缀，已取得结果不被失败重试覆盖。终止后不发起重试。独立统计词缀状态。删除独立补读 IPC、store 状态与入口。

## Risks / Trade-offs

复制或匹配仍失败时保留未知，不伪造词缀。重试仅限一次以限制耗时。
