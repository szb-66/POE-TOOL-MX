## 1. 默认边界修正

- [x] 1.1 修改共享默认边界计算，使正向 values 生成最小值、负向 values 生成最大值、无有限数值时两端为空
- [x] 1.2 确认初始模型、合并词缀、综合词缀刷新和手动消歧继续通过共享规则获得默认边界，且手动双边范围序列化保持不变

## 2. 回归验证

- [x] 2.1 更新查价单元测试，覆盖正向、真实和通用负向词缀、模型清理、无数值词缀、双边手动范围及消歧合并
- [x] 2.2 运行查价相关测试、完整 `npm test`、`npm run build` 和 OpenSpec 严格校验，并检查失败尝试没有留下无用代码或重复 artifact
