## 1. 正式开放

- [x] 1.1 开放圣所功能目录、路由和后台服务，仅保留模型训练开发限制。
- [x] 1.2 补齐解包脚本、依赖和资源，修正原生入口路径并更新发布检查。

## 2. 验证

- [x] 2.1 更新功能集合测试，增加正式路由和资源完整性回归，运行相关测试及 Vite 开发转换。
- [x] 2.2 运行 npm test 和 OpenSpec 严格校验，检查差异及记录验证边界。

## 验证记录

- 针对性测试 29/29 通过，包括 Vite 开发转换、正式路由、功能目录、原生通道与隔离目录资源读取。
- npm test 执行 2604 项，唯一失败为 mainWindowTheme 的旧正则路由提取：路由移动后跨越数组边界，误把模型训练判断为浮窗。已删除该正则实现，改为执行路由定义读取实际 meta，并覆盖圣所两个浮窗；该测试文件重跑 8/8 通过。未重复全量测试。
- OpenSpec 严格校验与 git diff --check 通过。未运行打包或安装版，也未执行游戏实机采集；已有安装程序需下次打包更新后生效。
